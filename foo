:- dynamic type_modes/3.

type_modes(gui_tracer, gdebug, det).
type_modes(gui_tracer, gspy(:(any)), det).
type_modes(gui_tracer, gtrace(:(any)), det).
type_modes(gui_tracer, gtrace, det).
type_modes(gui_tracer, noguitracer, det).
type_modes(gui_tracer, guitracer, det).
type_modes(logicmoo_util_bugger, logicmoo_bugger_loaded, semidet).
type_modes(logicmoo_util_bugger, hook_message_hook, semidet).
type_modes(logicmoo_util_bugger, disabled_this, semidet).
type_modes(logicmoo_util_bugger, bugger_error_info(?(any)), semidet).
type_modes(logicmoo_util_bugger, bugger_prolog_exception_hook(?(any), ?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_bugger, do_ref_job(:(any), ?(any)), semidet).
type_modes(logicmoo_util_bugger, in_toplevel, semidet).
type_modes(logicmoo_util_bugger, in_file_directive, semidet).
type_modes(logicmoo_util_bugger, in_file_expansion, semidet).
type_modes(logicmoo_util_bugger, nth_frame_attribute(?(any), ?(any), ?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_bugger, nth_frame(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_bugger, nth_goal(?(any), ?(any)), semidet).
type_modes(logicmoo_util_bugger, nth_pi(?(any), ?(any)), semidet).
type_modes(logicmoo_util_bugger, show_and_do(:(any)), semidet).
type_modes(logicmoo_util_bugger, pop_def(?(any)), semidet).
type_modes(logicmoo_util_bugger, push_def(?(any)), semidet).
type_modes(logicmoo_util_bugger, default_dumptrace(?(any)), semidet).
type_modes(logicmoo_util_bugger, asserta_if_ground(?(any)), semidet).
type_modes(logicmoo_util_bugger, test_tl(+any, +any, +any), semidet).
type_modes(logicmoo_util_bugger, test_tl(+any), semidet).
type_modes(logicmoo_util_bugger, test_tl(:(any), +any), semidet).
type_modes(logicmoo_util_bugger, module_hotrace(?(any)), semidet).
type_modes(logicmoo_util_bugger, traceIf(:(any)), semidet).
type_modes(logicmoo_util_bugger, listify(?(any), ?(any)), semidet).
type_modes(logicmoo_util_bugger, unlistify(?(any), ?(any)), semidet).
type_modes(logicmoo_util_bugger, debugFmtList1(?(any), ?(any)), semidet).
type_modes(logicmoo_util_bugger, debugFmtList0(:(any), :(any)), semidet).
type_modes(logicmoo_util_bugger, debugFmtList(?(any)), semidet).
type_modes(logicmoo_util_bugger, writeFileToStream(?(any), ?(any)), semidet).
type_modes(logicmoo_util_bugger, writeErrMsg(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_bugger, writeErrMsg(?(any), ?(any)), semidet).
type_modes(logicmoo_util_bugger, writeOverwritten, semidet).
type_modes(logicmoo_util_bugger, writeSavedPrompt, semidet).
type_modes(logicmoo_util_bugger, saveUserInput, semidet).
type_modes(logicmoo_util_bugger, fmtString(?(any), ?(any)), semidet).
type_modes(logicmoo_util_bugger, fmtString(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_bugger, show_module(?(any)), semidet).
type_modes(logicmoo_util_bugger, loading_module(?(any)), semidet).
type_modes(logicmoo_util_bugger, module_stack(?(any), ?(any)), semidet).
type_modes(logicmoo_util_bugger, caller_module(?(any), ?(any)), semidet).
type_modes(logicmoo_util_bugger, caller_module(?(any)), semidet).
type_modes(logicmoo_util_bugger, prolog_current_frame_or_parent(?(any), ?(any)), semidet).
type_modes(logicmoo_util_bugger, prolog_current_frames(?(any)), semidet).
type_modes(logicmoo_util_bugger, loading_module(?(any), ?(any)), semidet).
type_modes(logicmoo_util_bugger, loggerFmtReal(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_bugger, loggerReFmt(?(any), ?(any)), semidet).
type_modes(logicmoo_util_bugger, logLevel(?(any), ?(any)), semidet).
type_modes(logicmoo_util_bugger, setLogLevel(?(any), ?(any)), semidet).
type_modes(logicmoo_util_bugger, on_f_log_fail(:(any)), semidet).
type_modes(logicmoo_util_bugger, show_success(:(any)), semidet).
type_modes(logicmoo_util_bugger, show_success(+any, :(any)), semidet).
type_modes(logicmoo_util_bugger, show_failure(:(any)), semidet).
type_modes(logicmoo_util_bugger, show_failure(+any, :(any)), semidet).
type_modes(logicmoo_util_bugger, show_call(:(any)), semidet).
type_modes(logicmoo_util_bugger, show_call(+any, :(any)), semidet).
type_modes(logicmoo_util_bugger, dcall0(:(any)), semidet).
type_modes(logicmoo_util_bugger, show_entry(:(any)), semidet).
type_modes(logicmoo_util_bugger, show_entry(+any, :(any)), semidet).
type_modes(logicmoo_util_bugger, cleanup_strings, semidet).
type_modes(logicmoo_util_bugger, gripe_time(+any, :(any)), semidet).
type_modes(logicmoo_util_bugger, matches_term0(:(any), ?(any)), semidet).
type_modes(logicmoo_util_bugger, matches_term(?(any), ?(any)), semidet).
type_modes(logicmoo_util_bugger, contains_atom(?(any), ?(any)), semidet).
type_modes(logicmoo_util_bugger, logger_property(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_bugger, time_call(:(any)), semidet).
type_modes(logicmoo_util_bugger, if_prolog(?(any), :(any)), semidet).
type_modes(logicmoo_util_bugger, showProfilerStatistics(:(any)), semidet).
type_modes(logicmoo_util_bugger, printPredCount(?(any), :(any), ?(any)), semidet).
type_modes(logicmoo_util_bugger, prolog_must_not(:(any)), semidet).
type_modes(logicmoo_util_bugger, randomVars(:(any)), semidet).
type_modes(logicmoo_util_bugger, is_deterministic(:(any)), semidet).
type_modes(logicmoo_util_bugger, one_must(:(any), :(any), :(any)), semidet).
type_modes(logicmoo_util_bugger, must_each0(:(any)), semidet).
type_modes(logicmoo_util_bugger, must_each(:(any)), semidet).
type_modes(logicmoo_util_bugger, rmust_det(:(any)), semidet).
type_modes(logicmoo_util_bugger, programmer_error(:(any)), semidet).
type_modes(logicmoo_util_bugger, prolog_must_l(?(any)), semidet).
type_modes(logicmoo_util_bugger, replace_elements(:(any), ?(any), ?(any), :(any)), semidet).
type_modes(logicmoo_util_bugger, prolog_ecall_fa(?(any), :(any), ?(any), ?(any), :(any)), semidet).
type_modes(logicmoo_util_bugger, with_each(?(any), :(any), :(any)), semidet).
type_modes(logicmoo_util_bugger, bin_ecall(?(any), ?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_bugger, on_prolog_ecall_override(?(any), ?(any), ?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_bugger, default_ecall(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_bugger, on_prolog_ecall(?(any), ?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_bugger, show_goal_rethrow(?(any), ?(any)), semidet).
type_modes(logicmoo_util_bugger, will_debug_else_throw(:(any), :(any)), semidet).
type_modes(logicmoo_util_bugger, real_builtin_predicate(:(any)), semidet).
type_modes(logicmoo_util_bugger, atom_contains666(?(any), ?(any)), semidet).
type_modes(logicmoo_util_bugger, local_predicate(?(any), :(any)), semidet).
type_modes(logicmoo_util_bugger, kill_term_expansion, semidet).
type_modes(logicmoo_util_bugger, with_no_term_expansions(:(any)), semidet).
type_modes(logicmoo_util_bugger, beenCaught(:(any)), semidet).
type_modes(logicmoo_util_bugger, on_f_log_ignore(:(any)), semidet).
type_modes(logicmoo_util_bugger, logOnFailureEach(:(any)), semidet).
type_modes(logicmoo_util_bugger, logOnFailure0(:(any)), semidet).
type_modes(logicmoo_util_bugger, on_f_debug_ignore(:(any)), semidet).
type_modes(logicmoo_util_bugger, debugOnFailureEach(:(any)), semidet).
type_modes(logicmoo_util_bugger, on_f_debug(:(any)), semidet).
type_modes(logicmoo_util_bugger, with_each(?(any), :(any)), semidet).
type_modes(logicmoo_util_bugger, with_each(:(any)), semidet).
type_modes(logicmoo_util_bugger, on_x_debug_cont(:(any)), semidet).
type_modes(logicmoo_util_bugger, on_x_rtraceEach(:(any)), semidet).
type_modes(logicmoo_util_bugger, on_x_debug(:(any)), semidet).
type_modes(logicmoo_util_bugger, with_skip_bugger(:(any)), semidet).
type_modes(logicmoo_util_bugger, on_x_cont(:(any)), semidet).
type_modes(logicmoo_util_bugger, on_f_throw(:(any)), semidet).
type_modes(logicmoo_util_bugger, gmust(:(any), :(any)), semidet).
type_modes(logicmoo_util_bugger, prolog_must(:(any)), semidet).
type_modes(logicmoo_util_bugger, must_assign(?(any), ?(any)), semidet).
type_modes(logicmoo_util_bugger, must_assign(:(any)), semidet).
type_modes(logicmoo_util_bugger, forall_member(?(any), ?(any), :(any)), semidet).
type_modes(logicmoo_util_bugger, traceAll, semidet).
type_modes(logicmoo_util_bugger, ifThen(:(any), :(any)), semidet).
type_modes(logicmoo_util_bugger, fresh_line(?(any)), semidet).
type_modes(logicmoo_util_bugger, fresh_line, semidet).
type_modes(logicmoo_util_bugger, do_gc1, semidet).
type_modes(logicmoo_util_bugger, do_gc0, semidet).
type_modes(logicmoo_util_bugger, do_gc, semidet).
type_modes(logicmoo_util_bugger, set_optimize(?(any)), semidet).
type_modes(logicmoo_util_bugger, singletons(?(any)), semidet).
type_modes(logicmoo_util_bugger, buggeroo, semidet).
type_modes(logicmoo_util_bugger, ctrace, semidet).
type_modes(logicmoo_util_bugger, doHideTrace(?(any), ?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_bugger, hideTraceMFAT(?(any), ?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_bugger, hideTraceMFA(?(any), ?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_bugger, tryHide(?(any)), semidet).
type_modes(logicmoo_util_bugger, tryCatchIgnore(:(any)), semidet).
type_modes(logicmoo_util_bugger, hideTraceMP(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_bugger, hideTrace(?(any), -any), semidet).
type_modes(logicmoo_util_bugger, predicate_module(?(any), ?(any)), semidet).
type_modes(logicmoo_util_bugger, functor_source_file0(?(any), ?(any), ?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_bugger, functor_source_file(?(any), ?(any), ?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_bugger, hideRest, semidet).
type_modes(logicmoo_util_bugger, hideTrace, semidet).
type_modes(logicmoo_util_bugger, willTrace, semidet).
type_modes(logicmoo_util_bugger, non_user_console, semidet).
type_modes(logicmoo_util_bugger, isConsole, semidet).
type_modes(logicmoo_util_bugger, set_yes_debug_thread, semidet).
type_modes(logicmoo_util_bugger, set_yes_debug, semidet).
type_modes(logicmoo_util_bugger, set_gui_debug(:(any)), semidet).
type_modes(logicmoo_util_bugger, set_no_debug_thread, semidet).
type_modes(logicmoo_util_bugger, set_no_debug, semidet).
type_modes(logicmoo_util_bugger, traceok(:(any)), semidet).
type_modes(logicmoo_util_bugger, cli_ntrace(:(any)), semidet).
type_modes(logicmoo_util_bugger, call_skipping_n_clauses(?(any), ?(any)), semidet).
type_modes(logicmoo_util_bugger, call_or_list(?(any)), semidet).
type_modes(logicmoo_util_bugger, to_list_of(?(any), :(any), ?(any)), semidet).
type_modes(logicmoo_util_bugger, once_if_ground(:(any), -any), semidet).
type_modes(logicmoo_util_bugger, once_if_ground(:(any)), semidet).
type_modes(logicmoo_util_bugger, set_bugger_flag(?(any), ?(any)), semidet).
type_modes(logicmoo_util_bugger, bugger_flag(?(any), ?(any)), semidet).
type_modes(logicmoo_util_bugger, bugger_flag(:(any)), semidet).
type_modes(logicmoo_util_bugger, meta_interp(?(any), +any), semidet).
type_modes(logicmoo_util_bugger, meta_interp_signal(:(any)), semidet).
type_modes(logicmoo_util_bugger, list_difference_eq_memberchk_eq(?(any), :(any)), semidet).
type_modes(logicmoo_util_bugger, list_difference_eq(:(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_bugger, shrink_clause(?(any), ?(any)), semidet).
type_modes(logicmoo_util_bugger, shrink_clause(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_bugger, nodebugx(:(any)), semidet).
type_modes(logicmoo_util_bugger, has_gui_debug, semidet).
type_modes(logicmoo_util_bugger, dtrace_msg(?(any)), semidet).
type_modes(logicmoo_util_bugger, thread_local_leaks, semidet).
type_modes(logicmoo_util_bugger, bugger_term_expansion(+any, +any, -any), semidet).
type_modes(logicmoo_util_bugger, bugger_term_expansion(?(any), -any), semidet).
type_modes(logicmoo_util_bugger, format_safe(?(any), ?(any)), semidet).
type_modes(logicmoo_util_bugger, bugger_expand_term(:(any), -any), semidet).
type_modes(logicmoo_util_bugger, bugger_expand_goal(:(any), -any), semidet).
type_modes(logicmoo_util_bugger, bugger_goal_expansion(+any, +any, -any), semidet).
type_modes(logicmoo_util_bugger, bugger_goal_expansion(?(any), -any), semidet).
type_modes(logicmoo_util_bugger, differnt_modules(?(any), ?(any)), semidet).
type_modes(logicmoo_util_bugger, bugger_atom_change(?(any), -any), semidet).
type_modes(logicmoo_util_bugger, bugger_atom_change0(?(any), ?(any), ?(any), ?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_bugger, bugger_atom_change(?(any), :(any), +any, +any, -any, -any), semidet).
type_modes(logicmoo_util_bugger, bugger_t_expansion(+any, +any, -any), semidet).
type_modes(logicmoo_util_bugger, functor_h0(?(any), ?(any), :(any)), semidet).
type_modes(logicmoo_util_bugger, hidetrace(?(any)), semidet).
type_modes(logicmoo_util_bugger, prolog_call(:(any)), semidet).
type_modes(logicmoo_util_bugger, wdmsg(?(any), ?(any)), semidet).
type_modes(logicmoo_util_bugger, wdmsg(?(any)), semidet).
type_modes(logicmoo_util_bugger, bad_idea, semidet).
type_modes(logicmoo_util_bugger, new_a2s0(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_bugger, new_a2s(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_bugger, define_if_missing(:(any), ?(any)), semidet).
type_modes(logicmoo_util_bugger, callsc(:(any)), semidet).
type_modes(logicmoo_util_bugger, if_interactive0(:(any)), semidet).
type_modes(logicmoo_util_bugger, if_interactive(:(any)), semidet).
type_modes(logicmoo_util_bugger, test_for_release_problems(?(any)), semidet).
type_modes(logicmoo_util_bugger, test_for_release(?(any)), semidet).
type_modes(logicmoo_util_bugger, throw_safe(?(any)), semidet).
type_modes(logicmoo_util_bugger, must_maplist(:(any), ?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_bugger, must_maplist(:(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_bugger, must_maplist(:(any), ?(any), ?(any)), unknown).
type_modes(logicmoo_util_bugger, must_maplist(:(any), ?(any)), semidet).
type_modes(logicmoo_util_bugger, must_maplist(:(any), ?(any)), unknown).
type_modes(logicmoo_util_bugger, ignore_each(:(any)), semidet).
type_modes(logicmoo_util_bugger, debugFmt(?(any)), semidet).
type_modes(logicmoo_util_bugger, writeSTDERR0(?(any)), semidet).
type_modes(wsh_w, blocked(+any), semidet).
type_modes(logicmoo_util_prolog_frames, current_next_frames(?(any), ?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_prolog_frames, current_frames(?(any), ?(any), :(any), ?(any)), semidet).
type_modes(logicmoo_util_prolog_frames, prolog_frame_match(?(any), :(any), :(any)), semidet).
type_modes(logicmoo_util_prolog_frames, find_parent_frame_attribute(?(any), ?(any), ?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_prolog_frames, nth_parent_goal(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_prolog_frames, parent_goal(?(any), ?(any)), semidet).
type_modes(logicmoo_util_prolog_frames, parent_goal(?(any)), semidet).
type_modes(logicmoo_util_prolog_frames, relative_frame(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_prolog_frames, in_pengines, semidet).
type_modes(logicmoo_util_prolog_frames, stack_check_else(?(any), ?(any)), semidet).
type_modes(logicmoo_util_prolog_frames, stack_check(?(any), ?(any)), semidet).
type_modes(logicmoo_util_prolog_frames, stack_check(?(any)), semidet).
type_modes(logicmoo_util_prolog_frames, stack_check, semidet).
type_modes(logicmoo_util_prolog_frames, stack_depth(?(any)), semidet).
type_modes(pqConsole, blocked(+any), semidet).
type_modes(logicmoo_util_terms, logicmoo_library_file_loaded, semidet).
type_modes(logicmoo_util_terms, call_no_cuts(:(any)), semidet).
type_modes(logicmoo_util_terms, load_dirrective(:(any), ?(any)), semidet).
type_modes(logicmoo_util_terms, load_assert(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_terms, load_term2(:(any), ?(any)), semidet).
type_modes(logicmoo_util_terms, load_term(?(any), ?(any)), semidet).
type_modes(logicmoo_util_terms, dynamic_load_pl(?(any)), semidet).
type_modes(logicmoo_util_terms, doall(:(any)), semidet).
type_modes(logicmoo_util_terms, pred_delete(:(any), ?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_terms, delete_eq(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_terms, identical_memberchk(?(any), :(any)), semidet).
type_modes(logicmoo_util_terms, list_retain(?(any), :(any), ?(any)), semidet).
type_modes(logicmoo_util_terms, throw_if_true_else_fail(:(any), ?(any)), semidet).
type_modes(logicmoo_util_terms, pred_term_parts_l(:(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_terms, pred_term_parts(:(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_terms, term_parts_l(:(any), ?(any)), semidet).
type_modes(logicmoo_util_terms, term_parts(?(any), :(any)), semidet).
type_modes(logicmoo_util_terms, list_to_set_safe(:(any), ?(any)), semidet).
type_modes(logicmoo_util_terms, at_start(:(any)), semidet).
type_modes(logicmoo_util_terms, call_n_times(+any, :(any)), semidet).
type_modes(logicmoo_util_terms, functor_h(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_terms, get_functor(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_terms, get_functor(?(any), ?(any)), semidet).
type_modes(logicmoo_util_terms, functor_h(?(any), ?(any)), semidet).
type_modes(logicmoo_util_terms, remove_dupes(:(any), :(any), ?(any)), semidet).
type_modes(logicmoo_util_terms, remove_dupes(?(any), ?(any)), semidet).
type_modes(logicmoo_util_terms, flatten_set(?(any), ?(any)), semidet).
type_modes(logicmoo_util_terms, make_list(?(any), ?(any), :(any)), semidet).
type_modes(logicmoo_util_terms, weak_nd_subst2(?(any), ?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_terms, weak_nd_subst1(?(any), ?(any), ?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_terms, weak_nd_subst(?(any), ?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_terms, wsubst(?(any), ?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_terms, univ_term(?(any), :(any)), semidet).
type_modes(logicmoo_util_terms, nd_subst2(?(any), ?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_terms, nd_subst1(?(any), ?(any), ?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_terms, nd_subst(?(any), ?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_terms, subst(?(any), ?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_terms, univ_safe(?(any), ?(any)), semidet).
type_modes(logicmoo_util_terms, pred_subst(:(any), ?(any), ?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_terms, nd_predsubst2(:(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_terms, nd_predsubst1(:(any), ?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_terms, nd_predsubst(?(any), :(any), ?(any)), semidet).
type_modes(logicmoo_util_terms, predsubst(?(any), :(any), ?(any)), semidet).
type_modes(logicmoo_util_terms, in_thread_and_join(:(any), +any), semidet).
type_modes(logicmoo_util_terms, in_thread_and_join(:(any)), semidet).
type_modes(logicmoo_util_terms, proccess_status(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_terms, flatten_dedupe(?(any), ?(any)), semidet).
type_modes(logicmoo_util_terms, makeArgIndexes(?(any), ?(any)), semidet).
type_modes(logicmoo_util_terms, makeArgIndexes(?(any)), semidet).
type_modes(logicmoo_util_terms, each_subterm(?(any), :(any), ?(any)), semidet).
type_modes(logicmoo_util_terms, each_subterm(?(any), ?(any)), semidet).
type_modes(logicmoo_util_terms, read_each_term(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_terms, conjoin_op(?(any), ?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_terms, conjoin(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_terms, list_to_conjuncts(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_terms, list_to_conjuncts(?(any), ?(any)), semidet).
type_modes(logicmoo_util_terms, pred_juncts_to_list(?(any), ?(any)), semidet).
type_modes(logicmoo_util_terms, pred_juncts_to_list(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_terms, conjuncts_to_list(?(any), ?(any)), semidet).
type_modes(logicmoo_util_terms, disjuncts_to_list(?(any), ?(any)), semidet).
type_modes(logicmoo_util_terms, maptree(:(any), +any, -any), semidet).
type_modes(logicmoo_util_terms, is_proof(?(any)), semidet).
type_modes(logicmoo_util_terms, is_true(?(any)), semidet).
type_modes(logicmoo_util_terms, lastMember2(?(any), :(any)), semidet).
type_modes(vn, term_expansion(:(any), ?(any)), semidet).
type_modes(vn, make_hook(?(any), ?(any)), semidet).
type_modes(vn, logicmoo_util_varnames_file, semidet).
type_modes(vn, init_varname_stores(?(any)), semidet).
type_modes(vn, maybe_record_scanned_file, semidet).
type_modes(vn, term_expansion_save_vars(?(any)), semidet).
type_modes(vn, print_numbervars_2(?(any)), semidet).
type_modes(vn, print_numbervars_1(?(any)), semidet).
type_modes(vn, print_numbervars_maybe(?(any)), semidet).
type_modes(vn, print_numbervars(?(any)), semidet).
type_modes(vn, portray(:(any)), semidet).
type_modes(vn, ensure_vars_labled(?(any), ?(any)), semidet).
type_modes(vn, read_source_file_vars_1(?(any)), semidet).
type_modes(vn, read_source_vars(?(any), ?(any)), semidet).
type_modes(vn, save_file_source_vars(?(any), ?(any), ?(any)), semidet).
type_modes(vn, read_source_file_vars(?(any)), semidet).
type_modes(vn, dcall_if_verbose(:(any)), semidet).
type_modes(vn, scan_for_varnames, semidet).
type_modes(vn, maybe_scan_source_files_for_varnames, semidet).
type_modes(vn, try_save_vars(?(any)), semidet).
type_modes(vn, snumbervars5(:(any), ?(any), ?(any), ?(any)), semidet).
type_modes(vn, snumbervars4(:(any), ?(any), ?(any), ?(any)), semidet).
type_modes(vn, check_varnames(:(any)), semidet).
type_modes(vn, imploded_copyvars(?(any), ?(any)), semidet).
type_modes(vn, b_implode_varnames0(:(any)), semidet).
type_modes(vn, b_implode_varnames(?(any)), semidet).
type_modes(vn, mpred_name_variables(:(any)), semidet).
type_modes(vn, mpred_numbervars_with_names(?(any)), semidet).
type_modes(vn, bad_varnamez(?(any)), semidet).
type_modes(vn, contains_badvarnames(?(any)), semidet).
type_modes(vn, call_not_not(:(any)), semidet).
type_modes(vn, count_members_eq(+any, +any, -any), det).
type_modes(vn, contains_singletons(?(any), ?(any)), semidet).
type_modes(vn, contains_singletons(?(any)), semidet).
type_modes(vn, contain_numbervars(?(any)), semidet).
type_modes(vn, source_variables_lv(?(any)), semidet).
type_modes(vn, renumbervars(?(any), ?(any), ?(any)), semidet).
type_modes(vn, copy_term_and_varnames(?(any), ?(any)), semidet).
type_modes(vn, ensure_vars_labled_r(?(any), ?(any)), semidet).
type_modes(vn, contains_ftVar(+any), semidet).
type_modes(vn, ain00(?(any)), semidet).
type_modes(vn, save_clause_vars(?(any), ?(any), ?(any), ?(any), ?(any), ?(any)), semidet).
type_modes(vn, save_to_clause_ref(?(any), ?(any), ?(any)), semidet).
type_modes(vn, clause_ref_file(?(any), ?(any)), semidet).
type_modes(vn, clause_ref_vars(?(any), ?(any)), semidet).
type_modes(vn, locate_clause_ref(?(any), ?(any), ?(any), ?(any), ?(any)), semidet).
type_modes(vn, save_clause_vars(?(any), ?(any), :(any)), semidet).
type_modes(vn, save_clause_vars(?(any), ?(any)), semidet).
type_modes(vn, write_functor(?(any), ?(any)), semidet).
type_modes(vn, set_varname(+any, ?(any), ?(any)), semidet).
type_modes(vn, set_varname(+any, ?(any)), semidet).
type_modes(vn, try_get_body_vars(:(any)), semidet).
type_modes(vn, arg_varname(+any, ?(any), ?(any)), semidet).
type_modes(vn, sub_ft_var(?(any), ?(any)), semidet).
type_modes(vn, get_1head_arg_var(?(any), ?(any), ?(any), ?(any), ?(any)), semidet).
type_modes(vn, get_random_headvars(?(any), ?(any), ?(any), ?(any)), semidet).
type_modes(vn, get_random_headvars(?(any)), semidet).
type_modes(vn, try_varname_infos(?(any)), semidet).
type_modes(vn, try_get_head_vars(?(any)), semidet).
type_modes(vn, try_get_varname_cache(?(any)), semidet).
type_modes(vn, call_return_tf(:(any), ?(any)), semidet).
type_modes(vn, term_singletons(:(any), ?(any), ?(any), ?(any), ?(any)), semidet).
type_modes(vn, term_singletons(?(any), ?(any), ?(any)), semidet).
type_modes(vn, term_singletons(?(any), ?(any)), semidet).
type_modes(vn, term_slots(?(any), ?(any)), semidet).
type_modes(vn, try_get_inner_vars(?(any)), semidet).
type_modes(vn, no_vars_needed(?(any)), semidet).
type_modes(vn, fix_varcase_name(?(any), ?(any)), semidet).
type_modes(vn, replace_in_string_frak_0(?(any), ?(any), ?(any), ?(any)), semidet).
type_modes(vn, atom_subst_frak_0(?(any), ?(any), ?(any), ?(any)), semidet).
type_modes(vn, get_clause_vars_hb_int(?(any), ?(any)), semidet).
type_modes(vn, get_clause_vars_copy(?(any), ?(any)), semidet).
type_modes(vn, get_clause_vars(?(any), ?(any)), semidet).
type_modes(vn, del_attr_type(?(any), ?(any)), semidet).
type_modes(vn, get_clause_vars_nontraced(:(any)), semidet).
type_modes(vn, get_clause_vars(?(any)), semidet).
type_modes(vn, subterm_path(?(any), ?(any), :(any)), semidet).
type_modes(vn, make_subterm_path(?(any), ?(any), ?(any)), semidet).
type_modes(vn, unlock_vars(:(any)), semidet).
type_modes(vn, lock_vars(:(any)), semidet).
type_modes(vn, dif_matrix(?(any), ?(any)), semidet).
type_modes(vn, v_dif_rest(?(any), ?(any)), semidet).
type_modes(vn, all_different_vals(:(any), +any, +any), semidet).
type_modes(vn, all_different_vals(:(any), +any), semidet).
type_modes(vn, all_different_vars(?(any)), semidet).
type_modes(vn, all_different_vals(+any, ?(any)), semidet).
type_modes(vn, not_member_eq(?(any), ?(any)), semidet).
type_modes(vn, no_varnaming(:(any)), semidet).
type_modes(vn, dcall_when(:(any), ?(any), ?(any)), semidet).
type_modes(vn, vmust(:(any)), semidet).
type_modes(vn, attr_portray_hook(?(any), ?(any)), semidet).
type_modes(vn, attr_unify_hook(?(any), ?(any)), semidet).
type_modes(vn, attribute_goals(@any, ?(list), ?(list)), det).
type_modes(vn, project_attributes(?(any), ?(any)), semidet).
type_modes(vn, variable_name(+any, -any), semidet).
type_modes(vn, name_variable(+any, +any), det).
type_modes(logicmoo_util_loop_check, goal_expansion(?(any), ?(any)), semidet).
type_modes(logicmoo_util_loop_check, outside_of_loop_check, semidet).
type_modes(logicmoo_util_loop_check, really_can_table, semidet).
type_modes(logicmoo_util_loop_check, call_tabled1(?(any), ?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_loop_check, call_tabled0(?(any), ?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_loop_check, findall_nodupes(?(any), :(any), -any), semidet).
type_modes(logicmoo_util_loop_check, call_setof_tabled(?(any), ?(any), :(any), -any), semidet).
type_modes(logicmoo_util_loop_check, call_vars_tabled(?(any), ?(any), :(any)), semidet).
type_modes(logicmoo_util_loop_check, call_tabled(?(any), :(any)), semidet).
type_modes(logicmoo_util_loop_check, call_tabled(:(any)), semidet).
type_modes(logicmoo_util_loop_check, cannot_use_tables(:(any)), semidet).
type_modes(logicmoo_util_loop_check, cannot_table_call(:(any)), semidet).
type_modes(logicmoo_util_loop_check, skipped_table_call(:(any)), semidet).
type_modes(logicmoo_util_loop_check, atoms_of_0(:(any), ?(any)), semidet).
type_modes(logicmoo_util_loop_check, make_tabled_perm(:(any)), semidet).
type_modes(logicmoo_util_loop_check, any_term_overlap(?(any), ?(any)), semidet).
type_modes(logicmoo_util_loop_check, any_term_overlap_atoms_of(?(any), ?(any)), semidet).
type_modes(logicmoo_util_loop_check, expire_tabled_list(?(any)), semidet).
type_modes(logicmoo_util_loop_check, mpred_expire_caches(?(any)), semidet).
type_modes(logicmoo_util_loop_check, ex, semidet).
type_modes(logicmoo_util_loop_check, lex, semidet).
type_modes(logicmoo_util_loop_check, expire_dont_add, semidet).
type_modes(logicmoo_util_loop_check, retract_can_table, semidet).
type_modes(logicmoo_util_loop_check, lco_goal_expansion(:(any), :(any)), semidet).
type_modes(logicmoo_util_loop_check, get_where0(:(any)), semidet).
type_modes(logicmoo_util_loop_check, get_where(:(any)), semidet).
type_modes(logicmoo_util_loop_check, can_fail(?(any)), semidet).
type_modes(logicmoo_util_loop_check, loop_check_term(:(any), ?(any), :(any)), semidet).
type_modes(logicmoo_util_loop_check, no_loop_check_term_key(:(any), ?(any), :(any)), semidet).
type_modes(logicmoo_util_loop_check, no_loop_check(:(any), :(any)), semidet).
type_modes(logicmoo_util_loop_check, no_loop_check(:(any)), semidet).
type_modes(logicmoo_util_loop_check, loop_check_term_key(:(any), ?(any), :(any)), semidet).
type_modes(logicmoo_util_loop_check, loop_check(:(any), :(any)), semidet).
type_modes(logicmoo_util_loop_check, loop_check(:(any)), semidet).
type_modes(logicmoo_util_loop_check, loop_check_early(:(any), :(any)), semidet).
type_modes(logicmoo_util_loop_check, is_loop_checked(?(any)), semidet).
type_modes(logicmoo_util_loop_check, make_key(?(any), ?(any)), semidet).
type_modes(logicmoo_util_loop_check, cc_key(?(any), ?(any)), semidet).
type_modes(logicmoo_util_loop_check, reduce_make_key(+any, -any), semidet).
type_modes(logicmoo_util_loop_check, call_t(:(any)), semidet).
type_modes(logicmoo_util_loop_check, cyclic_break(?(any)), semidet).
type_modes(logicmoo_util_loop_check, memberchk_same_two(?(any), :(any)), semidet).
type_modes(logicmoo_util_loop_check, transitive_except(+any, :(any), +any, -any), semidet).
type_modes(logicmoo_util_loop_check, transitive_lc(:(any), +any, -any), semidet).
type_modes(logicmoo_util_loop_check, transitive(:(any), +any, -any), semidet).
type_modes(logicmoo_util_loop_check, go_as_last(:(any), :(any)), semidet).
type_modes(logicmoo_util_database, erase_safe(?(any), ?(any)), semidet).
type_modes(logicmoo_util_database, append_term(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_database, safe_univ0(?(any), :(any)), semidet).
type_modes(logicmoo_util_database, safe_univ(?(any), ?(any)), semidet).
type_modes(logicmoo_util_database, retract_eq(?(any)), semidet).
type_modes(logicmoo_util_database, clause_true(?(any)), semidet).
type_modes(logicmoo_util_database, clause_i(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_database, modulize_head(+any, -any), semidet).
type_modes(logicmoo_util_database, clause_asserted(?(any), ?(any), -any), semidet).
type_modes(logicmoo_util_database, clause_asserted(?(any), ?(any)), semidet).
type_modes(logicmoo_util_database, clause_asserted(?(any)), semidet).
type_modes(logicmoo_util_database, hb_to_clause(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_database, expand_to_hb(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_database, ainz_clause(?(any), ?(any)), semidet).
type_modes(logicmoo_util_database, ainz_clause(?(any)), semidet).
type_modes(logicmoo_util_database, ainz(?(any)), semidet).
type_modes(logicmoo_util_database, aina(?(any)), semidet).
type_modes(logicmoo_util_database, ain(?(any)), semidet).
type_modes(logicmoo_util_database, painz(?(any)), semidet).
type_modes(logicmoo_util_database, paina(?(any)), semidet).
type_modes(logicmoo_util_database, pain(?(any)), semidet).
type_modes(logicmoo_util_database, assertz_new(?(any)), semidet).
type_modes(logicmoo_util_database, asserta_new(?(any)), semidet).
type_modes(logicmoo_util_database, assertz_if_new(?(any)), semidet).
type_modes(logicmoo_util_database, asserta_if_new(?(any)), semidet).
type_modes(logicmoo_util_database, assert_if_new(?(any)), semidet).
type_modes(logicmoo_util_database, call_provider(?(any), ?(any)), semidet).
type_modes(logicmoo_util_database, call_provider(?(any)), semidet).
type_modes(logicmoo_util_database, mpred_provider(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_database, eraseall(+any, +any), semidet).
type_modes(logicmoo_util_database, mpred_op_prolog0(:(any), ?(any)), semidet).
type_modes(logicmoo_util_database, mpred_op_prolog(?(any), ?(any)), semidet).
type_modes(logicmoo_util_database, ain0(?(any)), semidet).
type_modes(logicmoo_util_database, find_and_call(:(any)), semidet).
type_modes(logicmoo_util_database, find_and_call(+any, +any, ?(any)), semidet).
type_modes(logicmoo_util_database, mpred_mop(+any, :(any), ?(any)), semidet).
type_modes(logicmoo_util_database, mpred_split_op_data(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_database, debugCallWhy(?(any), :(any)), semidet).
type_modes(logicmoo_util_database, clause_safe(?(any), ?(any)), semidet).
type_modes(logicmoo_util_database, my_module_sensitive_code(?(any)), semidet).
type_modes(lmcache, blocked(+any), semidet).
type_modes(logicmoo_util_attvar_reader, system_expanded_attvars(:(any), :(any)), semidet).
type_modes(logicmoo_util_attvar_reader, verbatum_term(?(any)), semidet).
type_modes(logicmoo_util_attvar_reader, serialize_attvars(+any, -any), semidet).
type_modes(logicmoo_varnames, blocked(+any), semidet).
type_modes(logicmoo_util_first, quiet_all_module_predicates_are_transparent(?(any)), semidet).
type_modes(logicmoo_util_first, all_module_predicates_are_transparent(?(any)), semidet).
type_modes(logicmoo_util_first, module_meta_predicates_are_transparent(?(any)), semidet).
type_modes(logicmoo_util_first, arg_is_transparent(:(any)), semidet).
type_modes(logicmoo_util_first, module_predicates_are_not_exported_list(?(any), ?(any)), semidet).
type_modes(logicmoo_util_first, export_if_noconflict(?(any), :(any)), semidet).
type_modes(logicmoo_util_first, module_predicates_are_exported0(?(any)), semidet).
type_modes(logicmoo_util_first, module_predicates_are_exported(?(any)), semidet).
type_modes(logicmoo_util_first, module_predicates_are_exported, semidet).
type_modes(logicmoo_util_first, module_predicate(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_first, export_all_preds(?(any)), semidet).
type_modes(logicmoo_util_first, export_all_preds, semidet).
type_modes(logicmoo_util_first, user_use_module(?(any)), semidet).
type_modes(logicmoo_util_first, user_ensure_loaded(?(any)), semidet).
type_modes(logicmoo_util_first, module_predicate(?(any), ?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_first, snumbervars(?(any), ?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_first, snumbervars(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_first, snumbervars(?(any)), semidet).
type_modes(logicmoo_util_first, name_to_var(?(any), :(any), ?(any)), semidet).
type_modes(logicmoo_util_first, var_to_name(?(any), :(any), ?(any)), semidet).
type_modes(logicmoo_util_first, samify(?(any), ?(any)), semidet).
type_modes(logicmoo_util_first, register_var_0(?(any), ?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_first, register_var(?(any), ?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_first, register_var(:(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_first, safe_numbervars(?(any)), semidet).
type_modes(logicmoo_util_first, get_gtime(?(any)), semidet).
type_modes(logicmoo_util_first, safe_numbervars(?(any), ?(any)), semidet).
type_modes(logicmoo_util_first, renumbervars1(:(any), ?(any), :(any), ?(any)), semidet).
type_modes(logicmoo_util_first, renumbervars1(?(any), ?(any)), semidet).
type_modes(logicmoo_util_first, renumbervars_prev(?(any), ?(any)), semidet).
type_modes(logicmoo_util_first, remove_grounds(:(any), :(any)), semidet).
type_modes(logicmoo_util_first, add_newvar(?(any), ?(any)), semidet).
type_modes(logicmoo_util_first, add_newvars(:(any)), semidet).
type_modes(logicmoo_util_first, unnumbervars4(?(any), ?(any), ?(any), ?(any)), det).
type_modes(logicmoo_util_first, unnumbervars_and_save(?(any), ?(any)), semidet).
type_modes(logicmoo_util_first, unnumbervars(?(any), ?(any)), semidet).
type_modes(logicmoo_util_first, add_var_to_list(?(any), ?(any), ?(any), ?(any), ?(any), ?(any)), det).
type_modes(logicmoo_util_first, mustvv(:(any)), semidet).
type_modes(logicmoo_util_first, term_to_string(?(any), ?(any)), semidet).
type_modes(logicmoo_util_first, oncely_clean(?(any)), unknown).
type_modes(logicmoo_util_first, always_show_dmsg, semidet).
type_modes(logicmoo_util_first, mpred_trace_all(?(any)), semidet).
type_modes(logicmoo_util_first, mpred_trace_childs(?(any)), semidet).
type_modes(logicmoo_util_first, mpred_trace_nochilds(?(any)), semidet).
type_modes(logicmoo_util_first, mpred_trace_none(?(any)), semidet).
type_modes(logicmoo_util_first, mpred_trace_less(?(any)), semidet).
type_modes(logicmoo_util_first, with_unlocked_pred(?(any), :(any)), semidet).
type_modes(logicmoo_util_first, if_may_hide(:(any)), semidet).
type_modes(logicmoo_util_first, match_predicates(?(any), -any, -any, -any, -any), semidet).
type_modes(logicmoo_util_first, match_predicates(?(any), -any), semidet).
type_modes(logicmoo_util_first, source_context_module(?(any)), semidet).
type_modes(logicmoo_util_first, alldiscontiguous, semidet).
type_modes(logicmoo_util_dumpst, dumptrace_ret(?(any)), semidet).
type_modes(logicmoo_util_dumpst, dumptrace(:(any), +any), semidet).
type_modes(logicmoo_util_dumpst, dumptrace(?(any)), semidet).
type_modes(logicmoo_util_dumpst, to_wmsg(:(any), :(any)), semidet).
type_modes(logicmoo_util_dumpst, dtrace(+any, ?(any)), semidet).
type_modes(logicmoo_util_dumpst, dtrace(:(any)), semidet).
type_modes(logicmoo_util_dumpst, dtrace, semidet).
type_modes(logicmoo_util_dumpst, end_dump(:(any)), semidet).
type_modes(logicmoo_util_dumpst, clauseST(?(any), :(any)), semidet).
type_modes(logicmoo_util_dumpst, getPFA2(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_dumpst, getPFA1(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_dumpst, getPFA(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_dumpst, simplify_goal_printed(:(any), :(any)), semidet).
type_modes(logicmoo_util_dumpst, fdmsg(?(any)), semidet).
type_modes(logicmoo_util_dumpst, simplify_m(?(any), ?(any)), semidet).
type_modes(logicmoo_util_dumpst, fdmsg1(?(any)), semidet).
type_modes(logicmoo_util_dumpst, neg1_numbervars(?(any), ?(any), :(any)), semidet).
type_modes(logicmoo_util_dumpst, fmsg_rout(:(any)), semidet).
type_modes(logicmoo_util_dumpst, frame_to_fmsg(?(any), ?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_dumpst, printFrame(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_dumpst, pushFrame(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_dumpst, dumpST_now_reversed(?(any), ?(any)), semidet).
type_modes(logicmoo_util_dumpst, dumpST_now(?(any), ?(any)), semidet).
type_modes(logicmoo_util_dumpst, drain_framelist_ele(?(any)), semidet).
type_modes(logicmoo_util_dumpst, drain_framelist(?(any)), semidet).
type_modes(logicmoo_util_dumpst, dumpST9(?(any), :(any)), semidet).
type_modes(logicmoo_util_dumpst, dumpST9(?(any)), semidet).
type_modes(logicmoo_util_dumpst, dumpST9, semidet).
type_modes(logicmoo_util_dumpst, get_m_opt(?(any), ?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_dumpst, dumpST(?(any)), semidet).
type_modes(logicmoo_util_dumpst, dumpST1, semidet).
type_modes(logicmoo_util_dumpst, dumpST, semidet).
type_modes(logicmoo_util_dumpst, dumpST0(?(any), ?(any)), semidet).
type_modes(logicmoo_util_dumpst, dumpST0(?(any)), semidet).
type_modes(logicmoo_util_dumpst, dumpST0, semidet).
type_modes(logicmoo_util_dumpst, dump_st, semidet).
type_modes(logicmoo_util_with_assertions, check_thread_local_1m(?(any)), semidet).
type_modes(logicmoo_util_with_assertions, to_thread_head_1m(?(any), ?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_with_assertions, wno_tl(:(any), :(any)), semidet).
type_modes(logicmoo_util_with_assertions, w_tl(?(any), ?(any)), semidet).
type_modes(logicmoo_util_with_assertions, wtg(?(any), :(any)), semidet).
type_modes(logicmoo_util_with_assertions, with_no_x(:(any)), semidet).
type_modes(logicmoo_util_with_assertions, with_each_item(+any, +any, +any), semidet).
type_modes(logicmoo_utils, mpred_is_impl_file(?(any)), semidet).
type_modes(logicmoo_utils, logicmoo_scan_autoloads, semidet).
type_modes(logicmoo_utils, logicmoo_pre_release, semidet).
type_modes(logicmoo_utils, add_library_search_path(?(any), ?(any)), semidet).
type_modes(logicmoo_utils, add_library_search_path(+any, +list(atom)), det).
type_modes(logicmoo_utils, add_file_search_path(?(any), ?(any)), semidet).
type_modes(logicmoo_utils, add_file_search_path(+any, +any), det).
type_modes(logicmoo_utils, resolve_dir(?(any), ?(any)), semidet).
type_modes(varname_cache, blocked(+any), semidet).
type_modes(logicmoo_util_term_listing, list_clauses(?(any), ?(any)), semidet).
type_modes(logicmoo_util_term_listing, prolog_listing_list_clauses(?(any), ?(any)), semidet).
type_modes(logicmoo_util_term_listing, locate_clauses(?(any), ?(any)), semidet).
type_modes(logicmoo_util_term_listing, use_listing_vars(?(any)), semidet).
type_modes(logicmoo_util_term_listing, use_listing_vars, semidet).
type_modes(logicmoo_util_term_listing, use_xlisting(?(any)), semidet).
type_modes(logicmoo_util_term_listing, use_xlisting, semidet).
type_modes(logicmoo_util_term_listing, pi_to_head_l(?(any), ?(any)), semidet).
type_modes(logicmoo_util_term_listing, pp_listing(?(any)), semidet).
type_modes(logicmoo_util_term_listing, make_headkey(:(any), :(any)), semidet).
type_modes(logicmoo_util_term_listing, maybe_separate_0(?(any), :(any)), semidet).
type_modes(logicmoo_util_term_listing, maybe_separate(?(any), :(any)), semidet).
type_modes(logicmoo_util_term_listing, portray_one_line(?(any)), semidet).
type_modes(logicmoo_util_term_listing, portray_hb(?(any), ?(any)), semidet).
type_modes(logicmoo_util_term_listing, portray_hbr(:(any), :(any), ?(any)), semidet).
type_modes(logicmoo_util_term_listing, bad_pred(:(any)), semidet).
type_modes(logicmoo_util_term_listing, mp(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_term_listing, remove_undef_search, semidet).
type_modes(logicmoo_util_term_listing, update_changed_files1, semidet).
type_modes(logicmoo_util_term_listing, update_changed_files0, semidet).
type_modes(logicmoo_util_term_listing, update_changed_files, semidet).
type_modes(logicmoo_util_term_listing, mmake, semidet).
type_modes(logicmoo_util_term_listing, real_list_undefined(?(any)), semidet).
type_modes(logicmoo_util_term_listing, scansrc_list_undefined(?(any)), semidet).
type_modes(logicmoo_util_term_listing, ok_show(?(any)), semidet).
type_modes(logicmoo_util_term_listing, cur_predicate(:(any), :(any)), semidet).
type_modes(logicmoo_util_term_listing, nonvar_search(:(any)), semidet).
type_modes(logicmoo_util_term_listing, term_matches_unify(:(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_term_listing, term_matches_hb(?(any), :(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_term_listing, term_matches_hb(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_term_listing, synth_in_listing(?(any)), semidet).
type_modes(logicmoo_util_term_listing, synth_clause_ref(:(any), ?(any), ?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_term_listing, synth_clause_for_large(?(any), ?(any), ?(any), :(any), :(any), ?(any)), semidet).
type_modes(logicmoo_util_term_listing, synth_clause_for_l2(:(any), ?(any), ?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_term_listing, synth_clause_for(?(any), ?(any), :(any), :(any), ?(any)), semidet).
type_modes(logicmoo_util_term_listing, plisting_1, semidet).
type_modes(logicmoo_util_term_listing, sourceTextPredicateSource(?(any)), semidet).
type_modes(logicmoo_util_term_listing, sourceTextPredicate(?(any)), semidet).
type_modes(logicmoo_util_term_listing, predicateUsesCall(:(any)), semidet).
type_modes(logicmoo_util_term_listing, bookeepingPredicateXRef(:(any)), semidet).
type_modes(logicmoo_util_term_listing, buggery_ok, semidet).
type_modes(logicmoo_util_term_listing, prolog_list_goal(:(any)), semidet).
type_modes(logicmoo_util_term_listing, xlisting_inner(:(any), +any, +any), semidet).
type_modes(logicmoo_util_term_listing, get_matcher_code(?(any), ?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_term_listing, mpred_match_listing_skip_pi(+any, +any), semidet).
type_modes(logicmoo_util_term_listing, mpred_match_listing(?(any)), semidet).
type_modes(logicmoo_util_term_listing, plisting_0(?(any)), semidet).
type_modes(logicmoo_util_term_listing, plisting(?(any)), semidet).
type_modes(logicmoo_util_term_listing, xlisting(?(any)), semidet).
type_modes(logicmoo_util_term_listing, printAll(:(any), ?(any)), semidet).
type_modes(logicmoo_util_term_listing, printAll(:(any)), semidet).
type_modes(logicmoo_util_term_listing, unify_listing(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_term_listing, unify_listing_header(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_term_listing, unify_listing_header(?(any)), semidet).
type_modes(logicmoo_util_term_listing, unify_listing(?(any)), semidet).
type_modes(logicmoo_util_term_listing, hide_data0(:(any)), semidet).
type_modes(logicmoo_util_term_listing, listing_filter(?(any)), semidet).
type_modes(logicmoo_util_term_listing, shared_hide_data(?(any)), semidet).
type_modes(logicmoo_util_term_listing, save_search_ref_tl(?(any), ?(any)), semidet).
type_modes(logicmoo_util_term_listing, save_search_ref_recorded(?(any), ?(any)), semidet).
type_modes(logicmoo_util_term_listing, save_search_ref_0(?(any), ?(any)), semidet).
type_modes(logicmoo_util_term_listing, save_search_ref(?(any), ?(any)), semidet).
type_modes(logicmoo_util_term_listing, get_search_ref_tl(?(any), ?(any)), semidet).
type_modes(logicmoo_util_term_listing, unify_in_thread_tl(?(any), :(any)), semidet).
type_modes(logicmoo_util_term_listing, process_unify_in_thread(?(any), ?(any)), semidet).
type_modes(logicmoo_util_term_listing, unify_in_thread(+any, :(any)), semidet).
type_modes(logicmoo_util_term_listing, get_search_ref0(?(any), ?(any)), semidet).
type_modes(logicmoo_util_term_listing, get_search_ref(?(any), ?(any)), semidet).
type_modes(logicmoo_util_term_listing, unmake_search_key(?(any), ?(any)), semidet).
type_modes(logicmoo_util_term_listing, make_search_key(?(any), ?(any)), semidet).
type_modes(logicmoo_util_term_listing, searchable_terms_tl(?(any)), semidet).
type_modes(logicmoo_util_term_listing, searchable_terms(?(any)), semidet).
type_modes(logicmoo_util_term_listing, search_refs_use_recorded, semidet).
type_modes(logicmoo_util_term_listing, searchable_of_clause_1(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_term_listing, searchable_of_clause_1(:(any), ?(any)), semidet).
type_modes(logicmoo_util_term_listing, searchable_of_clause_0(?(any), :(any)), semidet).
type_modes(logicmoo_util_term_listing, searchable_of_clause(:(any), ?(any)), semidet).
type_modes(logicmoo_util_term_listing, make_searchable_list_ref(?(any), ?(any)), semidet).
type_modes(logicmoo_util_term_listing, make_searchable_ref(?(any), ?(any)), semidet).
type_modes(logicmoo_util_term_listing, make_searchable(?(any)), semidet).
type_modes(logicmoo_util_term_listing, clause_ref(?(any), ?(any)), semidet).
type_modes(logicmoo_util_term_listing, m_clause0(?(any), ?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_term_listing, m_clause_no_missing(?(any), ?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_term_listing, m_clause(?(any), ?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_term_listing, make_searchable_index(?(any)), semidet).
type_modes(logicmoo_util_term_listing, print_clause_properties(?(any), ?(any)), semidet).
type_modes(logicmoo_util_term_listing, print_record_properties(?(any), ?(any)), semidet).
type_modes(logicmoo_util_term_listing, blob_count(?(any), ?(any)), semidet).
type_modes(logicmoo_util_term_listing, save_atoms, semidet).
type_modes(logicmoo_util_term_listing, new_atoms(?(any), ?(any)), semidet).
type_modes(logicmoo_util_term_listing, blob_info(?(any), :(any), :(any)), semidet).
type_modes(logicmoo_util_term_listing, current_atom_or_blob(?(any), ?(any)), semidet).
type_modes(logicmoo_util_term_listing, mstatistics, semidet).
type_modes(logicmoo_util_help, mpred_prolog_only_module(?(any)), semidet).
type_modes(logicmoo_util_help, mpred_type_module(?(any)), semidet).
type_modes(logicmoo_util_help, mpred_show_doc(?(any), ?(any)), semidet).
type_modes(logicmoo_util_help, mpred_show_doc(?(any)), semidet).
type_modes(logicmoo_util_help, longer_sumry(?(any), ?(any)), semidet).
type_modes(logicmoo_util_help, flatten_text(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_help, summary_ending(:(any), ?(any)), semidet).
type_modes(logicmoo_util_help, summary_start(?(any), ?(any)), semidet).
type_modes(logicmoo_util_help, make_l_summary_lc(?(any), :(any), ?(any)), semidet).
type_modes(logicmoo_util_help, make_l_summary(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_help, make_summary(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_help, for_m_make_summary(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_help, make_summary0(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_help, termw_to_atom(?(any), ?(any)), semidet).
type_modes(logicmoo_util_help, notes_to_better_text(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_help, notes_to_better_text(?(any), ?(any)), semidet).
type_modes(logicmoo_util_help, system_pred(:(any)), semidet).
type_modes(logicmoo_util_help, prefixed_module(?(any), ?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_help, name_to_mode(?(any), ?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_help, mode_to_name0(?(any), ?(any)), semidet).
type_modes(logicmoo_util_help, mode_to_name(?(any), ?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_help, fixda2(?(any), ?(any), :(any), :(any)), semidet).
type_modes(logicmoo_util_help, fixda(?(any), ?(any), :(any), :(any)), semidet).
type_modes(logicmoo_util_help, fixup_doc_args(?(any), ?(any), ?(any), :(any), :(any)), semidet).
type_modes(logicmoo_util_help, great_clause(:(any), ?(any)), semidet).
type_modes(logicmoo_util_help, attempt_head_modes_0(?(any), ?(any), :(any)), semidet).
type_modes(logicmoo_util_help, attempt_head_modes(:(any)), semidet).
type_modes(logicmoo_util_help, attempt_head_varnames_0(?(any)), semidet).
type_modes(logicmoo_util_help, attempt_head_vnames(:(any)), semidet).
type_modes(logicmoo_util_help, fix_doc_args_for_write(?(any), ?(any)), semidet).
type_modes(logicmoo_util_help, ignored_assignment(?(any), ?(any)), semidet).
type_modes(logicmoo_util_help, autodoc_case(?(any), ?(any)), semidet).
type_modes(logicmoo_util_help, autodoc_pred(?(any), :(any)), semidet).
type_modes(logicmoo_util_help, autodoc_stream_pred(?(any), ?(any), :(any)), semidet).
type_modes(logicmoo_util_help, autodoc_stream_data(?(any), ?(any), ?(any), :(any), ?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_help, echo_one_line_or_skip_autodoc(?(any)), semidet).
type_modes(logicmoo_util_help, autodoc_magic(?(any)), semidet).
type_modes(logicmoo_util_help, copy_until_line(?(any), ?(any)), semidet).
type_modes(logicmoo_util_help, autodoc_files, semidet).
type_modes(logicmoo_util_help, autodoc_stream(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_help, autodoc_module(?(any)), semidet).
type_modes(logicmoo_util_help, autodoc_file(:(any)), semidet).
type_modes(logicmoo_util_help, autodoc_output_path(?(any), ?(any)), semidet).
type_modes(logicmoo_util_help, target_module(?(any), ?(any)), semidet).
type_modes(logicmoo_util_help, merge_mode_and_varname(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_help, export_module_preds, semidet).
type_modes(logicmoo_util_help, export_file_preds(?(any), ?(any), ?(any), ?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_help, write_modules, semidet).
type_modes(logicmoo_util_help, predicate_decl_module(?(any), ?(any)), semidet).
type_modes(logicmoo_util_help, export_file_preds(?(any)), semidet).
type_modes(logicmoo_util_help, export_file_preds, semidet).
type_modes(logicmoo_util_help, some_flocation(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_help, some_location(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_help, no_location(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_help, current_predicate_mfa(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_help, mpred_source_file_0(:(any), ?(any)), semidet).
type_modes(logicmoo_util_help, mpred_source_file(:(any), ?(any)), semidet).
type_modes(logicmoo_util_help, module_meta_transparent(:(any)), semidet).
type_modes(logicmoo_util_help, list_file_preds(?(any), ?(any)), semidet).
type_modes(logicmoo_util_help, list_file_preds(?(any)), semidet).
type_modes(logicmoo_util_help, scan_and_list_file_preds(?(any)), semidet).
type_modes(logicmoo_util_help, to_mfa_0(?(any), ?(any), :(any), :(any)), semidet).
type_modes(logicmoo_util_help, is_crossfile_module_0(?(any)), semidet).
type_modes(logicmoo_util_help, skip_functor_export_0(:(any)), semidet).
type_modes(logicmoo_util_help, list_file_preds, semidet).
type_modes(logicmoo_util_help, functor_compare(?(any), ?(any), :(any)), semidet).
type_modes(logicmoo_util_help, to_comparable_name_arity(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_help, portray_clause_pi_UD(?(any), ?(any)), semidet).
type_modes(logicmoo_util_help, portray_clause_pi_LR(?(any), ?(any)), semidet).
type_modes(logicmoo_util_help, list_item_per_line(:(any), ?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_help, list_item_per_line(:(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_help, helper_name(:(any)), semidet).
type_modes(logicmoo_util_help, helper_name0(?(any)), semidet).
type_modes(logicmoo_util_help, make_module_name(?(any), ?(any)), semidet).
type_modes(logicmoo_util_shared_dynamic, goal_expansion(?(any), ?(any)), semidet).
type_modes(logicmoo_util_shared_dynamic, decl_shared(:(any)), semidet).
type_modes(logicmoo_util_shared_dynamic, safe_wrap(?(any), +any, -any), semidet).
type_modes(logicmoo_util_shared_dynamic, system_goal_expansion_safe_wrap(:(any), :(any)), semidet).
type_modes(logicmoo_util_shared_dynamic, wrap_shared(?(any), :(any), ?(any)), semidet).
type_modes(logicmoo_util_dmsg, cls, semidet).
type_modes(logicmoo_util_dmsg, writeFailureLog(?(any), ?(any)), semidet).
type_modes(logicmoo_util_dmsg, flush_output_safe(?(any)), semidet).
type_modes(logicmoo_util_dmsg, flush_output_safe, semidet).
type_modes(logicmoo_util_dmsg, withFormatter(?(any), ?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_dmsg, msg_to_string(:(any), ?(any)), semidet).
type_modes(logicmoo_util_dmsg, sgr_code_on_off(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_dmsg, sgr_off_code(?(any), :(any)), semidet).
type_modes(logicmoo_util_dmsg, sgr_on_code0(?(any), :(any)), semidet).
type_modes(logicmoo_util_dmsg, is_sgr_on_code(?(any)), semidet).
type_modes(logicmoo_util_dmsg, sgr_on_code(?(any), :(any)), semidet).
type_modes(logicmoo_util_dmsg, contrasting_color(?(any), ?(any)), semidet).
type_modes(logicmoo_util_dmsg, next_color(:(any)), semidet).
type_modes(logicmoo_util_dmsg, tst_color(?(any)), semidet).
type_modes(logicmoo_util_dmsg, tst_color, semidet).
type_modes(logicmoo_util_dmsg, random_color(?(any)), semidet).
type_modes(logicmoo_util_dmsg, fg_color(?(any), ?(any)), semidet).
type_modes(logicmoo_util_dmsg, unliked_ctrl(?(any)), semidet).
type_modes(logicmoo_util_dmsg, good_next_color(?(any)), semidet).
type_modes(logicmoo_util_dmsg, last_used_fg_color(?(any)), semidet).
type_modes(logicmoo_util_dmsg, functor_color(?(any), ?(any)), semidet).
type_modes(logicmoo_util_dmsg, predef_functor_color(?(any), ?(any)), semidet).
type_modes(logicmoo_util_dmsg, defined_message_color(?(any), ?(any)), semidet).
type_modes(logicmoo_util_dmsg, mesg_arg1(:(any), ?(any)), semidet).
type_modes(logicmoo_util_dmsg, f_word(?(any), ?(any)), semidet).
type_modes(logicmoo_util_dmsg, mesg_color(:(any), ?(any)), semidet).
type_modes(logicmoo_util_dmsg, term_color0(?(any), ?(any)), semidet).
type_modes(logicmoo_util_dmsg, keep_line_pos_w_w(?(any), :(any)), semidet).
type_modes(logicmoo_util_dmsg, ansicall1(?(any), ?(any), :(any)), semidet).
type_modes(logicmoo_util_dmsg, ansicall0(?(any), ?(any), :(any)), semidet).
type_modes(logicmoo_util_dmsg, ansicall(?(any), ?(any), :(any)), semidet).
type_modes(logicmoo_util_dmsg, is_tty(?(any)), semidet).
type_modes(logicmoo_util_dmsg, ansi_control_conv(?(any), ?(any)), semidet).
type_modes(logicmoo_util_dmsg, ansicall(?(any), :(any)), semidet).
type_modes(logicmoo_util_dmsg, colormsg(?(any), ?(any)), semidet).
type_modes(logicmoo_util_dmsg, debugm(?(any), ?(any)), semidet).
type_modes(logicmoo_util_dmsg, debugm(?(any)), semidet).
type_modes(logicmoo_util_dmsg, ansifmt(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_dmsg, ansifmt(?(any), ?(any)), semidet).
type_modes(logicmoo_util_dmsg, get_indent_level(:(any)), semidet).
type_modes(logicmoo_util_dmsg, dmsg5(?(any), ?(any)), semidet).
type_modes(logicmoo_util_dmsg, dmsg5(?(any)), semidet).
type_modes(logicmoo_util_dmsg, dmsg4(?(any)), semidet).
type_modes(logicmoo_util_dmsg, dmsg3(?(any)), semidet).
type_modes(logicmoo_util_dmsg, dmsg2(:(any)), semidet).
type_modes(logicmoo_util_dmsg, dmsg1(?(any)), semidet).
type_modes(logicmoo_util_dmsg, dmsg000(?(any)), semidet).
type_modes(logicmoo_util_dmsg, dmsg00(?(any)), semidet).
type_modes(logicmoo_util_dmsg, dmsg0(?(any)), semidet).
type_modes(logicmoo_util_dmsg, dmsg(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_dmsg, vdmsg(?(any), ?(any)), semidet).
type_modes(logicmoo_util_dmsg, dmsg0(?(any), ?(any)), semidet).
type_modes(logicmoo_util_dmsg, dmsginfo(?(any)), semidet).
type_modes(logicmoo_util_dmsg, dmsg(?(any), ?(any)), semidet).
type_modes(logicmoo_util_dmsg, dmsg(?(any)), semidet).
type_modes(logicmoo_util_dmsg, if_color_debug(:(any), :(any)), semidet).
type_modes(logicmoo_util_dmsg, if_color_debug(:(any)), semidet).
type_modes(logicmoo_util_dmsg, if_color_debug, semidet).
type_modes(logicmoo_util_dmsg, prepend_each_line(?(any), :(any)), semidet).
type_modes(logicmoo_util_dmsg, indent_to_spaces(:(any), ?(any)), semidet).
type_modes(logicmoo_util_dmsg, with_current_indent(:(any)), semidet).
type_modes(logicmoo_util_dmsg, in_cmt(:(any)), semidet).
type_modes(logicmoo_util_dmsg, print_prepended_lines(?(any), :(any)), semidet).
type_modes(logicmoo_util_dmsg, print_prepended(?(any), ?(any)), semidet).
type_modes(logicmoo_util_dmsg, portray_clause_w_vars(?(any)), semidet).
type_modes(logicmoo_util_dmsg, source_variables_lwv(?(any)), semidet).
type_modes(logicmoo_util_dmsg, portray_clause_w_vars(?(any), ?(any)), semidet).
type_modes(logicmoo_util_dmsg, portray_clause_w_vars(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_dmsg, portray_clause_w_vars(?(any), ?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_dmsg, sformat(?(any), ?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_dmsg, with_dmsg(?(any), :(any)), semidet).
type_modes(logicmoo_util_dmsg, to_stderror(:(any)), semidet).
type_modes(logicmoo_util_dmsg, with_output_to_stream(?(any), :(any)), semidet).
type_modes(logicmoo_util_dmsg, dfmt(?(any), ?(any)), semidet).
type_modes(logicmoo_util_dmsg, dfmt(?(any)), semidet).
type_modes(logicmoo_util_dmsg, with_output_to_main(:(any)), semidet).
type_modes(logicmoo_util_dmsg, with_output_to_console(:(any)), semidet).
type_modes(logicmoo_util_dmsg, fmt_or_pp(?(any)), semidet).
type_modes(logicmoo_util_dmsg, fmt_portray_clause(?(any)), semidet).
type_modes(logicmoo_util_dmsg, fmt_ansi(:(any)), semidet).
type_modes(logicmoo_util_dmsg, tst_fmt, semidet).
type_modes(logicmoo_util_dmsg, fmt9(?(any)), semidet).
type_modes(logicmoo_util_dmsg, fmt(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_dmsg, fmt(?(any), ?(any)), semidet).
type_modes(logicmoo_util_dmsg, fmt(?(any)), semidet).
type_modes(logicmoo_util_dmsg, fmt0(?(any)), semidet).
type_modes(logicmoo_util_dmsg, fmt0(?(any), ?(any)), semidet).
type_modes(logicmoo_util_dmsg, fmt0(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_dmsg, dmsg_text_to_string_safe(?(any), ?(any)), semidet).
type_modes(logicmoo_util_dmsg, indent_e(?(any)), semidet).
type_modes(logicmoo_util_dmsg, dmsg_showall(?(any)), semidet).
type_modes(logicmoo_util_dmsg, dmsg_show(?(any)), semidet).
type_modes(logicmoo_util_dmsg, dmsg_hide(?(any)), semidet).
type_modes(logicmoo_util_dmsg, dmsg_hides_message(?(any)), semidet).
type_modes(logicmoo_util_dmsg, with_no_dmsg(?(any), :(any)), semidet).
type_modes(logicmoo_util_dmsg, with_no_dmsg(:(any)), semidet).
type_modes(logicmoo_util_dmsg, with_show_dmsg(?(any), :(any)), semidet).
type_modes(logicmoo_util_dmsg, with_all_dmsg(:(any)), semidet).
type_modes(nr, blocked(+any), semidet).
type_modes(logicmoo_util_prolog_streams, test2(?(any)), semidet).
type_modes(logicmoo_util_prolog_streams, test1_0(?(any)), semidet).
type_modes(logicmoo_util_prolog_streams, read_received(?(any)), semidet).
type_modes(logicmoo_util_prolog_streams, with_input_from_pred(:(any), :(any)), semidet).
type_modes(logicmoo_util_prolog_streams, l_prolog_streams, semidet).
type_modes(logicmoo_util_prolog_streams, buffer_chars(?(any)), semidet).
type_modes(logicmoo_util_prolog_streams, with_write_stream_pred(:(any), -any, :(any), :(any)), semidet).
type_modes(logicmoo_util_prolog_streams, some_test, semidet).
type_modes(logicmoo_util_prolog_streams, with_err_to_pred(:(any), :(any)), semidet).
type_modes(logicmoo_util_prolog_streams, with_output_to_pred(:(any), :(any)), semidet).
type_modes(logicmoo_util_prolog_streams, on_x_fail_priv(:(any)), semidet).
type_modes(logicmoo_util_no_repeatsqqqqqqqqqq, blocked(+any), semidet).
type_modes(logicmoo_util_preddefs, logicmoo_util_preddefs, semidet).
type_modes(logicmoo_util_preddefs, rebuild_pred_into(:(any), :(any), :(any), ?(any)), semidet).
type_modes(logicmoo_util_preddefs, rebuild_pred_into(:(any), :(any), ?(any)), semidet).
type_modes(logicmoo_util_preddefs, pred_prop(:(any), :(any), ?(any)), semidet).
type_modes(logicmoo_util_preddefs, pred_prop(:(any), :(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_preddefs, dynamic_safe(+any, +any, +any), semidet).
type_modes(logicmoo_util_preddefs, rebuild_as_dyn(?(any), ?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_preddefs, convert_to_dynamic(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_preddefs, convert_to_dynamic(?(any)), semidet).
type_modes(logicmoo_util_preddefs, dynamic_safe(+any), semidet).
type_modes(logicmoo_util_preddefs, static_predicate(:(any)), semidet).
type_modes(logicmoo_util_preddefs, static_predicate(+any, +any, +any), semidet).
type_modes(logicmoo_util_preddefs, get_module_of(:(any), -any), semidet).
type_modes(logicmoo_util_preddefs, get_module_of_4(:(any), +any, +any, -any), semidet).
type_modes(logicmoo_util_preddefs, get_pi(?(any), ?(any)), semidet).
type_modes(logicmoo_util_preddefs, dynamic_if_missing(:(any)), semidet).
type_modes(logicmoo_util_preddefs, multi_transparent(:(any)), semidet).
type_modes(logicmoo_util_preddefs, dynamic_transparent(:(any)), semidet).
type_modes(logicmoo_util_preddefs, dynamic_multifile(:(any)), semidet).
type_modes(logicmoo_util_preddefs, current_predicate_module(:(any), ?(any)), semidet).
type_modes(logicmoo_util_preddefs, p_predicate_property(:(any), ?(any)), semidet).
type_modes(logicmoo_util_preddefs, call_if_defined(:(any)), semidet).
type_modes(logicmoo_util_preddefs, remove_pred(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_preddefs, def_meta_predicate(:(any), +any, +any), semidet).
type_modes(logicmoo_util_preddefs, fill_args(?(any), ?(any)), semidet).
type_modes(logicmoo_util_preddefs, with_pfa_single(:(any), ?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_preddefs, with_pfa_group(:(any), +any, +any, +any), semidet).
type_modes(logicmoo_util_preddefs, add_mi(?(any), ?(any), :(any)), semidet).
type_modes(logicmoo_util_preddefs, to_canonical_mpi(:(any), ?(any)), semidet).
type_modes(logicmoo_util_preddefs, only_3rd(:(any), ?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_preddefs, m_fa_to_m_p_fa(?(any), ?(any)), semidet).
type_modes(logicmoo_util_preddefs, m_m_fa_to_m_p_fa(?(any), ?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_preddefs, with_pfa(:(any), +any, +any, +any), semidet).
type_modes(logicmoo_util_preddefs, with_pfa(:(any), +any), semidet).
type_modes(logicmoo_util_preddefs, make_shared_multifile(?(any), ?(any), :(any)), semidet).
type_modes(logicmoo_util_preddefs, save_was(?(any), ?(any), ?(any), :(any)), semidet).
type_modes(logicmoo_util_preddefs, was_multifile(?(any)), semidet).
type_modes(logicmoo_util_preddefs, was_module_transparent(?(any)), semidet).
type_modes(logicmoo_util_preddefs, was_export(?(any)), semidet).
type_modes(logicmoo_util_preddefs, was_dynamic(?(any)), semidet).
type_modes(logicmoo_util_preddefs, shared_multifile(+any), semidet).
type_modes(logicmoo_util_preddefs, context_module_of_file(-any), semidet).
type_modes(logicmoo_util_preddefs, make_transparent(?(any), ?(any), ?(any), :(any)), semidet).
type_modes(logicmoo_util_preddefs, with_mfa_of(:(any), +any, +any, +any, +any), semidet).
type_modes(logicmoo_util_preddefs, with_mfa(:(any), :(any)), semidet).
type_modes(logicmoo_util_preddefs, with_pi_stub(+any, +any, +any, +any, :(any)), semidet).
type_modes(logicmoo_util_preddefs, must_pi(:(any)), semidet).
type_modes(logicmoo_util_preddefs, with_pi_selected(+any, +any, +any, +any), semidet).
type_modes(logicmoo_util_preddefs, with_pi(:(any), :(any)), semidet).
type_modes(logicmoo_util_preddefs, with_pi(?(any)), semidet).
type_modes(tlbugger, blocked(+any), semidet).
type_modes(logicmoo_util_no_repeats, no_repeats_var(+any), det).
type_modes(logicmoo_util_no_repeats, no_repeats_findall_r(+any, :(any), -any, -any, -any), semidet).
type_modes(logicmoo_util_no_repeats, no_repeats_save(+any, :(any)), semidet).
type_modes(logicmoo_util_no_repeats, no_repeats_save(+any, :(any), -any, -any), semidet).
type_modes(logicmoo_util_no_repeats, no_repeats_findall5(+any, :(any), -any, -any, -any), semidet).
type_modes(logicmoo_util_no_repeats, succeeds_n_times(:(any), -any), semidet).
type_modes(logicmoo_util_no_repeats, subtract_eq(:(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_no_repeats, subtract_eq(+any, +any, -any), det).
type_modes(logicmoo_util_no_repeats, no_repeats_u(+any, :(any)), semidet).
type_modes(logicmoo_util_no_repeats, no_repeats_old(+any, :(any)), semidet).
type_modes(logicmoo_util_no_repeats, memberchk_pred_rev(:(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_no_repeats, memberchk_pred(:(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_no_repeats, memberchk_same(?(any), :(any)), semidet).
type_modes(logicmoo_util_no_repeats, no_repeats_old(:(any)), semidet).
type_modes(logicmoo_util_no_repeats, no_repeats(+any, :(any)), semidet).
type_modes(logicmoo_util_no_repeats, no_repeats(:(any)), semidet).
type_modes(logicmoo_util_no_repeats, no_repeats_av, semidet).
type_modes(logicmoo_util_no_repeats, must_not_repeat(:(any)), semidet).
type_modes(logicmoo_util_no_repeats, loop_check_nr(?(any)), semidet).
type_modes(mfree, blocked(+any), semidet).
type_modes(logicmoo_util_strings, longest_string(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, sort_by_strlen(?(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, atoms_of(:(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, str_contains_all0(:(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, str_contains_all(?(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, to_word_list_2(?(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, is_simple_split(?(any)), semidet).
type_modes(logicmoo_util_strings, is_ending(?(any)), semidet).
type_modes(logicmoo_util_strings, read_stream_to_arglist(?(any), :(any)), semidet).
type_modes(logicmoo_util_strings, to_word_list_0(?(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, as_atom(?(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, to_word_list(?(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, replace_periods_string_list(?(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, to_list_of_sents(?(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, replace_periods(?(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, replace_in_string(?(any), ?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, replace_in_string(?(any), ?(any), ?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, convert_members(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, empty_string(?(any)), semidet).
type_modes(logicmoo_util_strings, atom_subst(?(any), ?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, ommitable(?(any)), semidet).
type_modes(logicmoo_util_strings, as_nc_str(?(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, string_equal_ci1(?(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, string_equal_ci0(?(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, string_equal_ci(?(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, divide_list(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, append_ci0(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, append_ci(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, string_ci(?(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, member_ci(?(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, remove_predupes(:(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, string_predicate(:(any)), semidet).
type_modes(logicmoo_util_strings, string_dedupe(?(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, ends_with_icase(?(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, starts_or_ends_with_icase(?(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, either_starts_with_icase(?(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, starts_with_icase(?(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, equals_icase(?(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, either_empty(?(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, both_empty(?(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, must_nonvar(?(any)), semidet).
type_modes(logicmoo_util_strings, non_empty(?(any)), semidet).
type_modes(logicmoo_util_strings, map_tree_to_list(:(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, interleave(:(any), ?(any), :(any)), semidet).
type_modes(logicmoo_util_strings, atomSplit2(:(any), :(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, atomSplit(?(any), ?(any), :(any)), semidet).
type_modes(logicmoo_util_strings, atomSplitEasy(?(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, atomSplit(?(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, vars_to_ucase_0(:(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, vars_to_ucase(?(any), :(any)), semidet).
type_modes(logicmoo_util_strings, merge_vars(:(any), ?(any), :(any)), semidet).
type_modes(logicmoo_util_strings, splt_words_0_atom(?(any), :(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, splt_words_0(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, splt_words(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, catch_read_term_from_atom(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, atomic_list_concat_catch(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, any_to_string1(?(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, any_to_string(?(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, ltrim(?(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, addSpaceBeforeSym(?(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, trim2(?(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, trim(?(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, stringToCodelist2(:(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, stringToCodelist(?(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, stringToList(:(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, destringify(:(any), :(any)), semidet).
type_modes(logicmoo_util_strings, escapeCodes(?(any), ?(any), :(any), :(any)), semidet).
type_modes(logicmoo_util_strings, escapeString(?(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, isWhitespace(?(any)), semidet).
type_modes(logicmoo_util_strings, is_ftString2(?(any)), semidet).
type_modes(logicmoo_util_strings, is_codelist(:(any)), semidet).
type_modes(logicmoo_util_strings, is_charlist(:(any)), semidet).
type_modes(logicmoo_util_strings, unquoteAtom(?(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, quoteAtomString(?(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, to_first_break_w(:(any), ?(any), ?(any), ?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, to_first_break(?(any), ?(any), ?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, to_first_break(?(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, to_case_breaks(:(any), ?(any), ?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, hide_char_type(?(any)), semidet).
type_modes(logicmoo_util_strings, ctype_switch(?(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, ctype_continue(?(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, breaked_codes(?(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, ctype_switcher(?(any)), semidet).
type_modes(logicmoo_util_strings, char_type_this(?(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, to_case_breaks(?(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, capitalized(?(any)), semidet).
type_modes(logicmoo_util_strings, empty_str(?(any)), semidet).
type_modes(logicmoo_util_strings, rejoined(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, toCaseSplit(?(any), :(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, toCase(:(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, toPropercase(?(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, unCamelcase(?(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, toCamelcase(?(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, toUppercase(?(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, toLowercase(?(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, text_to_string_safe(?(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, to_titlecase(?(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, first_char_to_lower(?(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, first_char_to_upper(?(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, noCaseChange(?(any)), semidet).
type_modes(logicmoo_util_strings, atomic_concat(?(any), ?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, list_replace(?(any), ?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, convert_to_string(?(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, atomic_list_concat_safe(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, atomic_list_concat_safe(?(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, any_to_string_or_var(?(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, any_to_atom(?(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, atom_contains(?(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, all_upper_atom(?(any)), semidet).
type_modes(logicmoo_util_strings, clean_out_atom(?(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, clean_codes(?(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, atomsSameCI(?(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, concat_atom_safe(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, concat_atom_safe(?(any), ?(any)), semidet).
type_modes(logicmoo_util_strings, camelSplitters(?(any)), semidet).
type_modes(logicmoo_util_strings, string_to_atom_safe(?(any), ?(any)), semidet).
type_modes(lmconf, blocked(+any), semidet).
type_modes(logicmoo_util_rtrace, on_x_rtrace(:(any)), semidet).
type_modes(logicmoo_util_rtrace, fixhotrace(:(any)), semidet).
type_modes(logicmoo_util_rtrace, traceafter_call(:(any)), semidet).
type_modes(logicmoo_util_rtrace, ftrace(:(any)), semidet).
type_modes(logicmoo_util_rtrace, motrace(:(any)), semidet).
type_modes(logicmoo_util_rtrace, rtrace(:(any)), semidet).
type_modes(logicmoo_util_rtrace, restore_trace(:(any), :(any)), semidet).
type_modes(logicmoo_util_rtrace, restore_trace(:(any)), semidet).
type_modes(logicmoo_util_rtrace, get_tracer(?(any)), semidet).
type_modes(logicmoo_util_rtrace, reset_tracer, semidet).
type_modes(logicmoo_util_rtrace, pop_tracer, semidet).
type_modes(logicmoo_util_rtrace, push_tracer, semidet).
type_modes(logicmoo_util_rtrace, nortrace, semidet).
type_modes(logicmoo_util_rtrace, rtrace, semidet).
type_modes(logicmoo_util_rtrace, restore_guitracer, semidet).
type_modes(logicmoo_util_rtrace, save_guitracer, semidet).
type_modes(logicmoo_util_rtrace, hotrace(:(any)), semidet).
type_modes(logicmoo_util_rtrace, hotrace0(:(any)), semidet).
type_modes(logicmoo_util_rtrace, thread_leash(?(any)), semidet).
type_modes(logicmoo_util_rtrace, hotrace, semidet).
type_modes(logicmoo_util_rtrace, cnotrace(:(any)), semidet).
type_modes(logicmoo_util_rtrace, grtrace(:(any)), semidet).
type_modes(logicmoo_util_rtrace, grtrace, semidet).
type_modes(logicmoo_util_rtrace, gftrace(:(any)), semidet).
type_modes(logicmoo_util_rtrace, gftrace, semidet).
type_modes(logicmoo_util_rtrace, ggtrace(:(any)), semidet).
type_modes(logicmoo_util_rtrace, ggtrace, semidet).
type_modes(logicmoo_util_catch, get_must(?(any), ?(any)), semidet).
type_modes(logicmoo_util_catch, must(:(any)), semidet).
type_modes(logicmoo_util_catch, y_must(?(any), :(any)), semidet).
type_modes(logicmoo_util_catch, without_must(:(any)), semidet).
type_modes(logicmoo_util_catch, badfood(?(any)), semidet).
type_modes(logicmoo_util_catch, not_is_release, semidet).
type_modes(logicmoo_util_catch, is_release, semidet).
type_modes(logicmoo_util_catch, sanity(:(any)), semidet).
type_modes(logicmoo_util_catch, slow_sanity(:(any)), semidet).
type_modes(logicmoo_util_catch, must_l(:(any)), semidet).
type_modes(logicmoo_util_catch, det_lm(?(any), ?(any)), semidet).
type_modes(logicmoo_util_catch, must_det_lm(?(any), :(any)), semidet).
type_modes(logicmoo_util_catch, must_det_l(:(any)), semidet).
type_modes(logicmoo_util_catch, must_det(:(any), :(any)), semidet).
type_modes(logicmoo_util_catch, one_must_det(:(any), :(any)), semidet).
type_modes(logicmoo_util_catch, must_det(:(any)), semidet).
type_modes(logicmoo_util_catch, one_must(:(any), :(any)), semidet).
type_modes(logicmoo_util_catch, skipWrapper, semidet).
type_modes(logicmoo_util_catch, errx, semidet).
type_modes(logicmoo_util_catch, on_x_log_cont(:(any)), semidet).
type_modes(logicmoo_util_catch, on_x_log_throw(:(any)), semidet).
type_modes(logicmoo_util_catch, on_x_log_fail(:(any)), semidet).
type_modes(logicmoo_util_catch, showHiddens, semidet).
type_modes(logicmoo_util_catch, on_x_fail(:(any)), semidet).
type_modes(logicmoo_util_catch, trace_or_throw(?(any)), semidet).
type_modes(logicmoo_util_catch, nop(?(any)), semidet).
type_modes(logicmoo_util_catch, strip_arity(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_catch, module_functor(?(any), ?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_catch, nd_dbgsubst2(?(any), ?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_catch, nd_dbgsubst1(?(any), ?(any), ?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_catch, nd_dbgsubst(?(any), ?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_catch, dbgsubst0(?(any), ?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_catch, dbgsubst(?(any), ?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_catch, with_preds(?(any), ?(any), ?(any), ?(any), ?(any), :(any)), semidet).
type_modes(logicmoo_util_catch, to_m_f_arity_pi(?(any), ?(any), ?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_catch, dif_safe(?(any), ?(any)), semidet).
type_modes(logicmoo_util_catch, ib_multi_transparent33(?(any)), semidet).
type_modes(logicmoo_util_catch, addLibraryDir, semidet).
type_modes(logicmoo_util_catch, throwNoLib, semidet).
type_modes(logicmoo_util_catch, !(?(any)), semidet).
type_modes(logicmoo_util_catch, block(?(any), ?(any)), semidet).
type_modes(logicmoo_util_catch, set_block_exit(?(any), ?(any)), semidet).
type_modes(logicmoo_util_catch, keep(?(any), ?(any)), semidet).
type_modes(logicmoo_util_catch, block(+any, ?(any), ?(any)), semidet).
type_modes(logicmoo_util_catch, functor_safe(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_catch, functor_catch(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_catch, catchv(:(any), ?(any), :(any)), semidet).
type_modes(logicmoo_util_catch, bubbled_ex_check(?(any)), semidet).
type_modes(logicmoo_util_catch, bubbled_ex(?(any)), semidet).
type_modes(logicmoo_util_catch, strip_f_module(?(any), ?(any)), semidet).
type_modes(logicmoo_util_catch, warn_bad_functor(?(any)), semidet).
type_modes(logicmoo_util_catch, bad_functor(?(any)), semidet).
type_modes(logicmoo_util_catch, maplist_safe(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_catch, maplist_safe(?(any), ?(any)), semidet).
type_modes(logicmoo_util_catch, is_ftNonvar(?(any)), semidet).
type_modes(logicmoo_util_catch, is_ftVar(:(any)), semidet).
type_modes(logicmoo_util_catch, is_ftCompound(?(any)), semidet).
type_modes(logicmoo_util_catch, as_clause_w_m(?(any), ?(any), ?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_catch, as_clause_w_m(?(any), ?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_catch, as_clause_no_m(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_catch, show_source_location, semidet).
type_modes(logicmoo_util_catch, source_variables(?(any)), semidet).
type_modes(logicmoo_util_catch, source_variables_l(?(any)), semidet).
type_modes(logicmoo_util_catch, source_file0(?(any)), semidet).
type_modes(logicmoo_util_catch, loading_file(?(any)), semidet).
type_modes(logicmoo_util_catch, source_module(?(any)), semidet).
type_modes(logicmoo_util_catch, current_why(?(any)), semidet).
type_modes(logicmoo_util_catch, current_source_location0(:(any)), semidet).
type_modes(logicmoo_util_catch, current_source_file(?(any)), semidet).
type_modes(logicmoo_util_catch, sl_to_filename(?(any), ?(any)), semidet).
type_modes(logicmoo_util_catch, show_new_src_location(?(any), ?(any)), semidet).
type_modes(logicmoo_util_catch, show_new_src_location(?(any)), semidet).
type_modes(logicmoo_util_catch, input_key(?(any)), semidet).
type_modes(logicmoo_util_catch, to_pi0(?(any), :(any), :(any)), semidet).
type_modes(logicmoo_util_catch, to_pi(?(any), ?(any)), semidet).
type_modes(logicmoo_util_catch, when_defined(?(any)), semidet).
type_modes(logicmoo_util_catch, if_defined_else(?(any), :(any)), semidet).
type_modes(logicmoo_util_catch, if_defined(?(any), :(any)), semidet).
type_modes(logicmoo_util_catch, if_defined(?(any)), semidet).
type_modes(logicmoo_util_catch, ddmsg_call(:(any)), semidet).
type_modes(logicmoo_util_catch, ddmsg(?(any), ?(any)), semidet).
type_modes(logicmoo_util_catch, ddmsg(?(any)), semidet).
type_modes(logicmoo_util_catch, is_hiding_dmsgs, semidet).
type_modes(logicmoo_util_catch, with_main_io(:(any)), semidet).
type_modes(logicmoo_util_catch, with_main_input(:(any)), semidet).
type_modes(logicmoo_util_catch, save_streams(?(any)), semidet).
type_modes(logicmoo_util_catch, save_streams, semidet).
type_modes(logicmoo_util_catch, fresh_line_to_err, semidet).
type_modes(logicmoo_util_catch, format_to_error(?(any), ?(any)), semidet).
type_modes(logicmoo_util_catch, current_main_error_stream(?(any)), semidet).
type_modes(logicmoo_util_catch, thread_current_error_stream(?(any)), semidet).
type_modes(logicmoo_util_catch, with_main_error_to_output(:(any)), semidet).
type_modes(logicmoo_util_catch, is_main_thread, semidet).
type_modes(logicmoo_util_catch, is_pdt_like, semidet).
type_modes(logicmoo_util_catch, catchvvnt(:(any), ?(any), :(any)), semidet).
type_modes(logicmoo_util_filesystem, os_to_prolog_filename(?(any), ?(any)), semidet).
type_modes(logicmoo_util_filesystem, atom_ensure_endswtih(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_filesystem, join_path(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_filesystem, canonical_pathname(?(any), ?(any)), semidet).
type_modes(logicmoo_util_filesystem, relative_pathname(?(any), ?(any)), semidet).
type_modes(logicmoo_util_filesystem, global_pathname(?(any), ?(any)), semidet).
type_modes(logicmoo_util_filesystem, if_file_exists(?(any)), semidet).
type_modes(logicmoo_util_filesystem, time_file_safe(?(any), ?(any)), semidet).
type_modes(logicmoo_util_filesystem, upcase_atom_safe(?(any), ?(any)), semidet).
type_modes(logicmoo_util_filesystem, exists_directory_safe(?(any)), semidet).
type_modes(logicmoo_util_filesystem, exists_file_safe(?(any)), semidet).
type_modes(logicmoo_util_filesystem, atom_concat_safe(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_filesystem, exists_dirf(?(any)), semidet).
type_modes(logicmoo_util_filesystem, local_directory_search(?(any)), semidet).
type_modes(logicmoo_util_filesystem, local_directory_search_combined2(?(any)), semidet).
type_modes(logicmoo_util_filesystem, local_directory_search_combined(?(any)), semidet).
type_modes(logicmoo_util_filesystem, locally_to_dir(?(any), ?(any)), semidet).
type_modes(logicmoo_util_filesystem, join_path_if_needed(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_filesystem, my_absolute_file_name(?(any), ?(any)), semidet).
type_modes(logicmoo_util_filesystem, clip_dir_sep(?(any), ?(any)), semidet).
type_modes(logicmoo_util_filesystem, normalize_path(?(any), ?(any)), semidet).
type_modes(logicmoo_util_filesystem, if_startup_script(:(any)), semidet).
type_modes(logicmoo_util_filesystem, if_startup_script, semidet).
type_modes(logicmoo_util_filesystem, add_to_search_path(:(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_filesystem, add_to_search_path(?(any), ?(any)), semidet).
type_modes(logicmoo_util_filesystem, in_search_path(?(any), ?(any)), semidet).
type_modes(logicmoo_util_filesystem, add_to_search_path_last(?(any), ?(any)), semidet).
type_modes(logicmoo_util_filesystem, add_to_search_path_first(?(any), ?(any)), semidet).
type_modes(logicmoo_util_filesystem, remove_search_path(?(any), ?(any)), semidet).
type_modes(logicmoo_util_filesystem, in_include_file, semidet).
type_modes(logicmoo_util_filesystem, prolog_file_dir(?(any), ?(any)), semidet).
type_modes(logicmoo_util_filesystem, prolog_file_dir(?(any)), semidet).
type_modes(logicmoo_util_filesystem, to_filename(?(any), ?(any)), semidet).
type_modes(logicmoo_util_filesystem, current_dirs0(?(any)), semidet).
type_modes(logicmoo_util_filesystem, current_dirs(?(any)), semidet).
type_modes(logicmoo_util_filesystem, concat_paths(:(any), ?(any)), semidet).
type_modes(logicmoo_util_filesystem, concat_paths(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_filesystem, is_directory(?(any)), semidet).
type_modes(logicmoo_util_filesystem, exists_file_or_dir(?(any)), semidet).
type_modes(logicmoo_util_filesystem, expand_file_name_safe(?(any), ?(any)), semidet).
type_modes(logicmoo_util_filesystem, enumerate_files1(:(any), ?(any)), semidet).
type_modes(logicmoo_util_filesystem, enumerate_files2(?(any), ?(any)), semidet).
type_modes(logicmoo_util_filesystem, filematch3(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_filesystem, enumerate_files00(?(any), ?(any)), semidet).
type_modes(logicmoo_util_filesystem, enumerate_files0(?(any), ?(any)), semidet).
type_modes(logicmoo_util_filesystem, enumerate_m_files_mscoped(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_filesystem, enumerate_m_files(?(any), ?(any), ?(any)), semidet).
type_modes(logicmoo_util_filesystem, enumerate_files(?(any), -any), semidet).
type_modes(logicmoo_util_filesystem, filematch_ext(+any, ?(any), -any), semidet).
type_modes(logicmoo_util_filesystem, filematch(?(any), -any), semidet).
type_modes(logicmoo_util_filesystem, current_filesource(?(any)), semidet).
type_modes(logicmoo_util_filesystem, current_filedir(?(any)), semidet).
type_modes(logicmoo_util_filesystem, expand_wfm(?(any), ?(any)), semidet).
type_modes(logicmoo_util_filesystem, with_filematches(?(any)), semidet).
type_modes(logicmoo_util_filesystem, with_filematch(:(any)), semidet).
type_modes(star, blocked(+any), semidet).
type_modes(t_l, blocked(+any), semidet).
type_modes(thlocal_global, blocked(+any), semidet).
type_modes(tl_with_prolog_streams, blocked(+any), semidet).
type_modes(was_was, blocked(+any), semidet).
type_modes(double_metaphone, double_metaphone(+any, -any, -any), det).
type_modes(double_metaphone, double_metaphone(+any, -any), det).
type_modes(rdf_triple, rdf_reset_ids, det).
type_modes(rdf_triple, anon_prefix(-any), semidet).
type_modes(rdf_triple, rdf_end_file(:(any)), det).
type_modes(rdf_triple, rdf_start_file(+any, -any), det).
type_modes(rdf_triple, collection(+any, -any), unknown).
type_modes(rdf_triple, li_pred(+any, -any, +any, -any), unknown).
type_modes(rdf_triple, statement(+any, +any, +any, +any, +any, -any), unknown).
type_modes(rdf_triple, property(?(any), ?(any), ?(any), ?(any), ?(list), ?(list)), det).
type_modes(rdf_triple, triples(-any, -any, +any, -any), unknown).
type_modes(rdf_triple, rdf_triples(+any, -any, +any), det).
type_modes(rdf_triple, rdf_triples(+any, -any), det).
type_modes(ssl, cert_accept_any(+any, +any, +any, +any, +any), det).
type_modes(ssl, ssl_exit(+any), unknown).
type_modes(ssl, ssl_open(+any, +any, -any, -any), det).
type_modes(ssl, ssl_open(+any, -any, -any), det).
type_modes(ssl, ssl_accept(+any, -any, -any), det).
type_modes(ssl, ssl_init(-any, +any, +any), det).
type_modes(ssl, rsa_public_encrypt(+any, +any, -any, +any), det).
type_modes(ssl, rsa_public_decrypt(+any, +any, -any, +any), det).
type_modes(ssl, rsa_private_encrypt(+any, +any, -any, +any), det).
type_modes(ssl, rsa_private_decrypt(+any, +any, -any, +any), det).
type_modes(ssl, rsa_public_encrypt(+any, +any, -any), det).
type_modes(ssl, rsa_public_decrypt(+any, +any, -any), det).
type_modes(ssl, rsa_private_encrypt(+any, +any, -any), det).
type_modes(ssl, rsa_private_decrypt(+any, +any, -any), det).
type_modes(ssl, load_public_key(+any, -any), det).
type_modes(ssl, load_private_key(+any, +any, -any), det).
type_modes(ssl, system_root_certificates(-any), det).
type_modes(ssl, load_crl(+any, -any), det).
type_modes(ssl, load_certificate(+any, -any), det).
type_modes(ssl, ssl_session(+any, -any), det).
type_modes(ssl, ssl_peer_certificate(+any, -any), semidet).
type_modes(ssl, ssl_negotiate(+any, +any, +any, -any, -any), det).
type_modes(ssl, ssl_context(+any, -any, :(any)), det).
type_modes(pce_meta, classify_class(+any, -any), det).
type_modes(pce_meta, type_accepts_function(+any), unknown).
type_modes(pce_meta, pce_to_pl_type(+any, -any), unknown).
type_modes(pce_meta, implements(?(atom), ?(atom), -object), nondet).
type_modes(pce_meta, implements(?(atom), ?(atom)), nondet).
type_modes(pce_meta, pce_library_class(?(any), ?(any), ?(any), ?(any)), unknown).
type_modes(pce_meta, to_class_name(+any, -any), unknown).
type_modes(pce_meta, current_class(?(any), ?(any)), unknown).
type_modes(pce_meta, isa_class(?(any), ?(any)), unknown).
type_modes(pce_meta, pce_to_method(+any, -any), semidet).
type_modes(tty, menu(+any, +any, -any), semidet).
type_modes(tty, string_action(+any), unknown).
type_modes(tty, tty_flash, unknown).
type_modes(tty, tty_clear, unknown).
type_modes(yall, lambda_calls(+any, +any, -any), det).
type_modes(yall, lambda_calls(+any, -any), det).
type_modes(yall, is_lambda(@any), semidet).
type_modes(yall, expand_lambda(+any, -any), semidet).
type_modes(yall, unify_lambda_parameters(+any, +any, -any, +any, -any), det).
type_modes(yall, /(+any, :(any), ?(any), ?(any), ?(any), ?(any), ?(any), ?(any), ?(any)), unknown).
type_modes(yall, /(+any, :(any), ?(any), ?(any), ?(any), ?(any), ?(any), ?(any)), unknown).
type_modes(yall, /(+any, :(any), ?(any), ?(any), ?(any), ?(any), ?(any)), unknown).
type_modes(yall, /(+any, :(any), ?(any), ?(any), ?(any), ?(any)), unknown).
type_modes(yall, /(+any, :(any), ?(any), ?(any), ?(any)), unknown).
type_modes(yall, /(+any, :(any), ?(any), ?(any)), unknown).
type_modes(yall, /(+any, :(any), ?(any)), unknown).
type_modes(yall, +any/ :(any), unknown).
type_modes(yall, >>(+any, +any, ?(any), ?(any), ?(any), ?(any), ?(any), ?(any), ?(any)), unknown).
type_modes(yall, >>(+any, +any, ?(any), ?(any), ?(any), ?(any), ?(any), ?(any)), unknown).
type_modes(yall, >>(+any, +any, ?(any), ?(any), ?(any), ?(any), ?(any)), unknown).
type_modes(yall, >>(+any, +any, ?(any), ?(any), ?(any), ?(any)), unknown).
type_modes(yall, >>(+any, +any, ?(any), ?(any), ?(any)), unknown).
type_modes(yall, >>(+any, +any, ?(any), ?(any)), unknown).
type_modes(yall, >>(+any, +any, ?(any)), unknown).
type_modes(yall, +any>> +any, unknown).
type_modes(prolog_server, prolog_server(?(any), +any), unknown).
type_modes(malloc_info, malloc_info(-dict), det).
type_modes(malloc_info, mallinfo(-dict), det).
type_modes(dcg_basics, atom(+any, ?(list), ?(list)), det).
type_modes(dcg_basics, prolog_var_name(-atom, ?(list), ?(list)), semidet).
type_modes(dcg_basics, eos(?(list), ?(list)), unknown).
type_modes(dcg_basics, xdigits(-any, ?(list), ?(list)), det).
type_modes(dcg_basics, xdigit(-any, ?(list), ?(list)), semidet).
type_modes(dcg_basics, xinteger(-any, ?(list), ?(list)), semidet).
type_modes(dcg_basics, xinteger(+any, ?(list), ?(list)), det).
type_modes(dcg_basics, number(-any, ?(list), ?(list)), semidet).
type_modes(dcg_basics, number(+any, ?(list), ?(list)), det).
type_modes(dcg_basics, float(?(any), ?(list), ?(list)), det).
type_modes(dcg_basics, integer(?(any), ?(list), ?(list)), det).
type_modes(dcg_basics, digit(?(any), ?(list), ?(list)), det).
type_modes(dcg_basics, digits(?(any), ?(list), ?(list)), det).
type_modes(dcg_basics, alpha_to_lower(?(any), ?(list), ?(list)), semidet).
type_modes(dcg_basics, white(?(list), ?(list)), semidet).
type_modes(dcg_basics, whites(?(list), ?(list)), det).
type_modes(dcg_basics, blanks_to_nl(?(list), ?(list)), semidet).
type_modes(dcg_basics, nonblank(-any, ?(list), ?(list)), semidet).
type_modes(dcg_basics, nonblanks(-any, ?(list), ?(list)), det).
type_modes(dcg_basics, blank(?(list), ?(list)), semidet).
type_modes(dcg_basics, blanks(?(list), ?(list)), det).
type_modes(dcg_basics, string(-any, ?(list), ?(list)), nondet).
type_modes(dcg_basics, string_without(+any, -any, ?(list), ?(list)), det).
type_modes(httpd_wrapper, debug_request(+any, +any, +any, +any, ?(any)), unknown).
type_modes(httpd_wrapper, http_relative_path(+any, -any), det).
type_modes(httpd_wrapper, http_peer(+any, -atom), semidet).
type_modes(httpd_wrapper, http_current_request(-any), semidet).
type_modes(httpd_wrapper, extend_request(+any, +any, -any), unknown).
type_modes(httpd_wrapper, expand_request(+any, -any), unknown).
type_modes(httpd_wrapper, http_send_header(+any), unknown).
type_modes(httpd_wrapper, redirect(+any, -any, -any), semidet).
type_modes(httpd_wrapper, cgi_hook(+any, +any), det).
type_modes(httpd_wrapper, thread_cputime(-any), det).
type_modes(httpd_wrapper, handler_with_output_to(:(any), +any, +any, +any, -any), det).
type_modes(httpd_wrapper, http_done(+any, +any, +any, +any), det).
type_modes(httpd_wrapper, send_error(+any, +any, +any, +any, -any), unknown).
type_modes(httpd_wrapper, cgi_close(+any, +any, +any, +any, -any), det).
type_modes(httpd_wrapper, http_spawned(+any), unknown).
type_modes(httpd_wrapper, http_wrap_spawned(:(any), -any, -any), det).
type_modes(httpd_wrapper, http_wrapper(:(any), +any, +any, -any, +any), det).
type_modes(error_locked, blocked(+any), semidet).
type_modes(md5, md5_hash(+any, -any, +any), det).
type_modes(pldoc_html, pred_anchor_name(+any, - (atom/integer), -atom), det).
type_modes(pldoc_html, mode_anchor_name(+any, -atom), det).
type_modes(pldoc_html, doc_for_wiki_file(+any, +any), det).
type_modes(pldoc_html, html_tokens_for_predicates(+any, +any, ?(list), ?(list)), semidet).
type_modes(pldoc_html, include(+any, +any, +any, ?(list), ?(list)), det).
type_modes(pldoc_html, existing_linked_file(+any, -any), semidet).
type_modes(pldoc_html, file_href(+any, -any), det).
type_modes(pldoc_html, map_extension(+any, -any, ?(any)), det).
type_modes(pldoc_html, file_href(+any, -any, +any), det).
type_modes(pldoc_html, file(+any, +any, ?(list), ?(list)), det).
type_modes(pldoc_html, file(+any, ?(list), ?(list)), det).
type_modes(pldoc_html, in_file(+any, ?(any)), nondet).
type_modes(pldoc_html, object_name(+any, +any, ?(list), ?(list)), det).
type_modes(pldoc_html, term_to_string(+any, -any), det).
type_modes(pldoc_html, localise_object(+any, -any), det).
type_modes(pldoc_html, object_href(+any, -any, +any), det).
type_modes(pldoc_html, object_href(+any, -any), det).
type_modes(pldoc_html, object_ref(+any, +any, ?(list), ?(list)), det).
type_modes(pldoc_html, pred_source_href(+predicate_indicator, +any, -any), semidet).
type_modes(pldoc_html, pred_href(+any, +any, -any), semidet).
type_modes(pldoc_html, manref(+any, -any, +any), det).
type_modes(pldoc_html, predref(+any, +any, ?(list), ?(list)), det).
type_modes(pldoc_html, predref(+any, ?(list), ?(list)), det).
type_modes(pldoc_html, term(+any, +any, +any, ?(list), ?(list)), det).
type_modes(pldoc_html, is_op_type(+any, ?(any)), unknown).
type_modes(pldoc_html, pred_head(+any), det).
type_modes(pldoc_html, canonicalise_predref(+any, - (_/_), +any, -any), det).
type_modes(pldoc_html, object_source_button(+any, +any, ?(list), ?(list)), det).
type_modes(pldoc_html, pred_source_button(+any, +any, ?(list), ?(list)), det).
type_modes(pldoc_html, object_edit_button(+any, +any, ?(list), ?(list)), det).
type_modes(pldoc_html, pred_edit_button(+any, +any, ?(list), ?(list)), det).
type_modes(pldoc_html, anon_vars(+any), det).
type_modes(pldoc_html, pred_dt(+any, +any, ?(any), ?(list), ?(list)), det).
type_modes(pldoc_html, object_tree(+any, +any, +any, ?(list), ?(list)), det).
type_modes(pldoc_html, nav_tree(+any, +any, +any, ?(list), ?(list)), det).
type_modes(pldoc_html, remove_unique_root(+any, -any), unknown).
type_modes(pldoc_html, objects_nav_tree(+any, -any), det).
type_modes(pldoc_html, args(+list), det).
type_modes(pldoc_html, doc_tag_title(+any, -any), det).
type_modes(pldoc_html, tag(+any, +list, ?(list), ?(list)), det).
type_modes(pldoc_html, tags(+any, ?(list), ?(list)), det).
type_modes(pldoc_html, print_html_head(+stream), det).
type_modes(pldoc_html, doc_page_dom(+any, +any, -any), det).
type_modes(pldoc_html, doc_write_html(+stream, +atomic, +any), det).
type_modes(pldoc_html, unquote_filespec(+any, -any), det).
type_modes(pldoc_html, object_synopsis(?(any), ?(any), ?(list), ?(list)), det).
type_modes(pldoc_html, object_page_footer(+any, +any, ?(list), ?(list)), det).
type_modes(pldoc_html, object_page(+any, +any, ?(list), ?(list)), semidet).
type_modes(pldoc_html, re_exported_doc(+list(pi), +atom, +atom, -any, -list(pi)), unknown).
type_modes(pldoc_html, is_pi(@any), semidet).
type_modes(pldoc_html, eq_pi(?(any), ?(any)), semidet).
type_modes(pldoc_html, undocumented(+any, +any, +any, ?(list), ?(list)), det).
type_modes(pldoc_html, need_mode(+atom, +list, -list, ?(list), ?(list)), det).
type_modes(pldoc_html, object(+any, +any, -any, +any, ?(list), ?(list)), det).
type_modes(pldoc_html, objects(+list, +any, ?(list), ?(list)), det).
type_modes(pldoc_html, source_button(+any, +any, ?(list), ?(list)), det).
type_modes(pldoc_html, zoom_button(?(any), +any, ?(list), ?(list)), det).
type_modes(pldoc_html, edit_button(+any, +any, ?(list), ?(list)), det).
type_modes(pldoc_html, reload_button(+any, +any, +any, ?(list), ?(list)), det).
type_modes(pldoc_html, file_title(+list, +any, +any, ?(list), ?(list)), det).
type_modes(pldoc_html, file_header(+any, +any, ?(list), ?(list)), det).
type_modes(pldoc_html, file_info(+any, -any, -any, +any), det).
type_modes(pldoc_html, multifile(+any, +any), semidet).
type_modes(pldoc_html, prolog:doc_is_public_object(+any), semidet).
type_modes(pldoc_html, private(+any, +any), semidet).
type_modes(pldoc_html, obj(+any, -any), det).
type_modes(pldoc_html, doc_hide_private(+any, +any, +any), unknown).
type_modes(pldoc_html, module_info(+any, -any, +any), det).
type_modes(pldoc_html, ensure_doc_objects(+any), det).
type_modes(pldoc_html, xref_doc_object(?(any), ?(any)), nondet).
type_modes(pldoc_html, doc_file_objects(+any, -any, -any, -any, +any), det).
type_modes(pldoc_html, doc_resources(+any, ?(list), ?(list)), det).
type_modes(pldoc_html, doc_for_file(+any, +any), det).
type_modes(bim, index(+any, +any), det).
type_modes(bim, bim_expansion(+any, +any), unknown).
type_modes(thread, thread_options(+any, -any, -any), det).
type_modes(thread, first_solution(-any, :(any), +any), semidet).
type_modes(thread, concurrent_maplist(:(any), +any, +any, +any), unknown).
type_modes(thread, concurrent_maplist(:(any), +any, +any), unknown).
type_modes(thread, concurrent_maplist(:(any), +any), unknown).
type_modes(thread, concur_cleanup(+any, +list, +list), det).
type_modes(thread, worker(+any, +any), det).
type_modes(thread, concur_wait(+any, +queue, +compound, -any, -any), semidet).
type_modes(thread, submit_goals(+any, +any, +any, +any, -any), det).
type_modes(thread, concurrent(+any, :(any), ?(any)), semidet).
type_modes(record, types(+any, -any, -any), unknown).
type_modes(record, defaults(+any, -any, -any), unknown).
type_modes(record, replace_nth(+any, +any, +any, -any), det).
type_modes(record, set_field_predicates(+any, +any, +any, +any, +any, ?(list), ?(list)), det).
type_modes(record, set_predicates(+any, +any, +any, +any, +any, ?(list), ?(list)), det).
type_modes(record, data_predicate(+any, +any, +any, +any, +any, ?(list), ?(list)), det).
type_modes(record, access_predicates(+any, +any, +any, +any, ?(list), ?(list)), det).
type_modes(record, type_goal(+any, +any, -any), det).
type_modes(record, is_predicate(+any, +any, ?(list), ?(list)), det).
type_modes(record, make_predicate(+any, ?(list), ?(list)), det).
type_modes(record, current_record_predicate(?(any), ?(any)), nondet).
type_modes(record, current_record(?(any), :(any)), unknown).
type_modes(record, compile_record(+any, ?(list), ?(list)), det).
type_modes(record, compile_records(+any, -any), det).
type_modes(record, record(+any), unknown).
type_modes(xpce_httpd, http_server(:(any), +any), det).
type_modes(clpb_omit_boolean, blocked(+any), semidet).
type_modes(ctypes, is_digit(-any, +any, +any), det).
type_modes(ctypes, is_digit(+any, +any, -any), det).
type_modes(ctypes, upper_lower(?(any), ?(any)), det).
type_modes(ctypes, to_upper(+any, -any), det).
type_modes(ctypes, to_lower(+any, -any), det).
type_modes(ctypes, is_paren(?(any), ?(any)), semidet).
type_modes(prolog, blocked(+any), semidet).
type_modes(javascript_grammar, q_codes(?(list), ?(list)), unknown).
type_modes(javascript_grammar, regex_literal(?(list), ?(list)), semidet).
type_modes(javascript_grammar, numeric_literal(?(list), ?(list)), unknown).
type_modes(javascript_grammar, string_literal(?(list), ?(list)), semidet).
type_modes(javascript_grammar, comment(?(list), ?(list)), semidet).
type_modes(javascript_grammar, token(-any), semidet).
type_modes(javascript_grammar, js_token(-any, ?(list), ?(list)), unknown).
type_modes(javascript_grammar, keyword(+any, ?(list), ?(list)), semidet).
type_modes(javascript_grammar, + ?(any), unknown).
type_modes(javascript_grammar, *(?(any)), unknown).
type_modes(javascript_grammar, ?(?(any)), unknown).
type_modes(qp_foreign, map_C_type(+any, -any), unknown).
type_modes(qp_foreign, load_foreign_resource(:(any), +any), unknown).
type_modes(qp_foreign, make_foreign_resource_wrapper(:(any), +any, +any), unknown).
type_modes(qp_foreign, make_foreign_wrapper_file(:(any), +any), det).
type_modes(qp_foreign, make_foreign_wrapper_file(:(any)), det).
type_modes(qp_foreign, make_shared_object(+any, :(any), +any), det).
type_modes(qp_foreign, load_foreign_files(+any, :(any), +any), det).
type_modes(qp_foreign, load_foreign_files(:(any), +any), det).
type_modes(qp_foreign, load_foreign_files, det).
type_modes(qp_foreign, make_C_file_header(+any), unknown).
type_modes(qp_foreign, make_C_deinit(+any, +any, +any), det).
type_modes(qp_foreign, make_C_init(+any, +any, +any, +any, +any), unknown).
type_modes(qp_foreign, make_C_output_conversions(+any, :(any)), unknown).
type_modes(qp_foreign, make_C_wrapper_check(+any), unknown).
type_modes(qp_foreign, make_C_wrapper_setup(+any), unknown).
type_modes(qp_foreign, make_C_call(+any, :(any), +any), unknown).
type_modes(qp_foreign, make_C_input_conversions(+any, :(any)), unknown).
type_modes(qp_foreign, make_C_prototype(+any, :(any)), unknown).
type_modes(qp_foreign, make_C_decls(+any, :(any)), unknown).
type_modes(qp_foreign, make_C_header(+any, +any, +any), unknown).
type_modes(qp_foreign, cvt_name(+any, +any, -any), det).
type_modes(qp_foreign, get_foreign_head(:(any), -any, -any), unknown).
type_modes(qp_foreign, make_wrapper(+any, :(any)), unknown).
type_modes(qp_foreign, make_wrappers(+any, +any, +any), unknown).
type_modes(jpl, java_home(-any), semidet).
type_modes(jpl, java_dirs(?(list), ?(list)), det).
type_modes(jpl, add_java_to_ldpath, det).
type_modes(jpl, add_jpl_to_ldpath(+any), det).
type_modes(jpl, libjpl(-any), det).
type_modes(jpl, add_jpl_to_classpath, unknown).
type_modes(jpl, library_search_path(-list, -any), det).
type_modes(jpl, check_shared_object(+any, -any, -any, -any), semidet).
type_modes(jpl, check_java_environment, unknown).
type_modes(jpl, search_path_separator(-atom), unknown).
type_modes(jpl, add_search_path(+any, +any), det).
type_modes(jpl, jpl_pl_syntax(-atom), unknown).
type_modes(jpl, to_atom(+any, -any), unknown).
type_modes(jpl, is_pair(?(term)), unknown).
type_modes(jpl, jpl_servlet_byval(+any, -atom, -atom), unknown).
type_modes(jpl, jpl_servlet_byref(+any, +any, +any), unknown).
type_modes(jpl, jpl_set_element(+jref, -datum), nondet).
type_modes(jpl, jpl_map_element(+jref, -pair(datum, datum)), nondet).
type_modes(jpl, jpl_terms_to_array(+list(term), -jref), semidet).
type_modes(jpl, jpl_list_to_array(+list(datum), -jref), unknown).
type_modes(jpl, jpl_iterator_element(+jref, -datum), unknown).
type_modes(jpl, jpl_hashtable_pair(+jref, -pair(datum, datum)), nondet).
type_modes(jpl, jpl_enumeration_to_list(+jref, -list(datum)), unknown).
type_modes(jpl, jpl_enumeration_element(+jref, -datum), unknown).
type_modes(jpl, jpl_datums_to_array(+list(datum), -jref), unknown).
type_modes(jpl, jpl_array_to_list(+jref, -list(datum)), unknown).
type_modes(jpl, jpl_array_to_length(+jref, -integer), unknown).
type_modes(jpl, jpl_void(-datum), semidet).
type_modes(jpl, jpl_true(-datum), semidet).
type_modes(jpl, jpl_null(-datum), semidet).
type_modes(jpl, jpl_false(-datum), semidet).
type_modes(jpl, jpl_is_void(@any), unknown).
type_modes(jpl, jpl_is_type(@any), unknown).
type_modes(jpl, jpl_is_true(@any), unknown).
type_modes(jpl, jpl_is_ref(@any), unknown).
type_modes(jpl, jpl_is_object_type(@any), unknown).
type_modes(jpl, jpl_is_object(@any), unknown).
type_modes(jpl, jpl_is_null(@any), unknown).
type_modes(jpl, jpl_is_methodID(-any), unknown).
type_modes(jpl, jpl_is_fieldID(-any), unknown).
type_modes(jpl, jpl_is_false(@any), unknown).
type_modes(jpl, jpl_is_class(@any), unknown).
type_modes(jpl, jpl_value_to_type_1(+datum, -type), semidet).
type_modes(jpl, jpl_value_to_type(+datum, -type), unknown).
type_modes(jpl, jpl_types_fit_types(+list(type), +list(type)), unknown).
type_modes(jpl, jpl_types_fit_type(+list(type), +type), unknown).
type_modes(jpl, jpl_type_to_preferred_concrete_type(+type, -type), unknown).
type_modes(jpl, jpl_type_to_super_type(+type, -type), unknown).
type_modes(jpl, jpl_type_to_findclassname(+type, -findClassName), unknown).
type_modes(jpl, jpl_type_to_descriptor(+type, -descriptor), unknown).
type_modes(jpl, jpl_type_to_classname(+type, -dottedName), unknown).
type_modes(jpl, jpl_type_to_nicename(+type, -dottedName), unknown).
type_modes(jpl, jpl_type_to_class(+type, -jref), unknown).
type_modes(jpl, jpl_type_to_canonical_type(+type, -type), unknown).
type_modes(jpl, jpl_type_to_ancestor_types(+type, -list(type)), unknown).
type_modes(jpl, jpl_type_fits_type_xprim(-any, -any), nondet).
type_modes(jpl, jpl_type_fits_type_direct_xtra(-type, -type), unknown).
type_modes(jpl, jpl_type_fits_type_1(+type, +type), unknown).
type_modes(jpl, jpl_type_fits_type(+type, +type), semidet).
type_modes(jpl, jpl_tag_to_type(+tag, -type), unknown).
type_modes(jpl, jpl_ref_to_type(+jref, -type), unknown).
type_modes(jpl, jpl_primitive_type_term_to_value_1(+any, +any, -any), unknown).
type_modes(jpl, jpl_primitive_type_term_to_value(+any, +any, -any), unknown).
type_modes(jpl, jpl_primitive_type_default_value(-type, -datum), unknown).
type_modes(jpl, jpl_primitive_type(-atom), nondet).
type_modes(jpl, jpl_primitive_buffer_to_array(+any, +any, +any, +any, +any, -any), unknown).
type_modes(jpl, jpl_object_to_type(+jref, -type), unknown).
type_modes(jpl, jpl_object_to_class(+jref, -jref), unknown).
type_modes(jpl, jpl_object_array_to_list_1(+any, +any, +any, -any), unknown).
type_modes(jpl, jpl_object_array_to_list(+jref, -list(datum)), unknown).
type_modes(jpl, jpl_ground_is_type(+term), unknown).
type_modes(jpl, jpl_datums_to_types(+list(datum), -list(type)), unknown).
type_modes(jpl, jpl_datum_to_type(+datum, -type), unknown).
type_modes(jpl, jpl_classname_type_cache(-className, -type), unknown).
type_modes(jpl, jpl_classname_to_type(+className, -type), unknown).
type_modes(jpl, jpl_classname_to_class(+className, -jref), unknown).
type_modes(jpl, jpl_class_to_type(+jref, -type), unknown).
type_modes(jpl, jpl_class_to_raw_classname_chars(+jref, -codes), unknown).
type_modes(jpl, jpl_class_to_raw_classname(+jref, -rawName), unknown).
type_modes(jpl, jpl_class_to_classname(+jref, -dottedName), unknown).
type_modes(jpl, jpl_class_to_ancestor_classes(+jref, -list(jref)), unknown).
type_modes(jpl, jpl_class_tag_type_cache(-tag, -type), unknown).
type_modes(jpl, jpl_cache_type_of_ref(+type, +jref), unknown).
type_modes(jpl, jpl_modifier_int_to_modifiers(+integer, -ordset(modifier)), unknown).
type_modes(jpl, jpl_method_to_return_type(+jref, -type), unknown).
type_modes(jpl, jpl_method_to_parameter_types_1(+jref, +jref, -list(type)), unknown).
type_modes(jpl, jpl_method_to_parameter_types(+jref, -list(type)), unknown).
type_modes(jpl, jpl_member_to_name_1(+jref, +jref, -atom), unknown).
type_modes(jpl, jpl_method_to_name(+jref, -atom), unknown).
type_modes(jpl, jpl_method_to_modifiers_1(+jref, +jref, -ordset(modifier)), unknown).
type_modes(jpl, jpl_method_to_modifiers(+jref, -ordset(modifier)), unknown).
type_modes(jpl, jpl_method_spec_1(+jref, +partialType, +list(method), +list(method)), unknown).
type_modes(jpl, jpl_method_spec(+type, -integer, -atom, -integer, -ordset(modifier), -methodId, -type, -list(type)), unknown).
type_modes(jpl, jpl_field_to_type(+jref, -type), unknown).
type_modes(jpl, jpl_field_to_name(+jref, -atom), unknown).
type_modes(jpl, jpl_field_to_modifiers(+jref, -ordset(modifier)), unknown).
type_modes(jpl, jpl_field_spec(+type, -integer, -atom, -any, -mId, -type), unknown).
type_modes(jpl, jpl_constructor_to_return_type(+jref, -type), unknown).
type_modes(jpl, jpl_constructor_to_parameter_types(+jref, -list(type)), unknown).
type_modes(jpl, jpl_constructor_to_name(+jref, -atom), unknown).
type_modes(jpl, jpl_constructor_to_modifiers(+any, -any), unknown).
type_modes(jpl, jpl_class_to_methods(+jref, -list(jref)), unknown).
type_modes(jpl, jpl_class_to_method_array(+jref, -jref), unknown).
type_modes(jpl, jpl_class_to_fields(+jref, -list(jref)), unknown).
type_modes(jpl, jpl_class_to_field_array(+jref, -jref), unknown).
type_modes(jpl, jpl_class_to_constructors(+jref, -list(jref)), unknown).
type_modes(jpl, jpl_class_to_constructor_array(+jref, -jref), unknown).
type_modes(jpl, jni_type_to_xput_code(+any, -any), unknown).
type_modes(jpl, jni_params_put_1(+list(datum), +integer, +list(type), +paramBuf), unknown).
type_modes(jpl, jni_params_put(+list(datum), +list(type), -paramBuf), unknown).
type_modes(jpl, jSetStaticShortField(+jref, +fieldId, +short), unknown).
type_modes(jpl, jSetStaticObjectField(+jref, +fieldId, +jref), unknown).
type_modes(jpl, jSetStaticLongField(+jref, +fieldId, +any), unknown).
type_modes(jpl, jSetStaticIntField(+jref, +fieldId, +int), unknown).
type_modes(jpl, jSetStaticFloatField(+jref, +fieldId, +float), unknown).
type_modes(jpl, jSetStaticDoubleField(+jref, +fieldId, +double), unknown).
type_modes(jpl, jSetStaticCharField(+jref, +fieldId, +char), unknown).
type_modes(jpl, jSetStaticByteField(+jref, +fieldId, +byte), unknown).
type_modes(jpl, jSetStaticBooleanField(+jref, +fieldId, +boolean), unknown).
type_modes(jpl, jSetShortField(+jref, +fieldId, +short), unknown).
type_modes(jpl, jSetShortArrayRegion(+jref, +int, +int, +short_buf), unknown).
type_modes(jpl, jSetObjectField(+jref, +fieldId, +jref), unknown).
type_modes(jpl, jSetObjectArrayElement(+jref, +int, +jref), unknown).
type_modes(jpl, jSetLongField(+jref, +fieldId, +long), unknown).
type_modes(jpl, jSetLongArrayRegion(+jref, +int, +int, +long_buf), unknown).
type_modes(jpl, jSetIntField(+jref, +fieldId, +int), unknown).
type_modes(jpl, jSetIntArrayRegion(+jref, +int, +int, +int_buf), unknown).
type_modes(jpl, jSetFloatField(+jref, +fieldId, +float), unknown).
type_modes(jpl, jSetFloatArrayRegion(+jref, +int, +int, +float_buf), unknown).
type_modes(jpl, jSetDoubleField(+jref, +fieldId, +double), unknown).
type_modes(jpl, jSetDoubleArrayRegion(+jref, +int, +int, +double_buf), unknown).
type_modes(jpl, jSetCharField(+jref, +fieldId, +char), unknown).
type_modes(jpl, jSetCharArrayRegion(+jref, +int, +int, +char_buf), unknown).
type_modes(jpl, jSetByteField(+jref, +fieldId, +byte), unknown).
type_modes(jpl, jSetByteArrayRegion(+jref, +int, +int, +byte_buf), unknown).
type_modes(jpl, jSetBooleanField(+jref, +fieldId, +boolean), unknown).
type_modes(jpl, jSetBooleanArrayRegion(+jref, +int, +int, +boolean_buf), unknown).
type_modes(jpl, jNewShortArray(+int, -jref), unknown).
type_modes(jpl, jNewObjectArray(+int, +jref, +jref, -jref), unknown).
type_modes(jpl, jNewObject(+jref, +methodId, +list(type), +list(datum), -jref), unknown).
type_modes(jpl, jNewLongArray(+int, -jref), unknown).
type_modes(jpl, jNewIntArray(+int, -jref), unknown).
type_modes(jpl, jNewFloatArray(+int, -jref), unknown).
type_modes(jpl, jNewDoubleArray(+int, -jref), unknown).
type_modes(jpl, jNewCharArray(+int, -jref), unknown).
type_modes(jpl, jNewByteArray(+int, -jref), unknown).
type_modes(jpl, jNewBooleanArray(+int, -jref), unknown).
type_modes(jpl, jIsAssignableFrom(+jref, +jref), unknown).
type_modes(jpl, jGetSuperclass(+jref, -jref), unknown).
type_modes(jpl, jGetStaticShortField(+jref, +fieldId, -short), unknown).
type_modes(jpl, jGetStaticObjectField(+jref, +fieldId, -jref), unknown).
type_modes(jpl, jGetStaticMethodID(+jref, +methodName, +type, -methodId), unknown).
type_modes(jpl, jGetStaticLongField(+jref, +fieldId, -long), unknown).
type_modes(jpl, jGetStaticIntField(+jref, +fieldId, -int), unknown).
type_modes(jpl, jGetStaticFloatField(+jref, +fieldId, -float), unknown).
type_modes(jpl, jGetStaticFieldID(+jref, +fieldName, +type, -fieldId), unknown).
type_modes(jpl, jGetStaticDoubleField(+jref, +fieldId, -double), unknown).
type_modes(jpl, jGetStaticCharField(+jref, +fieldId, -char), unknown).
type_modes(jpl, jGetStaticByteField(+jref, +fieldId, -byte), unknown).
type_modes(jpl, jGetStaticBooleanField(+jref, +fieldId, -boolean), unknown).
type_modes(jpl, jGetShortField(+jref, +fieldId, -short), unknown).
type_modes(jpl, jGetShortArrayRegion(+jref, +int, +int, +short_buf), unknown).
type_modes(jpl, jGetObjectField(+jref, +fieldId, -jref), unknown).
type_modes(jpl, jGetObjectClass(+jref, -jref), unknown).
type_modes(jpl, jGetObjectArrayElement(+jref, +int, -jref), unknown).
type_modes(jpl, jGetMethodID(+jref, +atom, +type, -methodId), unknown).
type_modes(jpl, jGetLongField(+jref, +fieldId, -long), unknown).
type_modes(jpl, jGetLongArrayRegion(+jref, +int, +int, +long_buf), unknown).
type_modes(jpl, jGetIntField(+jref, +fieldId, -int), unknown).
type_modes(jpl, jGetIntArrayRegion(+jref, +int, +int, +int_buf), unknown).
type_modes(jpl, jGetFloatField(+jref, +fieldId, -float), unknown).
type_modes(jpl, jGetFloatArrayRegion(+jref, +int, +int, +float_buf), unknown).
type_modes(jpl, jGetFieldID(+jref, +fieldName, +type, -fieldId), unknown).
type_modes(jpl, jGetDoubleField(+jref, +fieldId, -double), unknown).
type_modes(jpl, jGetDoubleArrayRegion(+jref, +int, +int, +double_buf), unknown).
type_modes(jpl, jGetCharField(+jref, +fieldId, -char), unknown).
type_modes(jpl, jGetCharArrayRegion(+jref, +int, +int, +char_buf), unknown).
type_modes(jpl, jGetByteField(+jref, +fieldId, -byte), unknown).
type_modes(jpl, jGetByteArrayRegion(+jref, +int, +int, +byte_buf), unknown).
type_modes(jpl, jGetBooleanField(+jref, +fieldId, -boolean), unknown).
type_modes(jpl, jGetBooleanArrayRegion(+jref, +int, +int, +boolean_buf), unknown).
type_modes(jpl, jGetArrayLength(+jref, -int), unknown).
type_modes(jpl, jFindClass(+findclassname, -jref), unknown).
type_modes(jpl, jCallVoidMethod(+jref, +methodId, +list(type), +list(datum)), unknown).
type_modes(jpl, jCallStaticVoidMethod(+jref, +methodId, +list(type), +list(datum)), unknown).
type_modes(jpl, jCallStaticShortMethod(+jref, +methodId, +list(type), +list(datum), -short), unknown).
type_modes(jpl, jCallStaticObjectMethod(+jref, +methodId, +list(type), +list(datum), -jref), unknown).
type_modes(jpl, jCallStaticLongMethod(+jref, +methodId, +list(type), +list(datum), -long), unknown).
type_modes(jpl, jCallStaticIntMethod(+jref, +methodId, +list(type), +list(datum), -int), unknown).
type_modes(jpl, jCallStaticFloatMethod(+jref, +methodId, +list(type), +list(datum), -float), unknown).
type_modes(jpl, jCallStaticDoubleMethod(+jref, +methodId, +list(type), +list(datum), -double), unknown).
type_modes(jpl, jCallStaticCharMethod(+jref, +methodId, +list(type), +list(datum), -char), unknown).
type_modes(jpl, jCallStaticByteMethod(+jref, +methodId, +list(type), +list(datum), -byte), unknown).
type_modes(jpl, jCallStaticBooleanMethod(+jref, +methodId, +list(type), +list(datum), -boolean), unknown).
type_modes(jpl, jCallShortMethod(+jref, +methodId, +list(type), +list(datum), -short), unknown).
type_modes(jpl, jCallObjectMethod(+jref, +methodId, +list(type), +list(datum), -jref), unknown).
type_modes(jpl, jCallLongMethod(+jref, +methodId, +list(type), +list(datum), -long), unknown).
type_modes(jpl, jCallIntMethod(+jref, +methodId, +list(type), +list(datum), -int), unknown).
type_modes(jpl, jCallFloatMethod(+jref, +methodId, +list(type), +list(datum), -float), unknown).
type_modes(jpl, jCallDoubleMethod(+jref, +methodId, +list(type), +list(datum), -double), unknown).
type_modes(jpl, jCallCharMethod(+jref, +methodId, +list(type), +list(datum), -char), unknown).
type_modes(jpl, jCallByteMethod(+jref, +methodId, +any, +list(datum), -byte), unknown).
type_modes(jpl, jCallBooleanMethod(+jref, +methodId, +list(type), +list(datum), -boolean), unknown).
type_modes(jpl, jpl_c_lib_version(-any), unknown).
type_modes(jpl, jpl_pl_lib_version(-any, -any, -any, -any), unknown).
type_modes(jpl, jpl_pl_lib_version(-any), unknown).
type_modes(jpl, jpl_z5s_to_most_specific_z5(+any, -any), unknown).
type_modes(jpl, jpl_z3s_to_most_specific_z3(+any, -any), unknown).
type_modes(jpl, jpl_fergus_is_the_greatest(+list(A), -A), unknown).
type_modes(jpl, jpl_tidy_iref_type_cache(+any), det).
type_modes(jpl, jpl_get_actual_jvm_opts(-list(atom)), semidet).
type_modes(jpl, jpl_set_default_jvm_opts(+list(atom)), det).
type_modes(jpl, jpl_get_default_jvm_opts(-list(atom)), det).
type_modes(jpl, jpl_set_static_field(+any, +any, +any, +any), unknown).
type_modes(jpl, jpl_set_instance_field(+any, +any, +any, +any), det).
type_modes(jpl, jpl_set_array_1(+any, +any, +any, +any), det).
type_modes(jpl, jpl_set_array(+any, +any, +any, +any, +any), det).
type_modes(jpl, jpl_set_static(+any, +any, +any, +any), det).
type_modes(jpl, jpl_set_instance(+any, +any, +any, +any, +any), det).
type_modes(jpl, jpl_set(+any, +any, +any), det).
type_modes(jpl, jpl_get_primitive_array_elements(+any, +any, +any, +any, -any), det).
type_modes(jpl, jpl_get_object_array_elements(+any, +any, +any, -any), det).
type_modes(jpl, jpl_get_array_elements(+any, +any, +any, +any, -any), unknown).
type_modes(jpl, jpl_get_array_element(+type, +jref, +any, -any), det).
type_modes(jpl, jpl_get_instance(+any, +any, +any, +any, -any), det).
type_modes(jpl, jpl_get_static(+type, +jref, +atom, -datum), det).
type_modes(jpl, jpl_get(+any, +any, -datum), det).
type_modes(jpl, jpl_call_static_method(+any, +any, +any, +any, +any, -any), unknown).
type_modes(jpl, jpl_call_instance_method(+any, +any, +any, +any, +any, -any), unknown).
type_modes(jpl, jpl_call_static(+any, +any, +any, +any, +any, +any, -any), unknown).
type_modes(jpl, jpl_call_instance(+any, +any, +any, +any, +any, +any, -any), unknown).
type_modes(jpl, jpl_call(+any, +atom, +list(datum), -datum), det).
type_modes(jpl, jpl_new_array(+any, +any, -any), det).
type_modes(jpl, jpl_new_1(+any, +any, -any), unknown).
type_modes(jpl, jpl_new(+any, +any, -any), det).
type_modes(date, day_of_the_year(+any, -any), det).
type_modes(date, day_of_the_week(+any, -any), det).
type_modes(date, iso_8601_rest(+int, -any, -any, -any, -any, -any), unknown).
type_modes(date, parse_time(+any, ?(any), -any), semidet).
type_modes(date, parse_time(+any, -any), semidet).
type_modes(date, date_time_value(?(atom), +datime, -any), nondet).
type_modes(arrays, aset(+any, +any, +any, -any), det).
type_modes(arrays, array_to_list(+any, -any), det).
type_modes(arrays, arefl(+any, +any, -any), det).
type_modes(arrays, arefa(+any, +any, -any), det).
type_modes(arrays, aref(+any, +any, -any), semidet).
type_modes(arrays, is_array(@any), semidet).
type_modes(arrays, new_array(-any), det).
type_modes(locked, blocked(+any), semidet).
type_modes(when, check_disj(?(any), ?(any), ?(any)), unknown).
type_modes(when, '$eval_when_condition'(+any, -any), unknown).
type_modes(when, when(+any, :(any)), unknown).
type_modes(occurs, count(:(any), -any), unknown).
type_modes(occurs, sub_var(-any, +any), unknown).
type_modes(occurs, sub_term(-any, +any), unknown).
type_modes(occurs, occurrences_of_var(+any, +any, ?(any)), unknown).
type_modes(occurs, occurrences_of_term(+any, +any, ?(any)), unknown).
type_modes(occurs, free_of_var(+any, +any), unknown).
type_modes(occurs, free_of_term(+any, +any), unknown).
type_modes(occurs, contains_var(+any, +any), det).
type_modes(occurs, contains_term(+any, +any), semidet).
type_modes(tipc_paxos, tipc_initialize, semidet).
type_modes(tipc_paxos, tipc_paxos_on_change(?(any), :(any)), det).
type_modes(tipc_paxos, tipc_paxos_replicate(?(any)), det).
type_modes(tipc_paxos, tipc_paxos_get(?(any)), semidet).
type_modes(tipc_paxos, tipc_paxos_set(?(any), +any), semidet).
type_modes(tipc_paxos, tipc_paxos_set(?(any)), semidet).
type_modes(tipc_broadcast, tipc_initialize, semidet).
type_modes(tipc_broadcast, tipc_host_to_address(?(any), ?(any)), nondet).
type_modes(tipc_broadcast, eventually_implies(:(any), :(any)), semidet).
type_modes(tipc_broadcast, ~>(:(any), :(any)), semidet).
type_modes(pldoc_http, pldoc_search(+any), unknown).
type_modes(pldoc_http, pldoc_object(+any), unknown).
type_modes(pldoc_http, pldoc_man(+any), unknown).
type_modes(pldoc_http, clean_path(+any, -any), unknown).
type_modes(pldoc_http, wiki_file(+any, -any), semidet).
type_modes(pldoc_http, pl_file(+any, -any), semidet).
type_modes(pldoc_http, edit_options(+any, -any), det).
type_modes(pldoc_http, pldoc_doc(+any), unknown).
type_modes(pldoc_http, pldoc_resource(+any), unknown).
type_modes(pldoc_http, allowed_file(+any), semidet).
type_modes(pldoc_http, allowed_directory(+any), semidet).
type_modes(pldoc_http, go_place(+any), unknown).
type_modes(pldoc_http, pldoc_edit(+any), unknown).
type_modes(pldoc_http, pldoc_file(+any), unknown).
type_modes(pldoc_http, pldoc_index(+any), unknown).
type_modes(pldoc_http, pldoc_root(+any), unknown).
type_modes(pldoc_http, prepare_editor, unknown).
type_modes(pldoc_http, doc_browser(+any), semidet).
type_modes(pldoc_http, doc_browser, det).
type_modes(pldoc_http, doc_current_server(-any), det).
type_modes(pldoc_http, doc_server(?(any), +any), det).
type_modes(pldoc_http, doc_server(?(any)), det).
type_modes(bdb, bdb_version(-integer), det).
type_modes(bdb, bdb_transaction(+any, :(any)), semidet).
type_modes(bdb, bdb_transaction(:(any)), semidet).
type_modes(bdb, bdb_closeall, det).
type_modes(bdb, bdb_current(?(any)), nondet).
type_modes(bdb, bdb_getall(+any, +any, -any), semidet).
type_modes(bdb, bdb_enum(+any, -any, -any), unknown).
type_modes(bdb, bdb_get(+any, ?(any), -any), nondet).
type_modes(bdb, bdb_delall(+any, +any, ?(any)), det).
type_modes(bdb, bdb_del(+any, ?(any), ?(any)), nondet).
type_modes(bdb, bdb_put(+any, +any, +any), det).
type_modes(bdb, bdb_close(+any), det).
type_modes(bdb, bdb_open(+any, +any, -any, +any), det).
type_modes(bdb, bdb_environment_property(?(any), ?(any)), nondet).
type_modes(bdb, bdb_current_environment(-any), nondet).
type_modes(bdb, bdb_close_environment(+any), det).
type_modes(bdb, bdb_init(-any, +any), det).
type_modes(bdb, bdb_init(+any), det).
type_modes(solution_sequences, group_by(+any, +any, :(any), -any), nondet).
type_modes(solution_sequences, non_witness_template(+any, +any, -any), det).
type_modes(solution_sequences, join_orders(+any, -any), det).
type_modes(solution_sequences, order_by(?(any), ?(any)), unknown).
type_modes(solution_sequences, offset(+any, :(any)), unknown).
type_modes(solution_sequences, limit(+any, :(any)), unknown).
type_modes(solution_sequences, distinct(?(any), :(any)), unknown).
type_modes(solution_sequences, distinct(:(any)), unknown).
type_modes(prolog_xref, goal_name_arity(+any, -any, -any), unknown).
type_modes(prolog_xref, canonical_source(?(any), ?(any)), det).
type_modes(prolog_xref, xref_source_file(+any, -any, +any, +any), semidet).
type_modes(prolog_xref, xref_source_file(+any, -any, +any), semidet).
type_modes(prolog_xref, generalise(+any, -any), unknown).
type_modes(prolog_xref, pi_to_head(+any, -any), semidet).
type_modes(prolog_xref, process_predicates(:(any), +any, +any), unknown).
type_modes(prolog_xref, assert_module3(+any, +any), unknown).
type_modes(prolog_xref, assert_module(+any, +any), unknown).
type_modes(prolog_xref, assert_op(+any, +any), det).
type_modes(prolog_xref, assert_xmodule_callable(?(any), ?(any), ?(any), ?(any)), unknown).
type_modes(prolog_xref, assert_import(+any, +any, +any, +any, +any), det).
type_modes(prolog_xref, expand_hide_called(:(any)), semidet).
type_modes(prolog_xref, assert_called(+any, +any, +any), det).
type_modes(prolog_xref, process_foreign(+any, +any), unknown).
type_modes(prolog_xref, open_include_file(+any, -any, -any), unknown).
type_modes(prolog_xref, except(+any, +any, -any), unknown).
type_modes(prolog_xref, xref_public_list(+any, -any, -any, -any, -any, -any, +any), semidet).
type_modes(prolog_xref, xref_public_list(+any, -any, -any, -any, -any, +any), semidet).
type_modes(prolog_xref, xref_public_list(+any, -any, -any, +any), semidet).
type_modes(prolog_xref, xref_public_list(+any, +any, +any), semidet).
type_modes(prolog_xref, process_use_module2(+any, +any, +any, +any), det).
type_modes(prolog_xref, process_use_module(+any, +any, +any), det).
type_modes(prolog_xref, partial_evaluate(?(any)), det).
type_modes(prolog_xref, variants(+any, -any), det).
type_modes(prolog_xref, process_dcg_goal(+any, +any, +any), det).
type_modes(prolog_xref, process_body(+any, +any, +any), det).
type_modes(prolog_xref, arith_callable(+any, -any), unknown).
type_modes(prolog_xref, xref_hook(?(any)), unknown).
type_modes(prolog_xref, head_of(+any, -any), unknown).
type_modes(prolog_xref, xref_meta_src(+any, -any, +any), semidet).
type_modes(prolog_xref, xref_meta(+any, -any), semidet).
type_modes(prolog_xref, xref_meta(+any, +any, -any), semidet).
type_modes(prolog_xref, process_meta_predicate(+any, +any), unknown).
type_modes(prolog_xref, xref_option(?(any), ?(any)), nondet).
type_modes(prolog_xref, xref_mode(?(any), ?(any), ?(any)), nondet).
type_modes(prolog_xref, xref_comment(?(any), ?(any), ?(any), ?(any)), nondet).
type_modes(prolog_xref, xref_comment(?(any), ?(any), ?(any)), nondet).
type_modes(prolog_xref, xref_comments(+any, +any, +any), det).
type_modes(prolog_xref, process(+any, +any, +any, +any), det).
type_modes(prolog_xref, current_condition(-any), det).
type_modes(prolog_xref, update_condition(+any), det).
type_modes(prolog_xref, collect(+any, +any, +any, +any), unknown).
type_modes(prolog_xref, xref_op(?(any), ?(any)), nondet).
type_modes(prolog_xref, xref_uses_file(?(any), ?(any), ?(any)), nondet).
type_modes(prolog_xref, xref_module(?(any), ?(any)), nondet).
type_modes(prolog_xref, xref_exported(?(any), ?(any)), nondet).
type_modes(prolog_xref, xref_definition_line(+any, -any), unknown).
type_modes(prolog_xref, xref_defined(?(any), +any, ?(any)), nondet).
type_modes(prolog_xref, xref_called(?(any), ?(any), ?(any), ?(any)), nondet).
type_modes(prolog_xref, xref_called(?(any), ?(any), ?(any)), nondet).
type_modes(prolog_xref, xref_done(+any, -any), det).
type_modes(prolog_xref, xref_current_source(?(any)), unknown).
type_modes(prolog_xref, xref_clean(+any), det).
type_modes(prolog_xref, xref_push_op(?(any), +any, +any, :(any)), unknown).
type_modes(prolog_xref, xref_input_stream(-any), det).
type_modes(prolog_xref, set_initial_mode(+any, +any), det).
type_modes(prolog_xref, xref_cleanup(+any), det).
type_modes(prolog_xref, xref_source(+any, +any), det).
type_modes(prolog_xref, xref_source(+any), det).
type_modes(prolog_xref, built_in_predicate(+any), unknown).
type_modes(prolog_xref, hide_called(:(any), +any), semidet).
type_modes(prolog_xref, prolog:hook(?(any)), unknown).
type_modes(prolog_xref, prolog:meta_goal(+any, -any), unknown).
type_modes(prolog_xref, prolog:called_by(+any, -any), unknown).
type_modes(prolog_xref, prolog:called_by(+any, +any, +any, -any), semidet).
type_modes(json, atom_json_dict(-any, +any, +any), det).
type_modes(json, atom_json_dict(+any, -any, +any), det).
type_modes(json, json_write_dict(+any, +any, +any), det).
type_modes(json, json_write_dict(+any, +any), det).
type_modes(json, json_read_dict(+any, -any, +any), det).
type_modes(json, json_read_dict(+any, -any), det).
type_modes(json, is_json_term(@any, +any), semidet).
type_modes(json, is_json_term(@any), semidet).
type_modes(json, json_print_length(+any, +any, +any, +any, +any), semidet).
type_modes(json, json_write(+any, +any, +any), det).
type_modes(json, json_write(+any, +any), det).
type_modes(json, json_write_indent(+any, +any, +any), det).
type_modes(json, json_write_string(+any, +any), det).
type_modes(json, ws(+any, +any, -any), unknown).
type_modes(json, ws(+any, -any), det).
type_modes(json, json_read(+any, -any, +any), det).
type_modes(json, json_read(+any, -any), det).
type_modes(json, atom_json_term(-any, +any, +any), det).
type_modes(json, atom_json_term(+any, -any, +any), det).
type_modes(cgi, cgi_get_form(-any), unknown).
type_modes(http_unix_daemon, http_server_hook(+any), semidet).
type_modes(http_unix_daemon, wait(+any), unknown).
type_modes(http_unix_daemon, setup_signals(+any), unknown).
type_modes(http_unix_daemon, kill_x11(+any), det).
type_modes(http_unix_daemon, setup_debug(+any), det).
type_modes(http_unix_daemon, switch_user(+any), det).
type_modes(http_unix_daemon, write_pid(+any), det).
type_modes(http_unix_daemon, setup_output(+any), det).
type_modes(http_unix_daemon, setup_syslog(+any), det).
type_modes(http_unix_daemon, enable_development_system, unknown).
type_modes(http_unix_daemon, disable_development_system, unknown).
type_modes(http_unix_daemon, start_server(+any), det).
type_modes(http_unix_daemon, ssl_passwd(+any, -any), det).
type_modes(http_unix_daemon, store_password(+any), det).
type_modes(http_unix_daemon, http_daemon(+any), unknown).
type_modes(http_unix_daemon, argv_options(+any, -any, -any), det).
type_modes(http_unix_daemon, http_daemon, unknown).
type_modes(prolog_stack, prolog_stack:stack_guard(+any), semidet).
type_modes(prolog_stack, lineno(+any, +any, -any), unknown).
type_modes(prolog_stack, backtrace(+any), unknown).
type_modes(prolog_stack, clause_predicate_name(+any, -any), det).
type_modes(prolog_stack, print_prolog_backtrace(+any, +any, +any), det).
type_modes(prolog_stack, print_prolog_backtrace(+any, +any), det).
type_modes(prolog_stack, prolog_stack_frame_property(+any, ?(any)), nondet).
type_modes(prolog_stack, copy_goal(+any, +any, -any), det).
type_modes(prolog_stack, get_prolog_backtrace(+any, -any, +any), det).
type_modes(prolog_stack, get_prolog_backtrace(+any, -any), det).
type_modes(rdf_cache, replace(+any, ?(list), ?(list)), unknown).
type_modes(rdf_cache, url_to_file(+any, -any), unknown).
type_modes(rdf_cache, url_cache_file(+any, +any, +any, +any, -any), semidet).
type_modes(rdf_cache, local_cache_file(+any, -any), det).
type_modes(rdf_cache, rdf_cache_file(+any, +any, -any), semidet).
type_modes(rdf_cache, rdf_set_cache_options(+any), unknown).
type_modes(http_multipart_plugin, part_value(+any, +any, +any, +any, -any, +any), unknown).
type_modes(http_multipart_plugin, part_header(+any, -any, -any, -any), det).
type_modes(http_multipart_plugin, multipart_type(+any, -any), semidet).
type_modes(http_multipart_plugin, http_client:http_convert_data(+any, +any, -any, +any), semidet).
type_modes(prolog_listing, inc_indent(+any, +any, -any), unknown).
type_modes(prolog_listing, nlindent(+any, +any), unknown).
type_modes(prolog_listing, listing_write_options(+any, -any), det).
type_modes(prolog_listing, pprint(+any, +any, +any, +any), unknown).
type_modes(prolog_listing, portray_list(+any, +any, +any), unknown).
type_modes(prolog_listing, portray_meta(+any, +any, +any, +any), unknown).
type_modes(prolog_listing, or_layout(@any), semidet).
type_modes(prolog_listing, infix_op(+any, -any, -any), semidet).
type_modes(prolog_listing, portray_or(+any, +any, +any, +any), det).
type_modes(prolog_listing, portray_body(+any, +any, +any, +any, +any, +any), unknown).
type_modes(prolog_listing, portray_clause(+stream, +any, +any), det).
type_modes(prolog_listing, portray_clause(+stream, +any), det).
type_modes(prolog_listing, portray_clause(+any), det).
type_modes(prolog_listing, meta_implies_transparent(+any), semidet).
type_modes(prolog_listing, list_predicates(:(list(pi)), :(any)), det).
type_modes(prolog_listing, listing(:(any)), unknown).
type_modes(prolog_listing, listing, unknown).
type_modes(pengines_io, pengine_bind_io_to_html(+any), unknown).
type_modes(pengines_io, pengine_bind_user_streams, unknown).
type_modes(pengines_io, pengine_io_predicate(?(any)), unknown).
type_modes(pengines_io, map_output(+any, +any, -any), det).
type_modes(pengines_io, subst_to_html(+any, +any, -any), det).
type_modes(pengines_io, binding_term(+any, +any, +any, ?(list), ?(list)), semidet).
type_modes(pengines_io, term_html_string(+any, +any, +any, -any), det).
type_modes(pengines_io, binding_to_html(+any, +any, -any), det).
type_modes(pengines_io, answer_to_json_strings(+any, +any, -any), unknown).
type_modes(pengines_io, pengines:event_to_json(+any, -any, +any, +any), unknown).
type_modes(pengines_io, pengines:event_to_json(+any, -any, +any, +any), semidet).
type_modes(pengines_io, pengine_module(-any), det).
type_modes(pengines_io, send_html(+any), det).
type_modes(pengines_io, user:message_hook(+any, +any, +any), semidet).
type_modes(pengines_io, pengine_format(+any, +any), det).
type_modes(pengines_io, pengine_format(+any), det).
type_modes(pengines_io, pengine_write_canonical(+any), det).
type_modes(pengines_io, pengine_print(+any), det).
type_modes(pengines_io, pengine_display(+any), det).
type_modes(pengines_io, pengine_writeq(+any), det).
type_modes(pengines_io, pengine_write(+any), det).
type_modes(pengines_io, pengine_write_term(+any, +any), unknown).
type_modes(pengines_io, pengine_flush_output, unknown).
type_modes(pengines_io, pengine_nl, unknown).
type_modes(pengines_io, pengine_writeln(+any), unknown).
type_modes(tipc_linda, tipc_initialize, semidet).
type_modes(tipc_linda, tipc_linda_server, nondet).
type_modes(tipc_linda, tuple(?(any), :(any)), det).
type_modes(tipc_linda, tuple(:(any)), det).
type_modes(tipc_linda, linda_eval_detached(?(any), :(any)), det).
type_modes(tipc_linda, linda_eval_detached(:(any)), det).
type_modes(tipc_linda, linda_eval(?(any), :(any)), det).
type_modes(tipc_linda, linda_eval(:(any)), det).
type_modes(tipc_linda, bagof_rd_noblock(?(any), ?(any), -any), nondet).
type_modes(tipc_linda, bagof_in_noblock(?(any), ?(any), -any), nondet).
type_modes(tipc_linda, rd(?(any), -any), nondet).
type_modes(tipc_linda, rd_noblock(?(any)), nondet).
type_modes(tipc_linda, rd(?(any)), nondet).
type_modes(tipc_linda, in(+any, -any), det).
type_modes(tipc_linda, in_noblock(?(any)), semidet).
type_modes(tipc_linda, in(?(any)), det).
type_modes(tipc_linda, out(+any), det).
type_modes(tipc_linda, linda_timeout(+any), semidet).
type_modes(tipc_linda, linda_timeout(?(any), ?(any)), semidet).
type_modes(tipc_linda, close_client, det).
type_modes(tipc_linda, linda_client(+any), semidet).
type_modes(tipc_linda, linda(:(any)), det).
type_modes(tipc_linda, linda, det).
type_modes(shell, table(+any, +any, ?(list), ?(list)), unknown).
type_modes(shell, warning(+any, +list), det).
type_modes(shell, name_to_file(+any, -any), unknown).
type_modes(shell, rm(+any), det).
type_modes(shell, mv(+any, +any), det).
type_modes(shell, ls(+any), unknown).
type_modes(shell, ls, unknown).
type_modes(shell, pwd, unknown).
type_modes(shell, dirs, unknown).
type_modes(shell, popd, unknown).
type_modes(shell, pushd(+any), unknown).
type_modes(shell, pushd, unknown).
type_modes(shell, cd(?(any)), unknown).
type_modes(shell, cd, unknown).
type_modes(files, chdir(+any), det).
type_modes(files, can_open_file(+any, +any), unknown).
type_modes(http_header, http:status_page(+any, +any, -any), semidet).
type_modes(http_header, http:http_address(?(list), ?(list)), det).
type_modes(http_header, address(?(list), ?(list)), unknown).
type_modes(http_header, http_parse_header(+codes, -list), det).
type_modes(http_header, http_read_header(+any, -any), det).
type_modes(http_header, reply(+any, -list, ?(list), ?(list)), semidet).
type_modes(http_header, range(-any, ?(list), ?(list)), semidet).
type_modes(http_header, cookie_option(-any, ?(list), ?(list)), semidet).
type_modes(http_header, cookies(-any, ?(list), ?(list)), semidet).
type_modes(http_header, request_header(+stream, -list), det).
type_modes(http_header, request_uri_parts(+any, -any, ?(any)), det).
type_modes(http_header, http_timestamp(+timestamp, -atom), det).
type_modes(http_header, rfc_date(+any, ?(list), ?(list)), det).
type_modes(http_header, now(?(list), ?(list)), unknown).
type_modes(http_header, separators(-any), det).
type_modes(http_header, field_name(?(any)), unknown).
type_modes(http_header, header_fields(+any, ?(any), ?(list), ?(list)), det).
type_modes(http_header, quoted_string(-any, ?(list), ?(list)), semidet).
type_modes(http_header, token(-any, ?(list), ?(list)), semidet).
type_modes(http_header, value_parameters(-list), det).
type_modes(http_header, rank_specialised(+any, +any, -any), det).
type_modes(http_header, parse_content_type(-any, ?(list), ?(list)), unknown).
type_modes(http_header, content_disposition(-any, ?(list), ?(list)), unknown).
type_modes(http_header, accept(-any, ?(list), ?(list)), semidet).
type_modes(http_header, value_options(+any, +any, ?(list), ?(list)), unknown).
type_modes(http_header, parse_header_value(+any, +any, -any), semidet).
type_modes(http_header, field_to_prolog(+any, +any, -any), semidet).
type_modes(http_header, known_field(?(any), ?(any)), unknown).
type_modes(http_header, http_parse_header_value(+any, +any, -any), semidet).
type_modes(http_header, read_field_value(-any, ?(list), ?(list)), unknown).
type_modes(http_header, header_field(+any, +any), det).
type_modes(http_header, header_field(-any, -any, ?(list), ?(list)), det).
type_modes(http_header, content_range(+atom, +int, +int, +int, ?(list), ?(list)), det).
type_modes(http_header, content_length(+any, ?(any), ?(list), ?(list)), det).
type_modes(http_header, status_comment(+atom, ?(list), ?(list)), det).
type_modes(http_header, status_number(-atom, +nonneg), det).
type_modes(http_header, status_number(+atom, -nonneg), det).
type_modes(http_header, status_number(?(any), ?(any), ?(list), ?(list)), semidet).
type_modes(http_header, vstatus(+any, -any, +any, ?(list), ?(list)), det).
type_modes(http_header, vstatus(+any, -any, ?(list), ?(list)), det).
type_modes(http_header, reply_header(+any, +any, -any, ?(list), ?(list)), det).
type_modes(http_header, http_reply_header(+stream, +any, +any), det).
type_modes(http_header, post_header(+any, +any, ?(list), ?(list)), unknown).
type_modes(http_header, http_post_data(+any, +stream, +any), det).
type_modes(http_header, content_length_in_encoding(+any, +any, -any), unknown).
type_modes(http_header, transfer(+any, -any), unknown).
type_modes(http_header, http_update_transfer(+any, +any, -any, -any), unknown).
type_modes(http_header, connection(+any, -any), unknown).
type_modes(http_header, http_update_connection(+any, +any, -any, -any), unknown).
type_modes(http_header, mime_type_encoding(+any, -any), semidet).
type_modes(http_header, http_update_encoding(+any, -any, -any), unknown).
type_modes(http_header, http_join_headers(+any, +any, -any), unknown).
type_modes(http_header, status_page_hook(+any, +any, +any, -any), det).
type_modes(http_header, http_status_reply(+any, +any, +any, +any, -any), det).
type_modes(http_header, http_status_reply(+any, +any, +any, -any), det).
type_modes(http_header, http_reply_data(+any, +any, +any, -any), semidet).
type_modes(http_header, http_reply(+any, +stream, +any, +any, -any), det).
type_modes(http_header, http_reply(+any, +stream, +any, -any), det).
type_modes(http_header, http_reply(+any, +stream, +any), det).
type_modes(http_header, http_reply(+any, +stream), det).
type_modes(http_header, http_read_reply_header(+any, -any), unknown).
type_modes(http_header, http_read_request(+stream, -any), det).
type_modes(predicate_options, resolve_module(:(any), -any), det).
type_modes(predicate_options, option_decl(:(any), +any), det).
type_modes(predicate_options, annotate(+any, +any), det).
type_modes(predicate_options, check_options(:(any), +any, +any, +any, +any), unknown).
type_modes(predicate_options, check_called_by(+any, +any, +any), det).
type_modes(predicate_options, check_body(+any, +any, +any, +any), unknown).
type_modes(predicate_options, check_clause(+any, +any, +any, +any), det).
type_modes(predicate_options, check_predicate_options(:(any)), det).
type_modes(predicate_options, derive_predicate_options, det).
type_modes(predicate_options, check_predicate_options, det).
type_modes(predicate_options, retractall_predicate_options, det).
type_modes(predicate_options, derived_predicate_options(+any), det).
type_modes(predicate_options, expand_pass_to_options(+any, +any, -any, ?(list), ?(list)), det).
type_modes(predicate_options, derived_predicate_options(:(any), ?(any), ?(any)), nondet).
type_modes(predicate_options, current_predicate_options(:(any), ?(any), ?(any)), nondet).
type_modes(predicate_options, check_predicate_option(:(any), +any, +any), det).
type_modes(predicate_options, current_predicate_option(:(any), ?(any), ?(any)), nondet).
type_modes(predicate_options, current_option_arg(:(any), ?(any)), nondet).
type_modes(predicate_options, assert_predicate_options(:(any), +any, +any, ?(any)), semidet).
type_modes(predicate_options, predicate_options(:(any), +any, +any), det).
type_modes(sql_tokenizer, char_in(+any, +any), semidet).
type_modes(apply_macros, contains_illegal_dcgnt(+any), semidet).
type_modes(apply_macros, qcall_instantiated(@any), semidet).
type_modes(apply_macros, expand_phrase(+any, +any, -any, -any), semidet).
type_modes(apply_macros, expand_phrase(+any, -any), semidet).
type_modes(apply_macros, expand_apply(+callable, -any, +any, -any), semidet).
type_modes(apply_macros, expand_apply(+callable, -any), semidet).
type_modes(apply_macros, expand_maplist(+any, +any, -any), det).
type_modes(csv, csv_write_stream(+any, +any, +any), det).
type_modes(csv, csv_write_file(+any, +any, +any), det).
type_modes(csv, csv_write_file(+any, +any), det).
type_modes(csv, csv_read_file_row(+any, -any, +any), nondet).
type_modes(csv, string_codes(-any), unknown).
type_modes(csv, csv(?(any), +any, ?(list), ?(list)), det).
type_modes(csv, csv(?(any), ?(list), ?(list)), det).
type_modes(csv, csv_read_file(+any, -any, +any), det).
type_modes(csv, csv_read_file(+any, -any), det).
type_modes(heaps, merge_heaps(+any, +any, -any), det).
type_modes(heaps, min_of_heap(+any, ?(any), ?(any), ?(any), ?(any)), semidet).
type_modes(heaps, min_of_heap(+any, ?(any), ?(any)), semidet).
type_modes(heaps, list_to_heap(+list, -any), det).
type_modes(heaps, is_heap(+any), semidet).
type_modes(heaps, heap_to_list(+any, -list), det).
type_modes(heaps, heap_size(+any, -int), det).
type_modes(heaps, get_from_heap(?(any), ?(any), ?(any), -any), semidet).
type_modes(heaps, singleton_heap(?(any), ?(any), ?(any)), semidet).
type_modes(heaps, empty_heap(?(any)), semidet).
type_modes(heaps, delete_from_heap(+any, -any, +any, -any), semidet).
type_modes(heaps, add_to_heap(+any, +any, ?(any), -any), semidet).
type_modes(start_emacs, emacs_toplevel, det).
type_modes(start_emacs, emacs(+any), det).
type_modes(start_emacs, emacs, det).
type_modes(start_emacs, emacs_server, det).
type_modes(start_emacs, register_emacs, det).
type_modes(start_emacs, start_emacs, det).
type_modes(writef, swritef(-any, +any, +any), det).
type_modes(writef, swritef(-any, +any), det).
type_modes(writef, writef(+any, +any), det).
type_modes(writef, writef(+any), det).
type_modes(yadis, xrds_specified_location(+any, -any), nondet).
type_modes(yadis, html_head_dom(+any, -any), semidet).
type_modes(yadis, ssl_verify(+any, +any, +any, +any, +any), unknown).
type_modes(yadis, xrds_load(+any, -any), det).
type_modes(yadis, xrds_location(+any, -any), semidet).
type_modes(yadis, xid_normalize(+any, -any), det).
type_modes(yadis, xrds_dom(+any, -any), det).
type_modes(win_menu, go_home_on_plain_app_start, det).
type_modes(win_menu, prolog:file_open_event(+any), unknown).
type_modes(win_menu, init_win_app, unknown).
type_modes(online_help, line_index, unknown).
type_modes(online_help, line_start(?(any), ?(any)), det).
type_modes(online_help, copy_chars(+any, +any, +any), unknown).
type_modes(online_help, show_help(+any), unknown).
type_modes(online_help, apropos(?(any)), unknown).
type_modes(online_help, help(+any), unknown).
type_modes(prolog_breakpoints, break_location(+any, +any, -any, -any), det).
type_modes(prolog_breakpoints, file_line(+any, +any, -any), det).
type_modes(prolog_breakpoints, breakpoint_property(?(any), ?(any)), nondet).
type_modes(prolog_breakpoints, delete_breakpoint(+any), det).
type_modes(prolog_breakpoints, set_breakpoint(+any, +any, +any, +any, -any), det).
type_modes(prolog_breakpoints, set_breakpoint(+any, +any, +any, -any), det).
type_modes(sicstus, prolog:'$breaklevel'(-any, ?(any)), unknown).
type_modes(sicstus, prolog_flag(+any, -any), semidet).
type_modes(sicstus, prolog_flag(+any, -any, +any), semidet).
type_modes(sicstus, trimcore, unknown).
type_modes(sicstus, read_line(+any, -any), det).
type_modes(sicstus, read_line(-any), det).
type_modes(sicstus, update_mutable(?(any), !(any)), det).
type_modes(sicstus, get_mutable(?(any), +any), semidet).
type_modes(sicstus, create_mutable(?(any), -any), det).
type_modes(sicstus, bb_update(:(any), -any, +any), semidet).
type_modes(sicstus, bb_delete(:(any), -any), semidet).
type_modes(sicstus, bb_get(:(any), -any), semidet).
type_modes(sicstus, bb_put(:(any), +any), det).
type_modes(sicstus, use_module(-any, +any, +any), det).
type_modes(sicstus, use_module(+any, -any, +any), det).
type_modes(sicstus, rename_module(?(any), ?(any)), nondet).
type_modes(sicstus, if(:(any), :(any), :(any)), unknown).
type_modes(sicstus, setup_dialect, unknown).
type_modes(sicstus, push_sicstus_library, unknown).
type_modes(stream_info, stream_info(+any), det).
type_modes(sicstus_lists, sublist(?(any), +any), unknown).
type_modes(sicstus_lists, nth(?(any), ?(any), ?(any)), nondet).
type_modes(sicstus_lists, substitute(+any, +any, +any, -any), det).
type_modes(protobufs, protobuf_message(?(any), ?(any), ?(any)), nondet).
type_modes(protobufs, protobuf_message(?(any), ?(any)), semidet).
type_modes(nb_rbtrees, nb_rb_set_node_value(!(any), +any), det).
type_modes(nb_rbtrees, nb_rb_node_value(+any, -any), det).
type_modes(nb_rbtrees, nb_rb_get_node(+any, +any, -any), semidet).
type_modes(nb_rbtrees, nb_rb_insert(!(any), +any, +any), unknown).
type_modes(clpb, weighted_maximum(+any, +any, -any), multi).
type_modes(clpb, random_labeling(+any, +any), det).
type_modes(clpb, sat_count(+any, -any), det).
type_modes(clpb, labeling(+any), multi).
type_modes(clpb, taut(+any, -any), semidet).
type_modes(clpb, sat(+any), semidet).
type_modes(url, file_name_to_url(-any, +any), semidet).
type_modes(url, file_name_to_url(+any, -any), det).
type_modes(url, parse_url_search(?(any), ?(list(_=_))), det).
type_modes(url, url_iri(-any, +any), det).
type_modes(url, url_iri(+any, -any), det).
type_modes(url, set_url_encoding(?(any), +any), semidet).
type_modes(url, www_decode(-any, ?(list), ?(list)), unknown).
type_modes(url, www_encode(+any, +any, ?(list), ?(list)), unknown).
type_modes(url, www_form_encode(-any, +any), det).
type_modes(url, www_form_encode(+any, -any), det).
type_modes(url, unreserved(+any), unknown).
type_modes(url, sub_delim(?(any)), unknown).
type_modes(url, lwalpha(-any, ?(list), ?(list)), unknown).
type_modes(url, pchar(-any, ?(list), ?(list)), unknown).
type_modes(url, fragment_char(-any), unknown).
type_modes(url, fragment(-any, ?(any), ?(list), ?(list)), unknown).
type_modes(url, search_sep(?(list), ?(list)), semidet).
type_modes(url, query(-any, ?(any), ?(list), ?(list)), det).
type_modes(url, hier_part(+any, -any, ?(any), ?(list), ?(list)), unknown).
type_modes(url, schema(-any, ?(list), ?(list)), unknown).
type_modes(url, uri(-any, ?(list), ?(list)), unknown).
type_modes(url, absolute_url(?(list), ?(list)), unknown).
type_modes(url, globalise_path(+any, +any, -any), det).
type_modes(url, parse_url(+any, +any, -any), det).
type_modes(url, parse_url(?(any), ?(any)), det).
type_modes(url, cfragment(+any, ?(list), ?(list)), unknown).
type_modes(url, cvalue(+any, ?(list), ?(list)), det).
type_modes(url, csearch(+any, ?(list), ?(list)), unknown).
type_modes(url, http_location(?(any), ?(any)), unknown).
type_modes(url, is_absolute_url(+any), unknown).
type_modes(url, global_url(+any, +any, -any), det).
type_modes(plunit, user:message_hook(+any, +any, +any), semidet).
type_modes(plunit, info(+any), unknown).
type_modes(plunit, load_test_files(+any), det).
type_modes(plunit, unit_file(-any, +any), nondet).
type_modes(plunit, unit_file(+any, -any), det).
type_modes(plunit, current_test_set(?(any)), nondet).
type_modes(plunit, test_report(?(any)), det).
type_modes(plunit, report, det).
type_modes(plunit, check_for_test_errors, semidet).
type_modes(plunit, running_tests, det).
type_modes(plunit, end_test(?(any), ?(any), ?(any), ?(any)), det).
type_modes(plunit, begin_test(?(any), ?(any), ?(any), ?(any)), det).
type_modes(plunit, assert_cyclic(+any), det).
type_modes(plunit, cleanup(+any, +any), det).
type_modes(plunit, call_ex(+any, +any), unknown).
type_modes(plunit, setup(+any, +any, +any), semidet).
type_modes(plunit, match_error(+any, +any), semidet).
type_modes(plunit, call_det(:(any), -any), nondet).
type_modes(plunit, cmp(+any, -any, -any, -any), det).
type_modes(plunit, nondet_compare(+any, +any, +any, +any, +any), semidet).
type_modes(plunit, result_vars(+any, -any), det).
type_modes(plunit, non_det_test(+any, +any, +any, +any, +any, +any, -any), unknown).
type_modes(plunit, run_test_6(+any, +any, +any, +any, :(any), -any), det).
type_modes(plunit, test_caps(-any, +any, +any, +any, +any, +any, -any, -any), nondet).
type_modes(plunit, run_test(+any, +any, +any, +any, +any), det).
type_modes(plunit, make_run_tests(+any), unknown).
type_modes(plunit, run_tests_in_files(+list), det).
type_modes(plunit, run_tests(+any), semidet).
type_modes(plunit, run_tests, semidet).
type_modes(plunit, test_option(+any), semidet).
type_modes(plunit, valid_options(+any, :(any)), det).
type_modes(plunit, expand(+any, -any), semidet).
type_modes(plunit, expand_test(+any, +any, +any, -any), det).
type_modes(plunit, unit_module(+any, -any), det).
type_modes(plunit, make_unit_module(+any, -any), det).
type_modes(plunit, end_tests(+any), det).
type_modes(plunit, begin_tests(+atom, ?(any)), det).
type_modes(plunit, begin_tests(+atom), det).
type_modes(plunit, loading_tests, unknown).
type_modes(plunit, set_test_options(+any), unknown).
type_modes(plunit, set_test_flag(+any, +any), det).
type_modes(plunit, current_test_flag(?(any), ?(any)), nondet).
type_modes(user, blocked(+any), semidet).
type_modes(rdf_ntriples, rdf_db:rdf_file_type(+any, -any), unknown).
type_modes(rdf_ntriples, rdf_db:rdf_load_stream(+any, +any, :(any)), semidet).
type_modes(rdf_ntriples, init_state(+any, +any, -any), det).
type_modes(rdf_ntriples, open_input(+any, -any, -any), det).
type_modes(rdf_ntriples, read_ntuple(+any, -any, +any, -any), det).
type_modes(rdf_ntriples, process_ntriple(+any, :(any), +any, -any), unknown).
type_modes(rdf_ntriples, read_ntuples(+any, -any, +any, -any), unknown).
type_modes(rdf_ntriples, rdf_process_ntriples(+any, :(any), +any), unknown).
type_modes(rdf_ntriples, rdf_read_nquads(+any, -any, +any), det).
type_modes(rdf_ntriples, rdf_read_ntriples(+any, -any, +any), det).
type_modes(rdf_ntriples, read_ntuple(+any, -any), det).
type_modes(rdf_ntriples, read_nquad(+any, -any), det).
type_modes(rdf_ntriples, read_ntriple(+any, -any), det).
type_modes(edinburgh, fileerrors(-any, +any), det).
type_modes(edinburgh, nodebug, det).
type_modes(edinburgh, debug, det).
type_modes(edinburgh, reconsult(+any), det).
type_modes(edinburgh, unknown(-any, +any), det).
type_modes(edinburgh, display(+any, +any), det).
type_modes(edinburgh, display(+any), det).
type_modes(rdf_litindex, rdf_literal_index(+any, -any), det).
type_modes(rdf_litindex, stem_index(-any), det).
type_modes(rdf_litindex, text_of(+any, -any, -any), semidet).
type_modes(rdf_litindex, default_stopgap(?(any)), unknown).
type_modes(rdf_litindex, rdf_stopgap_token(-any), nondet).
type_modes(rdf_litindex, rdf_tokenize_literal(+any, -any), semidet).
type_modes(rdf_litindex, unregister_literal(+any), unknown).
type_modes(rdf_litindex, register_literal(+any), unknown).
type_modes(rdf_litindex, check_index_workers(+any, +any), unknown).
type_modes(rdf_litindex, create_update_literal_thread(+any), unknown).
type_modes(rdf_litindex, rdf_delete_literal_index(+any), unknown).
type_modes(rdf_litindex, clean_token_index, unknown).
type_modes(rdf_litindex, make_literal_index, unknown).
type_modes(rdf_litindex, token_index(-any), unknown).
type_modes(rdf_litindex, dnf(+any, -any), unknown).
type_modes(rdf_litindex, nnf(+any, -any), unknown).
type_modes(rdf_litindex, compile_spec(+any, -any), unknown).
type_modes(rdf_litindex, rdf_token_expansions(+any, -any), unknown).
type_modes(rdf_litindex, rdf_find_literals(+any, -any), det).
type_modes(rdf_litindex, rdf_find_literal(+any, -any), nondet).
type_modes(rdf_litindex, rdf_set_literal_index_option(+list), unknown).
type_modes(yap, yap_style_check(+any), det).
type_modes(yap, yap_flag(+any, +any), det).
type_modes(yap, source, det).
type_modes(yap, assert_static(:(any)), unknown).
type_modes(yap, exists(+any), unknown).
type_modes(yap, system(+any), unknown).
type_modes(yap, depth_bound_call(:(any), :(any)), unknown).
type_modes(yap, gc, unknown).
type_modes(yap, push_yap_file_extension, unknown).
type_modes(yap, push_yap_library, unknown).
type_modes(yap, yap_expansion(+any, +any), unknown).
type_modes(cql_stale, blocked(+any), semidet).
type_modes(chr_runtime, 'chr module'(?(any)), unknown).
type_modes(chr_runtime, current_chr_constraint(:(any)), nondet).
type_modes(chr_runtime, find_chr_constraint(-any), nondet).
type_modes(clpfd, fd_dom(+any, -any), unknown).
type_modes(clpfd, fd_size(+any, -any), unknown).
type_modes(clpfd, fd_sup(+any, -any), unknown).
type_modes(clpfd, fd_inf(+any, -any), unknown).
type_modes(clpfd, fd_var(+any), unknown).
type_modes(clpfd, chain(+any, +any), unknown).
type_modes(clpfd, zcompare(?(any), ?(any), ?(any)), unknown).
type_modes(clpfd, transpose(+any, ?(any)), unknown).
type_modes(clpfd, automaton(+any, ?(any), +any, +any, +any, +any, +any, ?(any)), unknown).
type_modes(clpfd, automaton(+any, +any, +any), unknown).
type_modes(clpfd, disjoint2(+any), unknown).
type_modes(clpfd, cumulative(+any, +any), unknown).
type_modes(clpfd, cumulative(+any), unknown).
type_modes(clpfd, circuit(+any), unknown).
type_modes(clpfd, global_cardinality(+any, +any, +any), unknown).
type_modes(clpfd, global_cardinality(+any, +any), unknown).
type_modes(clpfd, element(?(any), +any, ?(any)), unknown).
type_modes(clpfd, serialized(+any, +any), unknown).
type_modes(clpfd, tuples_in(+any, +any), unknown).
type_modes(clpfd, lex_chain(+any), unknown).
type_modes(clpfd, #\(?(any), ?(any)), unknown).
type_modes(clpfd, #\/(?(any), ?(any)), unknown).
type_modes(clpfd, #/\(?(any), ?(any)), unknown).
type_modes(clpfd, #<==(?(any), ?(any)), unknown).
type_modes(clpfd, #==>(?(any), ?(any)), unknown).
type_modes(clpfd, #<==>(?(any), ?(any)), unknown).
type_modes(clpfd, #\(+any), unknown).
type_modes(clpfd, #<(?(any), ?(any)), unknown).
type_modes(clpfd, #>(?(any), ?(any)), unknown).
type_modes(clpfd, #\=(?(any), ?(any)), unknown).
type_modes(clpfd, #=(?(any), ?(any)), unknown).
type_modes(clpfd, #=<(?(any), ?(any)), unknown).
type_modes(clpfd, #>=(?(any), ?(any)), unknown).
type_modes(clpfd, scalar_product(+any, +any, +any, ?(any)), unknown).
type_modes(clpfd, sum(+any, +any, ?(any)), unknown).
type_modes(clpfd, all_distinct(+any), unknown).
type_modes(clpfd, all_different(+any), unknown).
type_modes(clpfd, labeling(+any, +any), unknown).
type_modes(clpfd, label(+any), unknown).
type_modes(clpfd, indomain(?(any)), unknown).
type_modes(clpfd, ins(+any, +any), unknown).
type_modes(clpfd, in(?(any), +any), unknown).
type_modes(hprolog, time(:(any), -any, -any), unknown).
type_modes(hprolog, chr_delete(+any, +any, -any), det).
type_modes(hprolog, bounded_sublist(?(any), +any, +integer), unknown).
type_modes(hprolog, sublist(?(any), +any), nondet).
type_modes(hprolog, or_list(+any, -any), unknown).
type_modes(hprolog, max_go_list(+any, -any), unknown).
type_modes(hprolog, split_at(+any, +any, +any, -any), semidet).
type_modes(hprolog, drop(+any, +any, -any), semidet).
type_modes(hprolog, take(+any, +any, -any), unknown).
type_modes(hprolog, intersect_eq(+any, +any, -any), unknown).
type_modes(hprolog, list_difference_eq(+any, -any, -any), unknown).
type_modes(hprolog, memberchk_eq(+any, +any), unknown).
type_modes(hprolog, substitute_eq(+any, +any, +any, -any), unknown).
type_modes(hprolog, push_hprolog_library, unknown).
type_modes(mimetype, name_mimetype(+any, -any), semidet).
type_modes(mimetype, ext_mimetype(+any, -any), semidet).
type_modes(mimetype, default_mimetype(-any), semidet).
type_modes(mimetype, mime:mime_extension(+any, -any), semidet).
type_modes(mimetype, file_mime_type(+any, -any), semidet).
type_modes(zlib, http:encoding_filter(+any, +any, -any), semidet).
type_modes(zlib, gzopen(+any, +any, -any, +any), det).
type_modes(zlib, gzopen(+any, +any, -any), det).
type_modes(pdt_console, pdt_install_console, unknown).
type_modes(thread_pool, create_pool(+any), semidet).
type_modes(thread_pool, worker_exitted(+any, +any, :(any)), unknown).
type_modes(thread_pool, update_pool(+any, +any, -any), det).
type_modes(thread_pool, manage_thread_pool(+any), unknown).
type_modes(thread_pool, pool_manager(-any), det).
type_modes(thread_pool, create_pool_lazily(+any), semidet).
type_modes(thread_pool, thread_create_in_pool(+any, :(any), -any, +any), det).
type_modes(thread_pool, thread_pool_property(?(any), ?(any)), nondet).
type_modes(thread_pool, current_thread_pool(?(any)), nondet).
type_modes(thread_pool, thread_pool_destroy(+any), det).
type_modes(thread_pool, thread_pool_create(+any, +any, +any), det).
type_modes(xpath, text_of_dom(+any, -atom), det).
type_modes(xpath, process_equality(+any, +any), semidet).
type_modes(xpath, modifiers(+any, +any, +any, +any, -any), unknown).
type_modes(xpath, nth_element(?(any), +any, -any, +list), nondet).
type_modes(xpath, count_named_elements(+any, +any, -any), det).
type_modes(xpath, sub_dom(-any, -any, +any, -any, +any), nondet).
type_modes(xpath, xpath(+any, +any, ?(any)), nondet).
type_modes(xpath, xpath_chk(+any, +any, ?(any)), semidet).
type_modes(gensym, reset_gensym(+any), unknown).
type_modes(gensym, reset_gensym, unknown).
type_modes(gensym, gensym(+any, -any), unknown).
type_modes(rdfa, rdf_db:rdf_load_stream(+any, +any, :(any)), unknown).
type_modes(rdfa, apply_patterns(+any, -any), det).
type_modes(rdfa, add_triple(+any, +any, +any, +any), det).
type_modes(rdfa, date_time_type(+any, -any), semidet).
type_modes(rdfa, term(-any, ?(list), ?(list)), unknown).
type_modes(rdfa, term_iri(?(any), ?(any), ?(any)), unknown).
type_modes(rdfa, term_or_curie_or_absiri(+any, -any, +any), det).
type_modes(rdfa, safe_curie_or_curie_or_iri(+any, -any, +any), det).
type_modes(rdfa, iri(+any, -any, +any), unknown).
type_modes(rdfa, iri_list(+any, -any, +any), det).
type_modes(rdfa, new_bnode(-any, +any), det).
type_modes(rdfa, iri_attr(+any, +any, -any, +any), semidet).
type_modes(rdfa, has_attribute(+any, +any, -any, +any), semidet).
type_modes(rdfa, has_attribute(+any, +any, +any), semidet).
type_modes(rdfa, complete_lists(+any), det).
type_modes(rdfa, descent(?(any), ?(any)), unknown).
type_modes(rdfa, complete_triples(+any), unknown).
type_modes(rdfa, update_property_value(+any, +any), det).
type_modes(rdfa, step_7_5_10(+any, +any), unknown).
type_modes(rdfa, step_7_5_9(+any, +any), unknown).
type_modes(rdfa, add_list_mapping(+any, !(any), +any), det).
type_modes(rdfa, get_list_mapping(+any, +any, -any), semidet).
type_modes(rdfa, empty_list_mapping(+any), semidet).
type_modes(rdfa, empty_list_mapping(-any), det).
type_modes(rdfa, update_list_mapping(+any, +any), det).
type_modes(rdfa, emit_typeof(+any, +any), det).
type_modes(rdfa, update_subject(+any, +any), det).
type_modes(rdfa, update_lang(+any, +any), det).
type_modes(rdfa, update_prefixes(+any, +any), det).
type_modes(rdfa, update_vocab(+any, +any), det).
type_modes(rdfa, rdfa_local_context(?(any), ?(any)), unknown).
type_modes(rdfa, rdfa_core_prefix(?(any), ?(any)), nondet).
type_modes(rdfa, default_prefixes(+any, -any), unknown).
type_modes(rdfa, rdfa_evaluation_context(+any, -any, +any), unknown).
type_modes(rdfa, xml_rdfa(+any, -any, +any), unknown).
type_modes(rdfa, detect_bom(+any, +any, -any), det).
type_modes(rdfa, read_rdfa(+any, -any, +any), det).
type_modes(prolog_history, prolog_history(+any), det).
type_modes(prolog_history, dir_history_file(?(any), ?(any)), nondet).
type_modes(prolog_history, dir_history_file(+any, -any), det).
type_modes(prolog_history, history_directory(-any), semidet).
type_modes(rdf_portray, rdf_portray_lang(+any), det).
type_modes(rdf_portray, rdf_portray_as(+any), det).
type_modes(crypto_hash, hash_atom(+any, -any), det).
type_modes(crypto_hash, file_sha1(+any, -atom), det).
type_modes(crypto_hash, hmac_sha(+any, +any, -any, +any), det).
type_modes(crypto_hash, sha_hash_ctx(+any, +any, -any, -any), det).
type_modes(crypto_hash, sha_new_ctx(-any, +any), det).
type_modes(crypto_hash, sha_hash(+any, -any, +any), det).
type_modes(websocket, ws_mask(-any), unknown).
type_modes(websocket, ws_opcode(?(any), ?(any)), unknown).
type_modes(websocket, to_opcode(+any, -int), det).
type_modes(websocket, ws_property(+any, ?(any)), nondet).
type_modes(websocket, ws_read_header(+any, -any, -any), det).
type_modes(websocket, ws_send(+any), det).
type_modes(websocket, ws_start_message(+any, +any, +any), det).
type_modes(websocket, ws_start_message(+any, +any), det).
type_modes(websocket, ws_open(+any, -any, +any), det).
type_modes(websocket, ws_close(+stream_pair, +any, +any), det).
type_modes(websocket, read_text_data(+any, +any, -any, +any), det).
type_modes(websocket, ws_receive(+any, -dict, +any), det).
type_modes(websocket, ws_receive(+any, -dict), det).
type_modes(websocket, ws_send(+any, +any), det).
type_modes(websocket, sec_websocket_accept(+any, -any), det).
type_modes(websocket, http_upgrade_to_websocket(:(any), +any, +any), unknown).
type_modes(websocket, http_open_websocket(+any, -any, +any), det).
type_modes(archive, archive_create(+any, +any, +any), det).
type_modes(archive, archive_data_stream(+any, -any, +any), nondet).
type_modes(archive, archive_entries(+any, -any), det).
type_modes(archive, archive_extract(+any, +any, +any), unknown).
type_modes(archive, archive_header_property(+any, ?(any)), unknown).
type_modes(archive, archive_set_header_property(+any, +any), unknown).
type_modes(archive, archive_open_entry(+any, -any), det).
type_modes(archive, archive_next_header(+any, -any), semidet).
type_modes(archive, archive_property(+any, ?(any)), nondet).
type_modes(archive, archive_close(+any), det).
type_modes(archive, archive_open(+any, +any, -any, +any), det).
type_modes(prolog_colour, specified_dict_kv(+any, +any, +any, +any), unknown).
type_modes(prolog_colour, specified_items(+any, +any, +any, +any), unknown).
type_modes(prolog_colour, term_colours(+any, -any, -any), unknown).
type_modes(prolog_colour, syntax_colour(?(any), ?(any)), nondet).
type_modes(prolog_colour, def_style(+any, -any), unknown).
type_modes(prolog_colour, goal_classification(+any, -any), unknown).
type_modes(prolog_colour, goal_classification(+any, +any, +any, -any), unknown).
type_modes(prolog_colour, colourise_prolog_flag_name(+any, +any, +any), unknown).
type_modes(prolog_colour, colourise_op_declaration(?(any), ?(any), ?(any)), det).
type_modes(prolog_colour, colourise_declaration(+any, +any, +any), det).
type_modes(prolog_colour, colourise_declarations(+any, +any, +any), unknown).
type_modes(prolog_colour, colourise_imports(+any, +any, +any, +any), unknown).
type_modes(prolog_colour, colourise_exports(+any, +any, +any), unknown).
type_modes(prolog_colour, colourise_dict_kv(+any, +any, +any), unknown).
type_modes(prolog_colour, colourise_qq_type(+any, +any, +any), unknown).
type_modes(prolog_colour, colourise_term_args(+any, +any, +any), unknown).
type_modes(prolog_colour, classify_class(+any, +any, -any), unknown).
type_modes(prolog_colour, colourise_class(?(any), ?(any), ?(any)), unknown).
type_modes(prolog_colour, colourise_langoptions(+any, +any, +any), det).
type_modes(prolog_colour, colourise_directory(+any, +any, +any), unknown).
type_modes(prolog_colour, colourise_files(+any, +any, +any, +any), unknown).
type_modes(prolog_colour, colourise_setof(+any, +any, +any), unknown).
type_modes(prolog_colour, expand_meta(+any, +any, -any), semidet).
type_modes(prolog_colour, meta_args(+any, +any, -any), semidet).
type_modes(prolog_colour, colourise_goal_args(+any, +any, +any), unknown).
type_modes(prolog_colour, colourise_goal(+any, +any, +any, +any), unknown).
type_modes(prolog_colour, colourise_method_body(+any, +any, +any), unknown).
type_modes(prolog_colour, colourise_directive(+any, +any, +any), unknown).
type_modes(prolog_colour, functor_position(+any, -any, -any), unknown).
type_modes(prolog_colour, colourise_extern_head(+any, +any, +any, +any), unknown).
type_modes(prolog_colour, colourise_extended_head(+any, +any, +any, +any), det).
type_modes(prolog_colour, colourise_term(+any, +any, +any), unknown).
type_modes(prolog_colour, structured_command_start(-any), unknown).
type_modes(prolog_colour, colourise_term(+any, +any, +any, +any), unknown).
type_modes(prolog_colour, prolog_colourise_term(+any, +any, :(any), +any), unknown).
type_modes(prolog_colour, prolog_colourise_query(+string, +any, :(any)), unknown).
type_modes(prolog_colour, process_use_module(+any, +any), unknown).
type_modes(prolog_colour, fix_operators(+any, +any), det).
type_modes(prolog_colour, safe_push_op(+any, +any, :(any), +any), unknown).
type_modes(prolog_colour, colour_item(+any, +any, +any), det).
type_modes(prolog_colour, read_error(+any, +any, +any, +any), failure).
type_modes(prolog_colour, source_module(+any, -any), semidet).
type_modes(prolog_colour, prolog_colourise_stream(+any, +any, :(any)), det).
type_modes(pldoc_process, compile_comment(+any, +any, +any, -any), semidet).
type_modes(pldoc_process, comment_warning(+any, +any), failure).
type_modes(pldoc_process, process_structured_comment(+any, +string, +list, +any), det).
type_modes(pldoc_process, parse_comment(+any, +any, -any), semidet).
type_modes(pldoc_process, process_comments(+list, +any, +any), det).
type_modes(pldoc_process, doc_comment(?(any), -any, -string, -string), nondet).
type_modes(pldoc_process, doc_file_has_comments(+atom), semidet).
type_modes(pldoc_process, doc_file_name(+atom, -atom, +list), det).
type_modes(pldoc_process, separator_line(?(list), ?(list)), semidet).
type_modes(pldoc_process, structured_comment(-list(codes), -any), semidet).
type_modes(pldoc_process, is_structured_comment(+string, -list(codes)), semidet).
type_modes(pldoc_process, prolog:predicate_summary(+any, -any), semidet).
type_modes(test_wizard, call_test(:(any), -any), nondet).
type_modes(test_wizard, make_template(+any, -any), det).
type_modes(test_wizard, error(+any, -any), unknown).
type_modes(test_wizard, success(+any, +any, -any), det).
type_modes(test_wizard, test_result(+any, +any, -any), det).
type_modes(test_wizard, pred_name(+any, -any), det).
type_modes(test_wizard, find_test_module(+any, ?(any), -any), unknown).
type_modes(test_wizard, make_test(+callable, -any, -term), det).
type_modes(test_wizard, make_tests(+any, +any, +any), det).
type_modes(modules, in_temporary_module(?(any), :(any), :(any)), unknown).
type_modes(authenticate, http:authenticate(+any, +any, -any), unknown).
type_modes(authenticate, http_write_passwd_file(+any, +list), det).
type_modes(authenticate, http_read_passwd_file(+any, -any), det).
type_modes(authenticate, update_passwd(+any, -any), det).
type_modes(authenticate, http_current_user(+any, ?(any), ?(any)), nondet).
type_modes(authenticate, validate(+any, +any, +any, -any), unknown).
type_modes(authenticate, cached_authenticated(+any, +any, -any, -any), unknown).
type_modes(authenticate, http_authorization_data(+any, ?(any)), semidet).
type_modes(authenticate, http_authenticate(+any, +any, -any), unknown).
type_modes(aggregate, sandbox:safe_meta(+any, -any), semidet).
type_modes(aggregate, list_is_free_of(+any, +any), semidet).
type_modes(aggregate, term_is_free_of(+any, +any), semidet).
type_modes(aggregate, free_variables(:(any), +any, +any, -any), det).
type_modes(aggregate, foreach(:(any), :(any)), unknown).
type_modes(aggregate, state1(+any, +any, -any, -any), unknown).
type_modes(aggregate, state0(+any, -any, -any), unknown).
type_modes(aggregate, step(+any, +any, +any, -any), unknown).
type_modes(aggregate, max_pair(+any, -any, -any), det).
type_modes(aggregate, min_pair(+any, -any, -any), det).
type_modes(aggregate, aggregate_list(+any, +any, -any), semidet).
type_modes(aggregate, needs_one(+any, -any), unknown).
type_modes(aggregate, template_to_pattern(+any, -any, -any, -any, -any), unknown).
type_modes(aggregate, clean_body(+any, -any), det).
type_modes(aggregate, aggregate_all(+any, +any, :(any), -any), semidet).
type_modes(aggregate, aggregate_all(+any, :(any), -any), semidet).
type_modes(aggregate, aggregate(+any, +any, :(any), -any), nondet).
type_modes(aggregate, aggregate(+any, :(any), -any), nondet).
type_modes(prolog_hotfix, file_module(+any, -any), semidet).
type_modes(prolog_hotfix, select_file_to_reload(+any, +any, -any), det).
type_modes(prolog_hotfix, load_hotfix(+any, +any), det).
type_modes(prolog_hotfix, ensure_dirsep(+any, -any), det).
type_modes(prolog_hotfix, apply_hotfix(+any, +any), det).
type_modes(prolog_hotfix, prolog_source_files(+any, ?(list), ?(list)), det).
type_modes(prolog_hotfix, load_hotfixes(+any), det).
type_modes(timeout, time_out(:(any), +any, -any), nondet).
type_modes(sandbox, prolog:sandbox_allowed_goal(:(any)), det).
type_modes(sandbox, prolog:sandbox_allowed_expansion(:(any)), det).
type_modes(sandbox, safe_directive_flag(+any, +any), det).
type_modes(sandbox, safe_directive(:(any)), semidet).
type_modes(sandbox, prolog:sandbox_allowed_directive(:(any)), det).
type_modes(sandbox, format_calls(+any, +any, -any), unknown).
type_modes(sandbox, safe_output(+any), unknown).
type_modes(sandbox, safe_meta(?(any)), unknown).
type_modes(sandbox, safe_meta_call(+any, -list(callable)), semidet).
type_modes(sandbox, expand_nt(+any, ?(any), ?(any), -any), unknown).
type_modes(sandbox, attr_hook_predicates(+any, +any, -any), det).
type_modes(sandbox, safe_meta(+any, -list(callable)), semidet).
type_modes(sandbox, safe_global_variable(?(any)), semidet).
type_modes(sandbox, safe_global_var(+any), semidet).
type_modes(sandbox, safe_clause(+any), semidet).
type_modes(sandbox, safe_assert(+any), semidet).
type_modes(sandbox, safe_primitive(?(any)), nondet).
type_modes(sandbox, verify_safe_declaration(+any), unknown).
type_modes(sandbox, copy_goal_args(+any, +any, +any, +any), det).
type_modes(sandbox, goal_id(:(any), -any, -any), nondet).
type_modes(sandbox, meta_qualify(:(any), +any, -any), det).
type_modes(sandbox, safe_list(+any, +any, +any, +any, -any), unknown).
type_modes(sandbox, safe_bodies(+any, +any, +any, +any, -any), unknown).
type_modes(sandbox, safe(+any, +any, +any, +any, -any), semidet).
type_modes(sandbox, safe_goal(:(any)), det).
type_modes(sandbox, safe_call(:(any)), unknown).
type_modes(prolog_statistics, predicate_functor_name(+any, -any), unknown).
type_modes(prolog_statistics, predicate_label(+any, -any), unknown).
type_modes(prolog_statistics, prof_node(+any, -any), nondet).
type_modes(prolog_statistics, prof_statistics(-any), det).
type_modes(prolog_statistics, show_profile(+any), unknown).
type_modes(prolog_statistics, profile(:(any), +any), unknown).
type_modes(prolog_statistics, profile(:(any)), unknown).
type_modes(prolog_statistics, time(:(any)), nondet).
type_modes(prolog_statistics, thread_statistics(?(any), -dict), nondet).
type_modes(prolog_statistics, statistics(-dict), det).
type_modes(prolog_statistics, statistics, det).
type_modes(unicode_blocks, unicode_block(?(any), ?(any), ?(any)), unknown).
type_modes(prolog_pretty_print, pprint(+any, +any, +any), unknown).
type_modes(prolog_pretty_print, print_width(+any, -any, +any), det).
type_modes(prolog_pretty_print, indent(+any, +any, +any), unknown).
type_modes(prolog_pretty_print, prepare_term(+any, -any, -any, -any), unknown).
type_modes(prolog_pretty_print, print_term(+any, +any), det).
type_modes(ifprolog, cosh(?(any)), unknown).
type_modes(ifprolog, sinh(?(any)), unknown).
type_modes(ifprolog, letter(+any), unknown).
type_modes(ifprolog, digit(+any), unknown).
type_modes(ifprolog, current_signal(?(any), ?(any)), nondet).
type_modes(ifprolog, current_visible(@any, @any), unknown).
type_modes(ifprolog, predicate_type(:(any), -any), det).
type_modes(ifprolog, retract_with_names(?(any), -any), det).
type_modes(ifprolog, clause_with_names(?(any), ?(any), -any), det).
type_modes(ifprolog, assertz_with_names(@any, +any), det).
type_modes(ifprolog, asserta_with_names(@any, +any), det).
type_modes(ifprolog, set_default_module(+any), det).
type_modes(ifprolog, current_default_module(-any), det).
type_modes(ifprolog, unset_global(+any), det).
type_modes(ifprolog, set_global(+any, ?(any)), det).
type_modes(ifprolog, get_global(+any, ?(any)), det).
type_modes(ifprolog, current_global(+any), semidet).
type_modes(ifprolog, localtime(+any, ?(any), ?(any), ?(any), ?(any), ?(any), ?(any), ?(any), ?(any)), unknown).
type_modes(ifprolog, system_name(-any), unknown).
type_modes(ifprolog, proroot(-any), unknown).
type_modes(ifprolog, prolog_version(-any), unknown).
type_modes(ifprolog, for(+any, ?(any), +any), nondet).
type_modes(ifprolog, list_length(+any, ?(any)), det).
type_modes(ifprolog, index(+any, +any, -any), semidet).
type_modes(ifprolog, parse_atom(+any, +any, ?(any), ?(any), ?(any), ?(any)), unknown).
type_modes(ifprolog, getchar(+any, +any, -any), unknown).
type_modes(ifprolog, if_concat_atom(+any, -any), det).
type_modes(ifprolog, if_concat_atom(+any, +any, -any), det).
type_modes(ifprolog, atom_split(+any, +any, ?(any)), unknown).
type_modes(ifprolog, atom_suffix(+any, +any, -any), det).
type_modes(ifprolog, atom_prefix(+any, +any, -any), det).
type_modes(ifprolog, atom_part(+any, +any, +any, -any), det).
type_modes(ifprolog, get_until(@any, +any, -any, -any), det).
type_modes(ifprolog, get_until(+any, -any, -any), det).
type_modes(ifprolog, format_modifiers(-any, -any, -any), det).
type_modes(ifprolog, write_formatted(@any, +any, +any), det).
type_modes(ifprolog, write_formatted(+any, +any), det).
type_modes(ifprolog, write_formatted_atom(-any, +any, +any), det).
type_modes(ifprolog, current_error(-any), unknown).
type_modes(ifprolog, write_atom(+any, -any), unknown).
type_modes(ifprolog, writeq_atom(+any, -any), unknown).
type_modes(ifprolog, assign_alias(+any, @any), det).
type_modes(ifprolog, filepos(@any, -any, -any), unknown).
type_modes(ifprolog, getcwd(-any), unknown).
type_modes(ifprolog, filepos(@any, -any), unknown).
type_modes(ifprolog, file_test(+any, +any), unknown).
type_modes(ifprolog, unload(+any), det).
type_modes(ifprolog, load(?(any)), unknown).
type_modes(ifprolog, lower_upper(-any, +any), det).
type_modes(ifprolog, lower_upper(+any, -any), det).
type_modes(ifprolog, match(+any, +any, ?(any)), nondet).
type_modes(ifprolog, match(+any, +any), semidet).
type_modes(ifprolog, user_parameters(-atom), unknown).
type_modes(ifprolog, program_parameters(-atom), unknown).
type_modes(ifprolog, float_format(-any, +any), unknown).
type_modes(ifprolog, debug_config(+any, -any, +any), unknown).
type_modes(ifprolog, ifprolog_debug(:(any)), unknown).
type_modes(ifprolog, debug_mode(:(any), -any, +any), unknown).
type_modes(ifprolog, modify_mode(+any, -any, +any), det).
type_modes(ifprolog, cut_block(+any), semidet).
type_modes(ifprolog, exit_block(+any), unknown).
type_modes(ifprolog, block(:(any), +any, :(any)), unknown).
type_modes(ifprolog, context(:(any), +any), unknown).
type_modes(ifprolog, calling_context(-any), unknown).
type_modes(ifprolog, push_ifprolog_file_extension, unknown).
type_modes(ifprolog, push_ifprolog_library, unknown).
type_modes(ifprolog, pi_list_to_pi_term(+any, -any), det).
type_modes(ifprolog, end_module(+any), unknown).
type_modes(ifprolog, begin_module(+any), unknown).
type_modes(ifprolog, module(+any), unknown).
type_modes(ifprolog,  (discontiguous+any), det).
type_modes(ifprolog, export(+any), det).
type_modes(ifprolog, meta(+any), unknown).
type_modes(ifprolog, ifprolog_term_expansion(+any, +any), unknown).
type_modes(ifprolog, retract(?(any), ?(any)), det).
type_modes(ifprolog, assertz(?(any), ?(any)), det).
type_modes(ifprolog, asserta(?(any), ?(any)), det).
type_modes(ifprolog, context(:(any), ?(any)), unknown).
type_modes(ifprolog, ifprolog_goal_expansion(+any, +any), unknown).
type_modes(doc_access, ip(?(any), ?(list), ?(list)), semidet).
type_modes(doc_access, primary_forwarded_host(+any, -any), det).
type_modes(doc_access, allowed_peer(+any), semidet).
type_modes(doc_access, match_peer(:(any), +any, +any), semidet).
type_modes(doc_access, host_access_options(+any, -any), det).
type_modes(cql_hooks, cql_update_history_hook(+any, +any, +any, +any, +any, +any, +any, +any, +any, +any, +any, +any, +any, +any, +any, +any), unknown).
type_modes(http_dirindex, http:mime_type_icon(+any, -any), nondet).
type_modes(http_dirindex, mime_type_icon(+any, -any), det).
type_modes(http_dirindex, directory_index(+any, +any, ?(list), ?(list)), det).
type_modes(http_dirindex, http_reply_dirindex(+any, +any, +any), det).
type_modes(rdf_turtle_write, tw_quoted_string(+any, +any, +any), det).
type_modes(rdf_turtle_write, tw_abbreviated_literal(+any, +any, +any, +any), semidet).
type_modes(rdf_turtle_write, tw_literal(+any, +any, +any), det).
type_modes(rdf_turtle_write, tw_resource(+any, +any, +any), det).
type_modes(rdf_turtle_write, bnode_hash(+any, -any), det).
type_modes(rdf_turtle_write, next_bnode_id(+any, +any, -any), det).
type_modes(rdf_turtle_write, bnode_to_term(+any, -any, +any), unknown).
type_modes(rdf_turtle_write, sort_bnode_pairs(+any, -any, +any), det).
type_modes(rdf_turtle_write, sort_bnodes(+any, -any, +any), det).
type_modes(rdf_turtle_write, subjects(+any, -ord_set), det).
type_modes(rdf_turtle_write, subject_triples(+any, +any, -any), det).
type_modes(rdf_turtle_write, nl_indent(+any, +any, +any), det).
type_modes(rdf_turtle_write, object_link_count(+any, +any, -any), det).
type_modes(rdf_turtle_write, unshared_collection(+any, +any, -any), semidet).
type_modes(rdf_turtle_write, tw_unshared_bnode(+any, +any, +any), semidet).
type_modes(rdf_turtle_write, tw_bnode_object(+any, +any, +any), det).
type_modes(rdf_turtle_write, tw_object(+any, +any, +any), det).
type_modes(rdf_turtle_write, tw_triples(+any, +any, +any, +any), det).
type_modes(rdf_turtle_write, tw_ungrouped_triples(+any, +any, +any, +any), unknown).
type_modes(rdf_turtle_write, tw_subject_triples(+any, +any, +any, +any), unknown).
type_modes(rdf_turtle_write, tw_cyclic_bnodes(+any, +any, +any, +any, +any), unknown).
type_modes(rdf_turtle_write, tw_top_bnodes(+any, +any, +any, -any), unknown).
type_modes(rdf_turtle_write, tw_bnodes(+any, +any, +any), det).
type_modes(rdf_turtle_write, tw_subject(+any, +any, +any), det).
type_modes(rdf_turtle_write, tw_proper_subjects(+any, +any, +any), det).
type_modes(rdf_turtle_write, tw_prefix_map(+any, +any, +any), det).
type_modes(rdf_turtle_write, tw_graph(+any, +any), det).
type_modes(rdf_turtle_write, init_base(+any, -any), det).
type_modes(rdf_turtle_write, namespace_parts(+any, -any), unknown).
type_modes(rdf_turtle_write, propose_abbrev(+any, +any, +any, -any), multi).
type_modes(rdf_turtle_write, turtle_prefix(+any, +any, +any, +any), semidet).
type_modes(rdf_turtle_write, user_prefix_map(+any, -any), det).
type_modes(rdf_turtle_write, init_prefix_map(+any, -any), det).
type_modes(rdf_turtle_write, open_output(+any, +any, -any, -any), det).
type_modes(rdf_turtle_write, trig_graphs(+any, -any), det).
type_modes(rdf_turtle_write, rdf_save_canonical_trig(+any, :(any)), det).
type_modes(rdf_turtle_write, rdf_save_trig(+any, :(any)), det).
type_modes(rdf_turtle_write, rdf_save_ntriples(+any, :(any)), det).
type_modes(rdf_turtle_write, rdf_save_canonical_turtle(+any, :(any)), det).
type_modes(rdf_turtle_write, rdf_save_turtle(+any, :(any)), det).
type_modes(http_open, http:update_cookies(+any, +any, +any), semidet).
type_modes(http_open, http:write_cookies(+any, +any, +any), semidet).
type_modes(http_open, http:open_options(+any, -any), nondet).
type_modes(http_open, keep_alive_error(+any), unknown).
type_modes(http_open, http_close_keep_alive(+any), det).
type_modes(http_open, keep_connection(+any), semidet).
type_modes(http_open, read_incomplete(+any, +any), semidet).
type_modes(http_open, consider_keep_alive(+any, +any, +any, +any, -any, +any), det).
type_modes(http_open, iostream:open_hook(+any, +any, -any, -any, +any, -any), semidet).
type_modes(http_open, parts_authority(+any, -any), semidet).
type_modes(http_open, parts_search(+any, -any), det).
type_modes(http_open, parts_request_uri(+any, -any), det).
type_modes(http_open, parts_uri(+any, -any), det).
type_modes(http_open, parts_scheme(+any, -any), det).
type_modes(http_open, parse_url_ex(+any, -any), unknown).
type_modes(http_open, authorization(+any, -any), semidet).
type_modes(http_open, http_set_authorization(+any, +any), det).
type_modes(http_open, rest(-atom, ?(list), ?(list)), unknown).
type_modes(http_open, integer(-any, ?(list), ?(list)), unknown).
type_modes(http_open, content_length(+any, -int), semidet).
type_modes(http_open, read_header(+stream, -any, -int, -atom, -list), det).
type_modes(http_open, content_encoding(+any, -any), semidet).
type_modes(http_open, transfer_encoding(+any, -any), semidet).
type_modes(http_open, http:disable_encoding_filter(+any), semidet).
type_modes(http_open, transfer_encoding_filter(+any, +any, -any), det).
type_modes(http_open, return_final_url(+any), semidet).
type_modes(http_open, parse_headers(+any, -list(compound)), det).
type_modes(http_open, open_socket(+any, -any, +any), det).
type_modes(http_open, map_error_code(+any, -any), semidet).
type_modes(http_open, redirect_options(+any, -any), det).
type_modes(http_open, redirect_loop(+any, +any), semidet).
type_modes(http_open, do_open(+any, +any, +any, +any, +any, +any, +any, +any, -any), det).
type_modes(http_open, auth_header(+any, +any, +any, +any), unknown).
type_modes(http_open, x_headers(+any, +any, +any), det).
type_modes(http_open, http_version(-atom), det).
type_modes(http_open, send_rec_header(+any, -any, +any, +any, +any, +any), det).
type_modes(http_open, autoload_https(+any), det).
type_modes(http_open, http_open(+any, -any, +any), det).
type_modes(http_open, user_agent(-any), det).
type_modes(emacs_prolog_colours, blocked(+any), semidet).
type_modes(term_html, blob_rendering(+any, +any, +any, ?(list), ?(list)), semidet).
type_modes(term_html, primitive_class(+any, +any, -any, -any), det).
type_modes(term_html, primitive(+any, -any), semidet).
type_modes(term_html, dict(+any, +any, ?(list), ?(list)), unknown).
type_modes(term_html, end_code_type(+any, -any, ?(any)), unknown).
type_modes(term_html, space(@any, @any, +any, ?(list), ?(list)), unknown).
type_modes(term_html, op2(+any, +any, +any, +any, +any, ?(list), ?(list)), det).
type_modes(term_html, op1(+any, +any, +any, +any, +any, ?(list), ?(list)), det).
type_modes(term_html, operator_module(-any, +any), det).
type_modes(term_html, op2(+any, -any, -any, -any, +any), semidet).
type_modes(term_html, op1(+any, -any, -any, -any, +any), semidet).
type_modes(term_html, list(+any, +any, ?(list), ?(list)), unknown).
type_modes(term_html, args(+any, +any, +any, +any, ?(list), ?(list)), unknown).
type_modes(term_html, arg_options(+any, +any, -any), det).
type_modes(term_html, arg_options(+any, -any), det).
type_modes(term_html, compound(+any, +any, ?(list), ?(list)), det).
type_modes(term_html, term(@any, +any, ?(list), ?(list)), det).
type_modes(rdf, on_xmlns(+any, +any, +any), unknown).
type_modes(rdf, process_rdf(+any, :(any), :(any)), unknown).
type_modes(rdf, xml_to_rdf(+any, -any, +any), unknown).
type_modes(rdf, load_rdf(+any, -any, :(any)), det).
type_modes(rdf, load_rdf(+any, -any), det).
type_modes(prolog_pack, confirm(+any, +any, +any), semidet).
type_modes(prolog_pack, menu(?(any), +any, +any, -any, +any), unknown).
type_modes(prolog_pack, run_process_output(+any, +any, :(any), +any), det).
type_modes(prolog_pack, run_process(+any, +any, +any), det).
type_modes(prolog_pack, resolve_dependencies(+any, -any), multi).
type_modes(prolog_pack, pack_depends_on(?(any), ?(any)), nondet).
type_modes(prolog_pack, pack_provides(?(any), ?(any)), multi).
type_modes(prolog_pack, validate_dependencies, det).
type_modes(prolog_pack, update_dependency_db, unknown).
type_modes(prolog_pack, available_download_versions(+any, -any), det).
type_modes(prolog_pack, install_dependency(+any, +any), unknown).
type_modes(prolog_pack, select_dependency_resolution(+any, -any, +any), unknown).
type_modes(prolog_pack, inquiry_result(+any, +any, +any), semidet).
type_modes(prolog_pack, query_pack_server(+any, -any, +any), unknown).
type_modes(prolog_pack, pack_inquiry(+any, +any, +any, +any), semidet).
type_modes(prolog_pack, github_release_url(+any, -any, -any), semidet).
type_modes(prolog_pack, pack_version_file(-any, -any, +any), semidet).
type_modes(prolog_pack, safe_pack_name(+atom), semidet).
type_modes(prolog_pack, git_url(+any, -any), semidet).
type_modes(prolog_pack, pack_property(?(any), ?(any)), nondet).
type_modes(prolog_pack, pack_remove(+any), det).
type_modes(prolog_pack, pack_upgrade(+any), semidet).
type_modes(prolog_pack, post_install_autoload(+any, +any), unknown).
type_modes(prolog_pack, def_environment(-any, -any), nondet).
type_modes(prolog_pack, environment(-any, -any), nondet).
type_modes(prolog_pack, save_build_environment(+any), unknown).
type_modes(prolog_pack, make_foreign(+any, +any), det).
type_modes(prolog_pack, configure_foreign(+any, +any), det).
type_modes(prolog_pack, post_install_foreign(+any, +any, +any), det).
type_modes(prolog_pack, pack_rebuild, det).
type_modes(prolog_pack, pack_rebuild(+any), det).
type_modes(prolog_pack, pack_post_install(+any, +any, +any), det).
type_modes(prolog_pack, download_url(+any), det).
type_modes(prolog_pack, ssl_verify(+any, +any, +any, +any, +any), unknown).
type_modes(prolog_pack, pack_url_file(+any, -any), det).
type_modes(prolog_pack, download_file(+any, +any, -any, +any), det).
type_modes(prolog_pack, pack_install_from_url(+any, +any, +any, +any, +any), unknown).
type_modes(prolog_pack, empty_directory(+any), semidet).
type_modes(prolog_pack, prepare_pack_dir(+any, +any), unknown).
type_modes(prolog_pack, download_file_sanity_check(+any, +any, +any), semidet).
type_modes(prolog_pack, pack_git_info(+any, -any, -any), det).
type_modes(prolog_pack, pack_archive_info(+any, +any, -any, -any), unknown).
type_modes(prolog_pack, pack_unpack(+any, +any, +any, +any), unknown).
type_modes(prolog_pack, pack_install_from_local(+any, +any, +any, +any), unknown).
type_modes(prolog_pack, pack_install(+any, +any, +any), unknown).
type_modes(prolog_pack, pack_install(+any, +any), det).
type_modes(prolog_pack, pack_install(+atom), det).
type_modes(prolog_pack, pack_list(+any), det).
type_modes(prolog_pack, pack_search(+any), det).
type_modes(prolog_pack, pack_info_term(?(any)), nondet).
type_modes(prolog_pack, pack_info_term(+any, ?(any)), nondet).
type_modes(prolog_pack, pack_info(+any), unknown).
type_modes(prolog_pack, pack_list_installed, det).
type_modes(prolog_pack, current_pack(?(any)), nondet).
type_modes(pengine_sandbox, blocked(+any), semidet).
type_modes(prolog_metainference, combine_meta_args(+any, -any), det).
type_modes(prolog_metainference, meta_arg(+any, -any), det).
type_modes(prolog_metainference, meta_annotation(+any, -any), semidet).
type_modes(prolog_metainference, annotate_meta_args(+any, +any, +any, +any, +any), unknown).
type_modes(prolog_metainference, annotate_meta_vars_in_body(+any, +any), det).
type_modes(prolog_metainference, meta_pred_args_in_clause(+any, +any, -any), nondet).
type_modes(prolog_metainference, infer_meta_predicate(:(any), -any), semidet).
type_modes(prolog_metainference, inferred_meta_predicate(:(any), ?(any)), nondet).
type_modes(snowball, snowball_current_algorithm(?(any)), nondet).
type_modes(snowball, snowball(+any, +any, -any), det).
type_modes(prolog_format, action_types(?(integer), ?(list(type))), unknown).
type_modes(prolog_format, is_action(-integer), multi).
type_modes(prolog_format, is_action(+integer), semidet).
type_modes(prolog_format, text_codes(?(text), ?(codes)), unknown).
type_modes(prolog_format, spec_types(+any, -list(type)), det).
type_modes(prolog_format, format_types(+text, -list), det).
type_modes(prolog_format, format_spec(-any, ?(list), ?(list)), unknown).
type_modes(prolog_format, format_spec(+any, -list), semidet).
type_modes(http_digest, keep_digest_credentials(+any, +any), unknown).
type_modes(http_digest, digest_credentials(+any, +any, -any, -any), semidet).
type_modes(http_digest, http:authenticate_client(+any, +any), semidet).
type_modes(http_digest, http:authenticate(+any, +any, -any), unknown).
type_modes(http_digest, http_digest_password_hash(+any, +any, +any, -any), det).
type_modes(http_digest, http_digest_response(+any, +any, +any, -any, +any), unknown).
type_modes(http_digest, http_parse_digest_challenge(+any, -any), det).
type_modes(http_digest, parse_digest_reponse(-any, ?(list), ?(list)), unknown).
type_modes(http_digest, parse_digest_request(-any, ?(list), ?(list)), unknown).
type_modes(http_digest, http_digest_authenticate(+any, -any, -any, +any), unknown).
type_modes(http_digest, nonce(+any), unknown).
type_modes(http_digest, http_digest_challenge(+any, +any, ?(list), ?(list)), unknown).
type_modes(http_digest, our_nonce(+any, -string), semidet).
type_modes(http_digest, private_key(-any), det).
type_modes(http_digest, gc_nonce, unknown).
type_modes(http_digest, nonce_ok(+any, +any, -any), semidet).
type_modes(http_digest, register_nonce(+any, +any), det).
type_modes(rdf11_containers, rdfs_nth0(?(any), ?(any), ?(any)), nondet).
type_modes(rdf11_containers, rdfs_member(?(any), ?(any)), nondet).
type_modes(rdf11_containers, rdfs_container_membership_property(?(any), ?(nonneg)), nondet).
type_modes(rdf11_containers, rdfs_container_membership_property(?(any)), nondet).
type_modes(rdf11_containers, rdfs_container(+any, -any), nondet).
type_modes(rdf11_containers, rdfs_assert_container(?(any), +any, +any, +any), unknown).
type_modes(rdf11_containers, rdf_assert_seq(?(any), +any, +any), det).
type_modes(rdf11_containers, rdf_assert_seq(?(any), +any), det).
type_modes(rdf11_containers, rdf_seq(+any, -list), nondet).
type_modes(rdf11_containers, rdf_assert_bag(?(any), +list, +any), det).
type_modes(rdf11_containers, rdf_assert_bag(?(any), +list), det).
type_modes(rdf11_containers, rdf_bag(+any, -list), nondet).
type_modes(rdf11_containers, rdf_assert_alt(?(any), +any, +list, +any), det).
type_modes(rdf11_containers, rdf_assert_alt(?(any), +any, +list), det).
type_modes(rdf11_containers, rdf_alt(+any, ?(any), ?(any)), nondet).
type_modes(optparse, parse_type(+any, +list(code), -any), semidet).
type_modes(optparse, opt_help(+any, -atom), det).
type_modes(optparse, opt_parse(+any, +any, -any, -any, +any), det).
type_modes(optparse, opt_parse(+any, +any, -any, -any), det).
type_modes(optparse, opt_arguments(+any, -any, -any), det).
type_modes(http_exception, keep_alive(+any, -any), det).
type_modes(http_exception, keep_alive(+any), semidet).
type_modes(http_exception, http:bad_request_error(+any, -any), semidet).
type_modes(http_exception, in_or_exclude_backtrace(+any, -any), unknown).
type_modes(http_exception, map_exception_to_http_status(+any, -any, -any, -any), unknown).
type_modes(http_host, global_host(+any, -any), unknown).
type_modes(http_host, primary_forwarded_host(+any, -any), det).
type_modes(http_host, http_current_host(?(any), -any, -any, +any), det).
type_modes(http_host, http_public_host(?(any), -any, -any, +any), det).
type_modes(http_host, http_public_host_url(+any, -any), det).
type_modes(http_host, http_public_url(+any, -any), det).
type_modes(term_to_json, term_to_json(+any, -any), det).
type_modes(term_to_json, term_to_json(+any, +any, -any), det).
type_modes(pengines, sandbox:safe_meta(+any, -any), semidet).
type_modes(pengines, sandbox:safe_primitive(+any), semidet).
type_modes(pengines, pengine_src_url(+any, +any), det).
type_modes(pengines, pengine_src_text(+any, +any), det).
type_modes(pengines, reply_options(+any, +any), semidet).
type_modes(pengines, pengine_user(-any), semidet).
type_modes(pengines, authentication_hook(+any, +any, -any), semidet).
type_modes(pengines, authenticate(+any, +any, -list), det).
type_modes(pengines, allowed(+any, +any), det).
type_modes(pengines, event_to_json(+any, -any, +any), semidet).
type_modes(pengines, add_error_location(+any, +any, -any), det).
type_modes(pengines, add_error_code(+any, +any, -any), det).
type_modes(pengines, add_error_details(+any, +any, -any), unknown).
type_modes(pengines, disable_client_cache, unknown).
type_modes(pengines, write_result(+any, +any, +any), semidet).
type_modes(pengines, portray_blob(+any, +any), det).
type_modes(pengines, output_result(+any, +any, +any), det).
type_modes(pengines, output_result(+any, +any), det).
type_modes(pengines, http_pengine_abort(+any), unknown).
type_modes(pengines, http_pengine_pull_response(+any), unknown).
type_modes(pengines, json_lang(+any), semidet).
type_modes(pengines, fix_bindings(+any, +any, +any, -any, -any), det).
type_modes(pengines, discard_post_data(+any), det).
type_modes(pengines, read_event(+any, +any, +any, -any, -any), unknown).
type_modes(pengines, sync_destroy_queue_from_pengine(+any, +any), unknown).
type_modes(pengines, sync_delay_destroy_queue(+any, +any), det).
type_modes(pengines, sync_destroy_queue_from_http(+any, +any), det).
type_modes(pengines, gc_abandoned_queues, unknown).
type_modes(pengines, destroy_queue_from_http(+any, +any, +any), semidet).
type_modes(pengines, create_wait_and_output_result(+any, +any, +any, +any, +any, +any), det).
type_modes(pengines, wait_and_output_result(+any, +any, +any, +any, +any), det).
type_modes(pengines, wait_and_output_result(+any, +any, +any, +any), det).
type_modes(pengines, template_varnames(+any, +any, -any), unknown).
type_modes(pengines, http_pengine_create(+any, +any, +any, +any), unknown).
type_modes(pengines, http_pengine_create(+any), unknown).
type_modes(pengines, output(+any, +any), semidet).
type_modes(pengines, prompt(+any, +any, -any), semidet).
type_modes(pengines, pengine_rpc(+any, +any, +any), nondet).
type_modes(pengines, pengine_rpc(+any, +any), nondet).
type_modes(pengines, pengine_event_loop(:(any), +any), det).
type_modes(pengines, pengine_event(?(any), +any), det).
type_modes(pengines, pengine_event(?(any)), det).
type_modes(pengines, probe(+any, +any), det).
type_modes(pengines, remote_send_rec(+any, +any, +any, +any, -any, +any), unknown).
type_modes(pengines, replace_blobs(?(any), ?(any)), det).
type_modes(pengines, send_error(+any), det).
type_modes(pengines, pengine_respond(+any, +any, +any), det).
type_modes(pengines, pengine_input(+any, -any), det).
type_modes(pengines, pengine_pull_response(+any, +any), det).
type_modes(pengines, not_sandboxed(+any, +any), semidet).
type_modes(pengines, pengine_not_sandboxed(+any), semidet).
type_modes(pengines, prepare_goal(+any, -any, +any), semidet).
type_modes(pengines, prepare_goal(+any, +any, -any, +any), det).
type_modes(pengines, ask(+any, :(any), +any), unknown).
type_modes(pengines, more_solutions(+any, +any, +any, +any), unknown).
type_modes(pengines, solve(+any, +any, :(any), +any), det).
type_modes(pengines, guarded_main_loop(+any), det).
type_modes(pengines, prepare_module(+any, +any, +any), semidet).
type_modes(pengines, pengine_prepare_source(:(any), +any), det).
type_modes(pengines, fix_streams, det).
type_modes(pengines, pengine_main(+any, +any, +any), unknown).
type_modes(pengines, pengine_done, det).
type_modes(pengines, create(+any, -any, +any, +any, +any), det).
type_modes(pengines, thread_pool:create_pool(+any), det).
type_modes(pengines, local_pengine_create(+any), unknown).
type_modes(pengines, pengine_debug(+any, +any), det).
type_modes(pengines, pengine_output(+any), det).
type_modes(pengines, pengine_property(?(any), ?(any)), nondet).
type_modes(pengines, current_pengine_application(?(any)), nondet).
type_modes(pengines, pengine_application(+any), det).
type_modes(pengines, pengine_unregister(+any), unknown).
type_modes(pengines, pengine_unregister(+any), det).
type_modes(pengines, pengine_register_remote(+any, +any, +any, +any, +any), det).
type_modes(pengines, pengine_register_local(+any, +any, +any, +any, +any, +any), det).
type_modes(pengines, current_pengine(?(any), ?(any), ?(any)), unknown).
type_modes(pengines, pengine_destroy(+any, +any), det).
type_modes(pengines, pengine_destroy(+any), det).
type_modes(pengines, pengine_abort(+any), det).
type_modes(pengines, pengine_stop(+any, +any), det).
type_modes(pengines, pengine_next(+any, +any), det).
type_modes(pengines, pengine_ask(+any, @any, +any), det).
type_modes(pengines, pengine_reply(+any, +any), det).
type_modes(pengines, pengine_reply(+any), det).
type_modes(pengines, pengine_request(-any), det).
type_modes(pengines, pengine_send(+any, +any, +any), det).
type_modes(pengines, pengine_send(+any, +any), det).
type_modes(pengines, translate_local_sources(+any, -any, +any), det).
type_modes(pengines, pengine_create(:(any)), det).
type_modes(varnumbers, max_var_number(+any, +any, -any), det).
type_modes(varnumbers, roundup_next_power_two(+any, -any), det).
type_modes(varnumbers, varnumbers(+any, +any, -any), det).
type_modes(varnumbers, varnumbers(+any, -any), det).
type_modes(varnumbers, numbervars(+any), det).
type_modes(http_path, http_clean_location_cache, unknown).
type_modes(http_path, path_list(+any, -any), det).
type_modes(http_path, clean_segments(+any, -any), det).
type_modes(http_path, relative_to(+any, +any, -any), det).
type_modes(http_path, http_location_path(+any, -any, -any), nondet).
type_modes(http_path, http_location_path(+any, -any), det).
type_modes(http_path, http_absolute_location(+any, -any, +any), det).
type_modes(http_path, http_absolute_uri(+any, -any), det).
type_modes(http_path, http:location(+any, -any, -any), nondet).
type_modes(settings, convert_setting_text(+any, +any, -any), unknown).
type_modes(settings, list_settings(+any), det).
type_modes(settings, list_settings, det).
type_modes(settings, setting_property(?(any), ?(any)), nondet).
type_modes(settings, setting_property(+any, +any), det).
type_modes(settings, current_setting(?(any)), nondet).
type_modes(settings, save_settings(+any), det).
type_modes(settings, save_settings, det).
type_modes(settings, store_setting(?(any), +any), unknown).
type_modes(settings, load_settings(?(any), +any), det).
type_modes(settings, load_settings(?(any)), det).
type_modes(settings, check_type(+any, +any), unknown).
type_modes(settings, set_setting_default(:(any), +any), det).
type_modes(settings, restore_setting(:(any)), det).
type_modes(settings, set_setting(:(any), +any), det).
type_modes(settings, numeric_type(+any, -any), unknown).
type_modes(settings, env(+atom, +number, -number), det).
type_modes(settings, env(+atom, -number), det).
type_modes(settings, expr_to_list(+any, +any, ?(list), ?(list)), det).
type_modes(settings, eval_list_default(+any, +any, +any, -any), unknown).
type_modes(settings, eval_default(+any, +any, +any, -any), det).
type_modes(settings, clear_setting_cache, det).
type_modes(settings, setting(:(any), ?(any)), nondet).
type_modes(settings, setting(:(any), +any, +any, +any), det).
type_modes(mime, mime_default_charset(-any, +any), det).
type_modes(mime, mime_parse(+any, -any), det).
type_modes(assoc, del_assoc(+any, +any, ?(any), -any), semidet).
type_modes(assoc, del_max_assoc(+any, ?(any), ?(any), -any), semidet).
type_modes(assoc, del_min_assoc(+any, ?(any), ?(any), -any), semidet).
type_modes(assoc, put_assoc(+any, +any, +any, -any), det).
type_modes(assoc, min_assoc(+any, -any, -any), semidet).
type_modes(assoc, max_assoc(+any, -any, -any), semidet).
type_modes(assoc, map_assoc(:(any), ?(any), ?(any)), semidet).
type_modes(assoc, map_assoc(:(any), +any), semidet).
type_modes(assoc, ord_pairs(+list(_-_)), semidet).
type_modes(assoc, ord_list_to_assoc(+list(_-_), -any), det).
type_modes(assoc, list_to_assoc(+list(_-_), -any), det).
type_modes(assoc, get_assoc(+any, +any, +any, -any, +any), semidet).
type_modes(assoc, get_assoc(+any, +any, -any), semidet).
type_modes(assoc, gen_assoc(?(any), +any, ?(any)), nondet).
type_modes(assoc, is_assoc(+any), unknown).
type_modes(assoc, assoc_to_values(+any, -list), det).
type_modes(assoc, assoc_to_keys(+any, -ord_set), det).
type_modes(assoc, assoc_to_list(+any, -list(_-_)), semidet).
type_modes(assoc, empty_assoc(+any), semidet).
type_modes(assoc, empty_assoc(-any), det).
type_modes(pwp, pwp_is_codes(+any), unknown).
type_modes(pwp, pwp_attach(+any, ?(list(xml)), ?(list(xml))), unknown).
type_modes(pwp, pwp_unite(+any, +list(atom=any)), unknown).
type_modes(pwp, pwp_unite(+any, +any, -list(atom=any)), unknown).
type_modes(pwp, pwp_attr(+any, -any), semidet).
type_modes(pwp, pwp_attributes(+list(atom=atom), -atom, -atom, -atom, -atom, -atom, -list(atom=atom)), unknown).
type_modes(pwp, pwp_xml(:(list(xml)), -list(xml), +any), unknown).
type_modes(pwp, pwp_stream(:(input_stream), +output_stream, +list), det).
type_modes(pwp, pwp_files(:(atom), +atom), det).
type_modes(read_util, read_file_to_terms(+any, -any, +any), det).
type_modes(read_util, read_file_to_string(+any, -any, +any), det).
type_modes(read_util, read_file_to_codes(+any, -any, +any), det).
type_modes(read_util, read_stream_to_terms(+any, -any, ?(any), +any), det).
type_modes(read_util, read_stream_to_codes(+any, -any, ?(any)), det).
type_modes(read_util, read_stream_to_codes(+any, -any), det).
type_modes(read_util, read_line_to_string(+any, -any), det).
type_modes(read_util, read_line_to_codes(+any, -any, ?(any)), det).
type_modes(read_util, read_line_to_codes(+stream, -codes), det).
type_modes(clpb_hash, blocked(+any), semidet).
type_modes(sort, locale_sort(+any, -any), det).
type_modes(sort, predsort(:(any), +any, -any), det).
type_modes(utf8, utf8_codes(?(any), ?(list), ?(list)), det).
type_modes(charsio, with_output_to_chars(:(any), -any, -any, ?(any)), det).
type_modes(charsio, with_output_to_chars(:(any), -any, ?(any)), det).
type_modes(charsio, with_output_to_chars(:(any), -any), det).
type_modes(charsio, open_chars_stream(+any, -any), det).
type_modes(charsio, read_term_from_chars(+any, -any, +any), det).
type_modes(charsio, read_from_chars(+any, -any), det).
type_modes(charsio, number_to_chars(+any, -any, ?(any)), det).
type_modes(charsio, number_to_chars(+any, -any), det).
type_modes(charsio, atom_to_chars(+any, -any, ?(any)), det).
type_modes(charsio, atom_to_chars(+any, -any), det).
type_modes(charsio, write_to_chars(+any, -any, ?(any)), unknown).
type_modes(charsio, write_to_chars(+any, -any), unknown).
type_modes(charsio, format_to_chars(+any, +any, -any, ?(any)), det).
type_modes(charsio, format_to_chars(+any, +any, -any), det).
type_modes(prolog_stream, open_prolog_stream(+any, +any, -any, +any), unknown).
type_modes(mime_pack, make_boundary(+any, ?(any)), det).
type_modes(mime_pack, mime_pack(+any, +stream, ?(any)), det).
type_modes(pldoc, tag_order(?(any), ?(any)), semidet).
type_modes(pldoc, pldoc_loading, semidet).
type_modes(pldoc, doc_collect(+any), det).
type_modes(block_directive, rename_clause(+any, +any, -any), det).
type_modes(block_directive, wrap_block(+any, +any, -any), det).
type_modes(block_directive, block(+any), unknown).
type_modes(uid, set_user_and_group(+any, +any), det).
type_modes(uid, set_user_and_group(+any), det).
type_modes(uid, setegid(+any), unknown).
type_modes(uid, setgid(+any), unknown).
type_modes(uid, seteuid(+any), unknown).
type_modes(uid, setuid(+any), unknown).
type_modes(uid, group_data(?(any), ?(any), ?(any)), unknown).
type_modes(uid, group_info(+any, -any), det).
type_modes(uid, user_data(?(any), ?(any), ?(any)), unknown).
type_modes(uid, user_info(+any, -any), det).
type_modes(uid, getegid(-any), det).
type_modes(uid, geteuid(-any), det).
type_modes(uid, getgid(-any), det).
type_modes(uid, getuid(-any), det).
type_modes(rdf_parser, remove_fragment(+any, -any), unknown).
type_modes(rdf_parser, modify_state(+any, +any, -any, -any), semidet).
type_modes(rdf_parser, an_rdf_object(-any, +any), unknown).
type_modes(rdf_parser, typed_literal(+any, +any, -any, +any), unknown).
type_modes(rdf_parser, mkliteral(+any, -any, +any), unknown).
type_modes(rdf_parser, literal_value(+any, -any, +any), unknown).
type_modes(rdf_parser, element_to_plrdf(+any, -any, +any), unknown).
type_modes(rdf_parser, xml_to_plrdf(+any, -any, +any), unknown).
type_modes(rdf_parser, rdf_name_space(?(any)), nondet).
type_modes(doc_util, ensure_slash_end(+any, -any), det).
type_modes(doc_util, path_alias(?(any), ?(any)), nondet).
type_modes(doc_util, expand_alias(+any, -any), det).
type_modes(doc_util, insert_alias(+any, -any), det).
type_modes(prolog_main, interrupt(+any), unknown).
type_modes(prolog_main, run_main(+any, +any), unknown).
type_modes(prolog_main, main, unknown).
type_modes(codesio, with_output_to_codes(:(any), -any, -any, ?(any)), det).
type_modes(codesio, with_output_to_codes(:(any), -any, ?(any)), det).
type_modes(codesio, with_output_to_codes(:(any), ?(any)), det).
type_modes(codesio, open_codes_stream(+any, -any), det).
type_modes(codesio, read_term_from_codes(+any, -any, +any), det).
type_modes(codesio, read_from_codes(+any, -any), det).
type_modes(codesio, write_term_to_codes(+any, -any, ?(any), +any), det).
type_modes(codesio, write_term_to_codes(+any, -any, +any), det).
type_modes(codesio, write_to_codes(+any, -any, ?(any)), unknown).
type_modes(codesio, write_to_codes(+any, -any), unknown).
type_modes(codesio, format_to_codes(+any, +any, -any, ?(any)), det).
type_modes(codesio, format_to_codes(+any, +any, -any), det).
type_modes(http_log, log_check_deleted(+any), semidet).
type_modes(http_log, log_completed(+any, +any, +any, +any, +any, +any), det).
type_modes(http_log, nolog(+any), unknown).
type_modes(http_log, password_field(+any), semidet).
type_modes(http_log, log_request(+any, -any), unknown).
type_modes(http_log, log_started(+any, +any, +any), det).
type_modes(http_log, http_log(+any, +any), det).
type_modes(http_log, http_log_close(+any), det).
type_modes(http_log, http_log_stream(-any), semidet).
type_modes(check, sort_reference_key(+any, -any), det).
type_modes(check, checker(:(any), +text), unknown).
type_modes(check, valid_string_goal(+any), semidet).
type_modes(check, string_predicate(:(any)), unknown).
type_modes(check, goal_in_body(-any, +any, +any), nondet).
type_modes(check, list_strings(+any), det).
type_modes(check, list_strings, det).
type_modes(check, qualify_meta_goal(+any, +any, +any, -any), unknown).
type_modes(check, trivial_fail_goal(:(any)), unknown).
type_modes(check, list_trivial_fails(+any), det).
type_modes(check, list_trivial_fails, det).
type_modes(check, list_void_declarations, det).
type_modes(check, list_redefined, unknown).
type_modes(check, referenced(+any, ?(any), -any), nondet).
type_modes(check, list_autoload, det).
type_modes(check, list_undefined(+any), det).
type_modes(check, list_undefined, det).
type_modes(check, check, det).
type_modes(git, git_format_string(:(any), -any, -any), unknown).
type_modes(git, git_show(+any, +any, -any, +any), det).
type_modes(git, git_shortlog(+any, -any, +any), det).
type_modes(git, git_tags_on_branch(+any, +any, -any), det).
type_modes(git, git_branches(-any, +any), det).
type_modes(git, git_default_branch(-any, +any), det).
type_modes(git, git_remote_branches(+any, -any), det).
type_modes(git, git_ls_remote(+any, -any, +any), det).
type_modes(git, git_remote_url(+any, -any, +any), det).
type_modes(git, git_ls_tree(-any, +any), det).
type_modes(git, git_hash(-any, +any), det).
type_modes(git, git_is_clean(+any), semidet).
type_modes(git, git_describe(-any, +any), semidet).
type_modes(git, is_git_directory(+any), semidet).
type_modes(git, git_open_file(+any, +any, +any, -any), det).
type_modes(git, git_process_output(+any, :(any), +any), det).
type_modes(git, close_streams(+list), det).
type_modes(git, git(+any, +any), det).
type_modes(ordsets, ord_symdiff(+any, +any, ?(any)), det).
type_modes(ordsets, ord_union(+any, +any, -any, -any), det).
type_modes(ordsets, ord_union(+any, +any, ?(any)), det).
type_modes(ordsets, ord_union(+any, -any), det).
type_modes(ordsets, ord_subtract(+any, +any, -any), det).
type_modes(ordsets, ord_subset(+any, +any), semidet).
type_modes(ordsets, ord_memberchk(+any, +any), semidet).
type_modes(ordsets, ord_selectchk(+any, ?(any), ?(any)), semidet).
type_modes(ordsets, ord_del_element(+any, +any, -any), det).
type_modes(ordsets, ord_add_element(+any, +any, ?(any)), det).
type_modes(ordsets, ord_intersection(+any, +any, ?(any), ?(any)), det).
type_modes(ordsets, ord_intersection(+any, +any, -any), det).
type_modes(ordsets, ord_intersection(+any, -any), unknown).
type_modes(ordsets, ord_intersect(+any, +any, -any), unknown).
type_modes(ordsets, ord_disjoint(+any, +any), semidet).
type_modes(ordsets, ord_intersect(+any, +any), semidet).
type_modes(ordsets, list_to_ord_set(+any, -any), det).
type_modes(ordsets, ord_seteq(+any, +any), semidet).
type_modes(ordsets, ord_empty(?(any)), semidet).
type_modes(ordsets, is_ordset(@any), semidet).
type_modes(time, call_with_time_limit(+any, :(any)), semidet).
type_modes(time, current_alarm(?(any), :(any), ?(any), ?(any)), nondet).
type_modes(time, remove_alarm(+any), det).
type_modes(time, uninstall_alarm(+any), det).
type_modes(time, install_alarm(+any, +any), det).
type_modes(time, install_alarm(+any), det).
type_modes(time, alarm_at(+any, :(any), -any, +any), det).
type_modes(time, alarm_at(+any, :(any), -any), det).
type_modes(time, alarm(+any, :(any), -any, +any), det).
type_modes(time, alarm(+any, :(any), -any), det).
type_modes(make, prolog:make_hook(+any, +any), semidet).
type_modes(make, reload_file(?(any)), unknown).
type_modes(make, make, unknown).
type_modes(http_server_files, serve_files_in_directory(+any, +any), unknown).
type_modes(http_sgml_plugin, merge_options(+any, +any, -any), unknown).
type_modes(http_sgml_plugin, default_markup_type(+any, -any), unknown).
type_modes(sicstus_terms, term_variables_bag(+any, -any), det).
type_modes(rbtrees, is_rbtree(?(any)), semidet).
type_modes(rbtrees, rb_size(+any, -any), det).
type_modes(rbtrees, ord_list_to_rbtree(+any, -any), det).
type_modes(rbtrees, list_to_rbtree(+any, -any), det).
type_modes(rbtrees, rb_partial_map(+any, +any, :(any), -any), unknown).
type_modes(rbtrees, rb_clone(+any, -any, -any), unknown).
type_modes(rbtrees, rb_map(+any, :(any), -any), semidet).
type_modes(rbtrees, rb_map(+any, :(any)), semidet).
type_modes(rbtrees, rb_visit(+any, -any), unknown).
type_modes(rbtrees, rb_del_max(+any, -any, -any, -any), unknown).
type_modes(rbtrees, rb_del_min(+any, -any, -any, -any), unknown).
type_modes(rbtrees, rb_delete(+any, +any, -any, -any), unknown).
type_modes(rbtrees, rb_delete(+any, +any, -any), unknown).
type_modes(rbtrees, rb_insert_new(+any, +any, ?(any), -any), semidet).
type_modes(rbtrees, rb_insert(+any, +any, ?(any), -any), det).
type_modes(rbtrees, rb_lookupall(+any, -any, +any), unknown).
type_modes(rbtrees, rb_in(?(any), ?(any), +any), nondet).
type_modes(rbtrees, rb_apply(+any, +any, :(any), -any), semidet).
type_modes(rbtrees, rb_update(+any, +any, ?(any), +any, -any), semidet).
type_modes(rbtrees, rb_update(+any, +any, +any, -any), semidet).
type_modes(rbtrees, rb_previous(+any, +any, -any, -any), semidet).
type_modes(rbtrees, rb_next(+any, +any, -any, -any), semidet).
type_modes(rbtrees, rb_max(+any, -any, -any), semidet).
type_modes(rbtrees, rb_min(+any, -any, -any), semidet).
type_modes(rbtrees, rb_lookup(+any, -any, +any), semidet).
type_modes(rbtrees, rb_empty(?(any)), semidet).
type_modes(rdf_persistency, time_stamp(-any), unknown).
type_modes(rdf_persistency, mkdir(+any), unknown).
type_modes(rdf_persistency, load_prefixes(+any), det).
type_modes(rdf_persistency, reindex_db(+any, +any), unknown).
type_modes(rdf_persistency, url_to_filename(-any, +any), det).
type_modes(rdf_persistency, url_to_filename(+any, -any), det).
type_modes(rdf_persistency, rdf_db_to_file(-any, +any), det).
type_modes(rdf_persistency, rdf_db_to_file(+any, -any), det).
type_modes(rdf_persistency, rdf_snapshot_file(-any, -any), nondet).
type_modes(rdf_persistency, rdf_snapshot_file(+any, -any), semidet).
type_modes(rdf_persistency, rdf_journal_file(-any, -any), nondet).
type_modes(rdf_persistency, rdf_journal_file(+any, -any), semidet).
type_modes(rdf_persistency, db_files(-any, -any, +any), unknown).
type_modes(rdf_persistency, db_files(-any, +any, -any), unknown).
type_modes(rdf_persistency, db_files(+any, -any, -any), unknown).
type_modes(rdf_persistency, dir_levels(+any, +any, ?(any), ?(any)), det).
type_modes(rdf_persistency, unlock_db(+any, +any), det).
type_modes(rdf_persistency, unlock_db(+any), det).
type_modes(rdf_persistency, lock_db(+any), unknown).
type_modes(rdf_persistency, delete_db(+any), unknown).
type_modes(rdf_persistency, create_db(+any), unknown).
type_modes(rdf_persistency, close_journals, unknown).
type_modes(rdf_persistency, close_journal(+any), det).
type_modes(rdf_persistency, sync_journal(+any, +any), unknown).
type_modes(rdf_persistency, limit_fd_pool, det).
type_modes(rdf_persistency, journal_fd(+any, -any), det).
type_modes(rdf_persistency, sync_loaded_graphs(+any), unknown).
type_modes(rdf_persistency, end_transactions(+list(atom:id)), det).
type_modes(rdf_persistency, next_transaction_id(+any, -any), det).
type_modes(rdf_persistency, open_transaction(+any, +any), det).
type_modes(rdf_persistency, check_nested(+any), semidet).
type_modes(rdf_persistency, monitor(+any), semidet).
type_modes(rdf_persistency, stop_monitor, det).
type_modes(rdf_persistency, start_monitor, det).
type_modes(rdf_persistency, rdf_db:property_of_graph(?(any), +any), nondet).
type_modes(rdf_persistency, rdf_persistency(+any, ?(any)), unknown).
type_modes(rdf_persistency, load_journal(+atom, +atom), det).
type_modes(rdf_persistency, attach_graph(+any, +any), det).
type_modes(rdf_persistency, scan_db_files(+any, +any, +any, +any, ?(list), ?(list)), det).
type_modes(rdf_persistency, find_dbs(+any, -any, -any, -any), det).
type_modes(rdf_persistency, concurrency(-any), unknown).
type_modes(rdf_persistency, make_goals(+any, +any, +any, +any, -any), unknown).
type_modes(rdf_persistency, load_db, det).
type_modes(rdf_persistency, rdf_flush_journals(+any), unknown).
type_modes(rdf_persistency, rdf_current_db(?(any)), unknown).
type_modes(rdf_persistency, rdf_detach_db, det).
type_modes(rdf_persistency, no_agc(:(any)), unknown).
type_modes(rdf_persistency, rdf_persistency_property(?(any)), nondet).
type_modes(rdf_persistency, rdf_attach_db_ro(+any, +any), unknown).
type_modes(rdf_persistency, rdf_attach_db(+any, +any), det).
type_modes(prolog_clause, clause_name(+any, -any), unknown).
type_modes(prolog_clause, predicate_name(:(any), -string), det).
type_modes(prolog_clause, initialization_layout(+any, ?(any), -any, -any), semidet).
type_modes(prolog_clause, ubody(+any, +any, +any, +any, -any), unknown).
type_modes(prolog_clause, unify_goal(+any, +any, +any, +any, -any), semidet).
type_modes(prolog_clause, does_not_dcg_after_binding(+any, +any), semidet).
type_modes(prolog_clause, unify_body(+any, +any, +any, +any, -any), unknown).
type_modes(prolog_clause, expand_failed(+any, +any), unknown).
type_modes(prolog_clause, unify_clause(+any, +any, +any, +any, -any), unknown).
type_modes(prolog_clause, make_varnames(+any, +any, +any, +any, -any), det).
type_modes(prolog_clause, open_source(+any, -any), semidet).
type_modes(prolog_clause, read_term_at_line(+any, +any, +any, -any, -any, -any), semidet).
type_modes(prolog_clause, unify_term(+any, +any), unknown).
type_modes(prolog_clause, clause_info(+any, -any, -any, -any, +any), semidet).
type_modes(prolog_clause, clause_info(+any, -any, -any, -any), semidet).
type_modes(prolog_autoload, autoload_step(-any, -any, +any), det).
type_modes(prolog_autoload, autoload(+any), det).
type_modes(prolog_autoload, autoload, det).
type_modes(http_parameters, truth(+any, -any), semidet).
type_modes(http_parameters, check_type2(+any, +any), semidet).
type_modes(http_parameters, check_type3(+any, +any, -any), semidet).
type_modes(http_parameters, http_convert_parameter(+any, +any, +any, -any), det).
type_modes(http_parameters, http_convert_parameters(+any, ?(any), :(any)), det).
type_modes(http_parameters, http_convert_parameters(+any, ?(any)), det).
type_modes(http_parameters, fill_parameters(+any, +any, +any), unknown).
type_modes(http_parameters, posted_form(+any, -any), det).
type_modes(http_parameters, http_parameters(+any, ?(any), :(any)), det).
type_modes(http_parameters, http_parameters(+any, ?(any)), det).
type_modes(pldoc_colours, fragment_hierarchy(+any, -any), det).
type_modes(pldoc_colours, colour_fragments(+any, -list), det).
type_modes(process, process_group_kill(+any, +any), det).
type_modes(process, process_group_kill(+any), det).
type_modes(process, process_kill(+any, +any), det).
type_modes(process, process_kill(+any), det).
type_modes(process, process_wait(+any, -any, +any), det).
type_modes(process, process_wait(+any, -any), det).
type_modes(process, process_release(+any), unknown).
type_modes(process, is_process(+any), semidet).
type_modes(process, process_id(+any, -any), det).
type_modes(process, process_id(-any), det).
type_modes(process, map_arg(+any, -any), det).
type_modes(process, process_create(+any, +list, +any), det).
type_modes(clpfd_gcc_num, blocked(+any), semidet).
type_modes(clpfd_relation, blocked(+any), semidet).
type_modes(base64, decode_url(-any, ?(list), ?(list)), unknown).
type_modes(base64, base64url(-any, ?(list), ?(list)), det).
type_modes(base64, base64url(+any, ?(list), ?(list)), det).
type_modes(base64, base64(-any, ?(list), ?(list)), det).
type_modes(base64, base64(+any, ?(list), ?(list)), det).
type_modes(base64, base64url(-any, +any), det).
type_modes(base64, base64url(+any, -any), det).
type_modes(base64, base64(-any, +any), det).
type_modes(base64, base64(+any, -any), det).
type_modes(http_json, reply_json_dict(+any, +any), det).
type_modes(http_json, reply_json_dict(+any), det).
type_modes(http_json, reply_json(+any, +any), det).
type_modes(http_json, reply_json(+any), det).
type_modes(http_json, http_read_json_dict(+any, -any, +any), det).
type_modes(http_json, http_read_json_dict(+any, -any), det).
type_modes(http_json, http_read_json(+any, -any, +any), det).
type_modes(http_json, http_read_json(+any, -any), det).
type_modes(http_json, http:post_data_hook(+any, +stream, +any), semidet).
type_modes(http_json, json_type(?(any)), semidet).
type_modes(http_json, http_client:http_convert_data(+any, +any, -any, +any), unknown).
type_modes(pure_input, stream_to_lazy_list(+any, -any), det).
type_modes(pure_input, lazy_list_character_count(-any, ?(list), ?(list)), unknown).
type_modes(pure_input, lazy_list_location(-any, ?(list), ?(list)), det).
type_modes(pure_input, syntax_error(+any, ?(list), ?(list)), unknown).
type_modes(pure_input, phrase_from_stream(:(any), +any), unknown).
type_modes(pure_input, phrase_from_file(:(any), +any, +any), nondet).
type_modes(pure_input, phrase_from_file(:(any), +any), nondet).
type_modes(pldoc_search, apropos_match(+any, +any), semidet).
type_modes(pldoc_search, doc_category(?(any), ?(any), ?(any)), nondet).
type_modes(pldoc_search, object_summary(?(any), ?(any), ?(any), ?(any)), nondet).
type_modes(pldoc_search, search_spec(-any, ?(list), ?(list)), det).
type_modes(pldoc_search, exec_search(+any, -any, +any), nondet).
type_modes(pldoc_search, optimise_search(+any, -any), unknown).
type_modes(pldoc_search, matching_object(+any, -any, +any), nondet).
type_modes(pldoc_search, search_doc(+any, -list, +any), det).
type_modes(pldoc_search, matches(+any, +any, +any, ?(list), ?(list)), det).
type_modes(pldoc_search, matching_object_table(+any, +any, ?(list), ?(list)), det).
type_modes(pldoc_search, search_reply(+any, +any, ?(list), ?(list)), det).
type_modes(pldoc_search, search_field(+any, ?(list), ?(list)), det).
type_modes(pldoc_search, search_form(+any, ?(list), ?(list)), unknown).
type_modes(http_ax, ax_form_attributes(+any, -any), det).
type_modes(http_ax, ax_alias(?(any), ?(any)), nondet).
type_modes(http_ax, http_ax_attributes(+any, -any), det).
type_modes(pldoc_files, copy_resources(+any, +any), unknown).
type_modes(pldoc_files, blocked(+any), semidet).
type_modes(pldoc_files, prolog_file_in_dir(+any, -any, +any), nondet).
type_modes(pldoc_files, ensure_dir(+any, +any), det).
type_modes(pldoc_files, ensure_slash(+any, -any), det).
type_modes(pldoc_files, doc_extension(+any, -any), det).
type_modes(pldoc_files, document_file(+any, -any, +any), semidet).
type_modes(pldoc_files, file_map(+any, -any), unknown).
type_modes(pldoc_files, main_option(?(any)), unknown).
type_modes(pldoc_files, doc_target(+any, -any, +any), semidet).
type_modes(pldoc_files, generate(+any, +any), det).
type_modes(pldoc_files, doc_save(+any, +any), unknown).
type_modes(http_proxy, socket:try_proxy(+any, +any, -any, -any), unknown).
type_modes(test_util_iso, test(+any, +any), det).
type_modes(test_util_iso, test(+any), det).
type_modes(http_cors, cors_enable(+any, +any), det).
type_modes(http_cors, cors_enable, det).
type_modes(pldoc_latex, load_urldefs(+any), unknown).
type_modes(pldoc_latex, load_urldefs, unknown).
type_modes(pldoc_latex, urldef_name(?(any), ?(any)), unknown).
type_modes(pldoc_latex, identifier(+any), semidet).
type_modes(pldoc_latex, max_nl(?(any), ?(any), ?(any), ?(any)), unknown).
type_modes(pldoc_latex, print_latex(+any, +atomic), det).
type_modes(pldoc_latex, print_latex_tokens(+any, +any), unknown).
type_modes(pldoc_latex, latex_summary(+any), unknown).
type_modes(pldoc_latex, latex_table(?(any), ?(any), ?(list), ?(list)), det).
type_modes(pldoc_latex, termitem(+any, +any, +any, ?(list), ?(list)), det).
type_modes(pldoc_latex, term(+any, +any, +any, ?(list), ?(list)), det).
type_modes(pldoc_latex, pred_attributes(+any, -any), det).
type_modes(pldoc_latex, pred_head(+any, ?(any)), det).
type_modes(pldoc_latex, anon_vars(+any), det).
type_modes(pldoc_latex, pred_dt(+any, +any, ?(any), ?(list), ?(list)), det).
type_modes(pldoc_latex, need_mode(+atom, +list, -list, ?(list), ?(list)), det).
type_modes(pldoc_latex, objects(+list, +any, ?(list), ?(list)), det).
type_modes(pldoc_latex, file_title(+list, +any, +any, ?(list), ?(list)), det).
type_modes(pldoc_latex, file_header(+any, +any, ?(list), ?(list)), det).
type_modes(pldoc_latex, args(+list), det).
type_modes(pldoc_latex, tag(+any, +list, ?(list), ?(list)), det).
type_modes(pldoc_latex, tags(+list(_)), det).
type_modes(pldoc_latex, predref(+any, ?(list), ?(list)), det).
type_modes(pldoc_latex, file(+any, +any, ?(list), ?(list)), det).
type_modes(pldoc_latex, include(+any, +any, +any, ?(list), ?(list)), det).
type_modes(pldoc_latex, delete_unsafe_label_chars(+any, -any), unknown).
type_modes(pldoc_latex, latex_section(+any, +any, +any, ?(list), ?(list)), det).
type_modes(pldoc_latex, short_list(+any), semidet).
type_modes(pldoc_latex, latex_arg(+any, ?(list), ?(list)), unknown).
type_modes(pldoc_latex, latex_arguments(+list, ?(list), ?(list)), det).
type_modes(pldoc_latex, latex_special(?(any), ?(any), ?(list), ?(list)), semidet).
type_modes(pldoc_latex, latex_for_predicates(+list, +any, +any), det).
type_modes(pldoc_latex, latex_for_wiki_file(+any, +any, +any), det).
type_modes(pldoc_latex, latex_tokens_for_file(+any, +any, ?(list), ?(list)), unknown).
type_modes(pldoc_latex, latex_for_file(+any, +any, +any), det).
type_modes(pldoc_latex, doc_latex(+any, +any, +any), det).
type_modes(rdf_db, rdf_atom_md5(+any, +any, -any), det).
type_modes(rdf_db, rdf_debug(+any), det).
type_modes(rdf_db, rdf_estimate_complexity(?(any), ?(any), ?(any), -any), unknown).
type_modes(rdf_db, rdf_generation(-any), det).
type_modes(rdf_db, rdf_md5(+any, -any), det).
type_modes(rdf_db, rdf_set(+any), det).
type_modes(rdf_db, rdf_version(-any), det).
type_modes(rdf_db, rdf_statistics_literal_map(+any, -any), unknown).
type_modes(rdf_db, rdf_keys_in_literal_map(+any, +any, -any), det).
type_modes(rdf_db, rdf_find_literal_map(+any, +any, -any), det).
type_modes(rdf_db, rdf_delete_literal_map(+any, +any, +any), det).
type_modes(rdf_db, rdf_delete_literal_map(+any, +any), det).
type_modes(rdf_db, rdf_insert_literal_map(+any, +any, +any, -any), det).
type_modes(rdf_db, rdf_insert_literal_map(+any, +any, +any), det).
type_modes(rdf_db, rdf_reset_literal_map(+any), det).
type_modes(rdf_db, rdf_destroy_literal_map(+any), det).
type_modes(rdf_db, rdf_new_literal_map(-any), det).
type_modes(rdf_db, rdf_url_namespace(+any, -any), unknown).
type_modes(rdf_db, rdf_split_url(-any, -any, +any), det).
type_modes(rdf_db, rdf_split_url(+any, +any, -any), det).
type_modes(rdf_db, rdf_match_label(+any, +any, +any), semidet).
type_modes(rdf_db, rdf_compare(-any, +any, +any), det).
type_modes(rdf_db, rdf_value(+any, +any, -any, +any), unknown).
type_modes(rdf_db, rdf_write_id(+any, +any), det).
type_modes(rdf_db, rdf_id(+any, +any, -any), unknown).
type_modes(rdf_db, rdf_save_list(+any, +any, +any, +any, +any), unknown).
type_modes(rdf_db, rdf_collection(+any), semidet).
type_modes(rdf_db, save_xml_literal(+any, +any, +any, +any, +any), det).
type_modes(rdf_db, save_body_literal(+any, +any, +any, +any, +any, +any), unknown).
type_modes(rdf_db, save_attributes(+any, +any, +any, +any), unknown).
type_modes(rdf_db, simple_literal_attributes(+any, -any, -any), unknown).
type_modes(rdf_db, duplicate_attributes(+any, -any, -any), unknown).
type_modes(rdf_db, split_attributes(+any, -any, -any, ?(any)), unknown).
type_modes(rdf_db, save_attributes(+any, +any, +any, +any, +any, +any), unknown).
type_modes(rdf_db, save_about(+any, +any, +any), det).
type_modes(rdf_db, rdf_save_subject(+stream, +resource, +any, +list(_=_), +int, +any), det).
type_modes(rdf_db, rdf_save_subject(+stream, +resource, +any, +int, +any), det).
type_modes(rdf_db, rdf_save_subject(+any, +resource, +any), det).
type_modes(rdf_db, rdf_save_non_anon_subject(+any, +any, +any), unknown).
type_modes(rdf_db, rdf_save_footer(?(stream)), det).
type_modes(rdf_db, used_namespace_entities(-any, ?(any)), det).
type_modes(rdf_db, rdf_graph_prefixes(?(any), -ord_set, :(any)), det).
type_modes(rdf_db, rdf_graph_prefixes(?(any), -ord_set), det).
type_modes(rdf_db, header_namespaces(?(any), -any), unknown).
type_modes(rdf_db, xml_escape_parameter_entity(+any, -any), det).
type_modes(rdf_db, nsmap(+any, -list(id=uri)), det).
type_modes(rdf_db, rdf_save_header(+any, +any), unknown).
type_modes(rdf_db, rdf_save(+any, :(any)), det).
type_modes(rdf_db, rdf_save(+any), det).
type_modes(rdf_db, rdf_reset_db, unknown).
type_modes(rdf_db, assert_triples(+any, +any), unknown).
type_modes(rdf_db, save_cache(+any, +any), det).
type_modes(rdf_db, rdf_set_graph(+any, +any), det).
type_modes(rdf_db, rdf_graph_property(?(any), ?(any)), nondet).
type_modes(rdf_db, rdf_make, unknown).
type_modes(rdf_db, rdf_source(?(any)), unknown).
type_modes(rdf_db, rdf_source(?(any), ?(any)), nondet).
type_modes(rdf_db, rdf_graph(?(any)), nondet).
type_modes(rdf_db, rdf_create_graph(+any), det).
type_modes(rdf_db, rdf_unload_graph(+any), det).
type_modes(rdf_db, rdf_unload(+any), det).
type_modes(rdf_db, report_loaded(+any, +any, +any, +any, +any, +any), unknown).
type_modes(rdf_db, rdf_load_stream(+any, +any, :(any)), unknown).
type_modes(rdf_db, rdf_file_encoding(+any, -any), semidet).
type_modes(rdf_db, rdf_file_type(+any, -any), semidet).
type_modes(rdf_db, is_url(@any, -any, -any), semidet).
type_modes(rdf_db, storage_open(+any, +any, -any, -any), unknown).
type_modes(rdf_db, load_graph(+any, -any, +any), det).
type_modes(rdf_db, source_url(+any, -any, -any), det).
type_modes(rdf_db, rdf_open_input(+any, +any, +any, -any, -any, -any, -any, +any), unknown).
type_modes(rdf_db, rdf_load_file(+any, +any, +any, +any, +any, +any, +any), det).
type_modes(rdf_db, rdf_end_load(+any), det).
type_modes(rdf_db, rdf_start_load(+any, -any), det).
type_modes(rdf_db, rdf_load(+any, :(any)), det).
type_modes(rdf_db, rdf_load(+any), det).
type_modes(rdf_db, rdf_load_db(+any), det).
type_modes(rdf_db, check_loaded_cache(+any, +any, +any), det).
type_modes(rdf_db, rdf_load_db_no_admin(+any, +any, -any), det).
type_modes(rdf_db, rdf_save_db(+any, +any), det).
type_modes(rdf_db, rdf_save_db(+any), det).
type_modes(rdf_db, rdf_update_duplicates_thread, unknown).
type_modes(rdf_db, rdf_update_duplicates, det).
type_modes(rdf_db, rdf_warm_indexes(+any), det).
type_modes(rdf_db, rdf_warm_indexes, unknown).
type_modes(rdf_db, monitor_mask(?(any), ?(any)), unknown).
type_modes(rdf_db, rdf_monitor(:(any), +any), unknown).
type_modes(rdf_db, rdf_active_transaction(?(any)), nondet).
type_modes(rdf_db, rdf_transaction(:(any), +any, +any), semidet).
type_modes(rdf_db, rdf_transaction(:(any), +any), semidet).
type_modes(rdf_db, rdf_transaction(:(any)), semidet).
type_modes(rdf_db, rdf_current_snapshot(?(any)), nondet).
type_modes(rdf_db, rdf_delete_snapshot(+any), det).
type_modes(rdf_db, rdf_snapshot(-any), det).
type_modes(rdf_db, rdf_set_predicate(+any, +any), det).
type_modes(rdf_db, rdf_predicate_property(?(any), ?(any)), unknown).
type_modes(rdf_db, rdf_current_predicate(?(any)), nondet).
type_modes(rdf_db, rdf_statistics(?(any)), nondet).
type_modes(rdf_db, consider_gc(+any), semidet).
type_modes(rdf_db, has_garbage, semidet).
type_modes(rdf_db, rdf_gc, det).
type_modes(rdf_db, rdf_gc(-any), det).
type_modes(rdf_db, rdf_gc_loop, unknown).
type_modes(rdf_db, rdf_create_gc_thread, unknown).
type_modes(rdf_db, rdf_source_location(+any, -any), nondet).
type_modes(rdf_db, rdf_literal_value(+any, -any), semidet).
type_modes(rdf_db, rdf_current_literal(-any), nondet).
type_modes(rdf_db, rdf_is_literal(@any), semidet).
type_modes(rdf_db, rdf_is_resource(@any), semidet).
type_modes(rdf_db, rdf_is_bnode(+any), unknown).
type_modes(rdf_db, rdf_bnode(-any), unknown).
type_modes(rdf_db, rdf_node(-any), unknown).
type_modes(rdf_db, rdf_member_property(?(any), ?(any)), unknown).
type_modes(rdf_db, rdf_update(+any, +any, +any, +any, +any), det).
type_modes(rdf_db, rdf_update(+any, +any, +any, +any), det).
type_modes(rdf_db, rdf_retractall(?(any), ?(any), ?(any), ?(any)), det).
type_modes(rdf_db, rdf_retractall(?(any), ?(any), ?(any)), det).
type_modes(rdf_db, rdf_assert(+any, +any, +any, +any), det).
type_modes(rdf_db, rdf_assert(+any, +any, +any), det).
type_modes(rdf_db, rdf_resource(?(any)), nondet).
type_modes(rdf_db, rdf_subject(?(any)), nondet).
type_modes(rdf_db, rdf_reachable(?(any), +any, ?(any), +any, -any), nondet).
type_modes(rdf_db, rdf_reachable(?(any), +any, ?(any)), nondet).
type_modes(rdf_db, rdf_has(?(any), +any, ?(any), -any), nondet).
type_modes(rdf_db, rdf_has(?(any), +any, ?(any)), nondet).
type_modes(rdf_db, rdf(?(any), ?(any), ?(any), ?(any)), nondet).
type_modes(rdf_db, rdf(?(any), ?(any), ?(any)), nondet).
type_modes(rdf_db, lang_matches(+any, +any), semidet).
type_modes(rdf_db, lang_equal(+any, +any), semidet).
type_modes(rdf_db, rdf_equal(?(any), ?(any)), unknown).
type_modes(rdf_db, mk_global(+any, -any), unknown).
type_modes(rdf_db, rdf_meta(+any), unknown).
type_modes(rdf_db, rdf_global_graph(+any, -any), det).
type_modes(rdf_db, rdf_global_term(+any, -any), det).
type_modes(rdf_db, rdf_global_object(-any, +any), semidet).
type_modes(rdf_db, rdf_global_object(+any, -any), semidet).
type_modes(rdf_db, rdf_global_id(?(any), ?(any)), det).
type_modes(rdf_db, register_file_ns(+list(pair)), det).
type_modes(rdf_db, rdf_register_ns(?(any), ?(any), +any), det).
type_modes(rdf_db, rdf_register_ns(?(any), ?(any)), det).
type_modes(rdf_db, rdf_current_ns(?(any), ?(any)), nondet).
type_modes(rdf_db, rdf_register_prefix(+any, +any, +any), det).
type_modes(rdf_db, rdf_register_prefix(+any, +any), det).
type_modes(rdf_db, ns(?(any), ?(any)), nondet).
type_modes(rdf_db, rdf_current_prefix(?(any), ?(any)), nondet).
type_modes(pldoc_index, source_directory(-any), nondet).
type_modes(pldoc_index, source_directory(+any), semidet).
type_modes(pldoc_index, places_menu(?(any), ?(list), ?(list)), det).
type_modes(pldoc_index, version(?(list), ?(list)), det).
type_modes(pldoc_index, doc_links(+any, +any, ?(list), ?(list)), det).
type_modes(pldoc_index, object_summary(+any, +any, +any, ?(list), ?(list)), det).
type_modes(pldoc_index, object_summaries(+any, +any, +any, ?(list), ?(list)), det).
type_modes(pldoc_index, ensure_slash_start(+any, -any), det).
type_modes(pldoc_index, doc_file_href(+any, -any), det).
type_modes(pldoc_index, doc_file_href(+any, -any, +any), det).
type_modes(pldoc_index, file_index_header(+any, +any, ?(list), ?(list)), det).
type_modes(pldoc_index, file_index(+any, +any, ?(list), ?(list)), det).
type_modes(pldoc_index, file_indices(+any, +any, ?(list), ?(list)), det).
type_modes(pldoc_index, wiki_file_type(+any, -any), nondet).
type_modes(pldoc_index, wiki_file(+any, +any, +any, ?(list), ?(list)), semidet).
type_modes(pldoc_index, dir_footer(+any, +any, ?(list), ?(list)), det).
type_modes(pldoc_index, dir_header(+any, +any, ?(list), ?(list)), det).
type_modes(pldoc_index, subdir_links(+any, +any, ?(list), ?(list)), det).
type_modes(pldoc_index, dir_source_files(+any, -any, +any), det).
type_modes(pldoc_index, dir_index(+any, +any, ?(list), ?(list)), unknown).
type_modes(pldoc_index, doc_for_dir(+any, +any), det).
type_modes(iso_predicates, iso_builtin_function(?(callable)), nondet).
type_modes(iso_predicates, iso_builtin_predicate(?(callable)), nondet).
type_modes(cql, cql_normalize_name(+any, +any, -any), unknown).
type_modes(cql, sql_gripe_hook(+any, +any, +any), unknown).
type_modes(cql, cql_history_attribute(+any, +any, +any), unknown).
type_modes(cql, cql_event_notification_table(+any, +any), unknown).
type_modes(cql, cql_update_history_hook(+any, +any, +any, +any, +any, +any, +any, +any, +any, +any, +any, +any, +any), unknown).
type_modes(cql, database_key(?(atom), ?(atom), ?(atom), ?(list), ?(any)), unknown).
type_modes(cql, database_identity(?(atom), ?(atom), ?(atom)), unknown).
type_modes(cql, attribute_domain(+any, +any, +any, -any), unknown).
type_modes(cql, database_constraint(?(atom), ?(atom), ?(atom), ?(any)), nondet).
type_modes(cql, routine_return_type(?(atom), ?(atom), ?(any)), unknown).
type_modes(cql, database_attribute(?(atom), ?(any)), nondet).
type_modes(cql, database_attribute(?(table/view), ?(atom), ?(atom), ?(atom), ?(atom), ?(allows_nulls(true/false)), ?(is_identity(true/false)), ?(any)), nondet).
type_modes(cql, primary_key_column_name(+any, +any, -any), unknown).
type_modes(cql, odbc_data_type(+any, +any, +any, ?(any)), unknown).
type_modes(cql, dbms(+any, -any), unknown).
type_modes(cql, statistic_monitored_attribute_change(+any, +any, +any, +any, +any), unknown).
type_modes(cql, statistic_monitored_attribute(+any, +any, +any), unknown).
type_modes(cql, cql_suggest_indices(+any, +any), det).
type_modes(cql, on_to_where(+any, +any, -any), unknown).
type_modes(cql, cql_show(:(any), +any), unknown).
type_modes(cql, odbc_data_type_and_input(+any, +any, +any, +any, -any, -any), unknown).
type_modes(cql, inline(+atom, +list, -atom, -list), unknown).
type_modes(cql, odbc_prepare_and_execute(+any, +any, +any, +any, +any, +any, +any, -any), det).
type_modes(cql, cql_temporary_column_name(?(any), ?(any), ?(any), ?(any)), unknown).
type_modes(cql, execute_on_connection(+any, +any, +any), unknown).
type_modes(cql, cql_runtime(+any, +any, +any, +any, +any, +any, +any), unknown).
type_modes(cql, cql_goal_expansion(?(any), ?(any), ?(any)), unknown).
type_modes(cql, cql_get_module_default_schema(+any, ?(any)), unknown).
type_modes(cql, cql_set_module_default_schema(+any), unknown).
type_modes(http_pwp, pwp_context(+any, -any), nondet).
type_modes(http_pwp, default_mime_type(+any, +any), det).
type_modes(http_pwp, copy_http_equiv(+any), det).
type_modes(http_pwp, reply_pwp_page(:(any), +any, +any), unknown).
type_modes(http_pwp, pwp_handler(+any, +any), unknown).
type_modes(prolog_http_load, ensure_extension(+any, +any, -any), unknown).
type_modes(prolog_http_load, ssl_verify(+any, +any, +any, +any, +any), unknown).
type_modes(prolog_http_load, user:prolog_load_file(+any, +any), unknown).
type_modes(prolog_explain, explain_predicate(:(any), -any), det).
type_modes(prolog_explain, known_predicate(:(any)), unknown).
type_modes(prolog_explain, explain(@any, -any), nondet).
type_modes(prolog_explain, explain(@any), det).
type_modes(clpfd_aux, blocked(+any), semidet).
type_modes(files_ex, delete_directory_contents(+any), det).
type_modes(files_ex, delete_directory_and_contents(+any), det).
type_modes(files_ex, copy_directory(+any, +any), det).
type_modes(files_ex, make_directory_path(+any), det).
type_modes(files_ex, copy_file(?(any), ?(any)), det).
type_modes(files_ex, directory_file_path(?(any), ?(any), +any), det).
type_modes(files_ex, directory_file_path(+any, +any, -any), det).
type_modes(files_ex, relative_file_name(-atom, +atom, +atom), det).
type_modes(files_ex, relative_file_name(+atom, +atom, -atom), det).
type_modes(files_ex, link_file(+any, +any, +any), det).
type_modes(files_ex, set_time_file(+any, -any, +any), det).
type_modes(check_installation, archive_features, unknown).
type_modes(check_installation, run_collect_messages(?(any), ?(any), ?(any)), det).
type_modes(check_installation, run_silent(:(any), +any), semidet).
type_modes(check_installation, check_features(+any), semidet).
type_modes(check_installation, test_component(+any), semidet).
type_modes(check_installation, check_installation, unknown).
type_modes(check_installation, component(?(any), -any), nondet).
type_modes(pldoc_modes, compile_clause(:(any), +any), det).
type_modes(pldoc_modes, modes_to_predicate_indicators(+list, -list), det).
type_modes(pldoc_modes, mode_indicator(?(atom)), nondet).
type_modes(pldoc_modes, is_mode(@any), semidet).
type_modes(pldoc_modes, mode(:(any), ?(any)), nondet).
type_modes(pldoc_modes, compile_mode(+any, -any), det).
type_modes(pldoc_modes, extract_varnames(+any, -any, ?(any)), det).
type_modes(pldoc_modes, public_operators(+any, -list(op(_, _, _))), det).
type_modes(pldoc_modes, prepare_module_operators, det).
type_modes(pldoc_modes, mode_syntax_error(+any), det).
type_modes(pldoc_modes, modes(+codes, +any, +any, -any), det).
type_modes(pldoc_modes, mode_lines(+any, -codes, ?(codes), -any), det).
type_modes(pldoc_modes, process_modes(+lines, +any, +any, -list, -list(atom), -lines), det).
type_modes(thread_util, init_pce, unknown).
type_modes(thread_util, tprofile(+any), det).
type_modes(thread_util, tnodebug(+any), det).
type_modes(thread_util, tnodebug, det).
type_modes(thread_util, tdebug(+any), det).
type_modes(thread_util, tdebug, det).
type_modes(thread_util, tspy(:(any), +any), det).
type_modes(thread_util, tspy(:(any)), det).
type_modes(thread_util, attach_console, det).
type_modes(thread_util, thread_has_console, semidet).
type_modes(thread_util, interactor, unknown).
type_modes(thread_util, join_threads, unknown).
type_modes(thread_util, threads, unknown).
type_modes(thread_httpd, create_pool(+any), unknown).
type_modes(thread_httpd, http_spawn(:(any), +any), det).
type_modes(thread_httpd, close_connection(+any, +any, +any), unknown).
type_modes(thread_httpd, http_close_connection(+any), unknown).
type_modes(thread_httpd, http_process(?(any), ?(any), +any), unknown).
type_modes(thread_httpd, http_requeue(+any), unknown).
type_modes(thread_httpd, recreate_worker(+any, +any), semidet).
type_modes(thread_httpd, done_worker, unknown).
type_modes(thread_httpd, check_keep_alive_connection(+any, +any, +any, +any, +any), semidet).
type_modes(thread_httpd, open_client(+any, +any, -any, -any, -any, +any, -any), semidet).
type_modes(thread_httpd, http_worker(+any), unknown).
type_modes(thread_httpd, resize_pool(+any, +any), det).
type_modes(thread_httpd, create_workers(+any), unknown).
type_modes(thread_httpd, http:schedule_workers(+dict), semidet).
type_modes(thread_httpd, http_enough_workers(+any, +any, +any), det).
type_modes(thread_httpd, http_stop_server(+any, +any), unknown).
type_modes(thread_httpd, close_server_socket(+any), unknown).
type_modes(thread_httpd, accept_server(:(any), +any), unknown).
type_modes(thread_httpd, http_current_worker(?(any), ?(any)), nondet).
type_modes(thread_httpd, http_add_worker(+any, +any), det).
type_modes(thread_httpd, http_workers(+any, +int), det).
type_modes(thread_httpd, http_workers(+any, -any), det).
type_modes(thread_httpd, http_server_property(?(any), ?(any)), nondet).
type_modes(thread_httpd, http_current_server(:(any), ?(any)), nondet).
type_modes(thread_httpd, create_server(:(any), +any, +any), det).
type_modes(thread_httpd, make_addr_atom(+any, +any, -any), det).
type_modes(thread_httpd, make_socket(?(any), :(any), -any), det).
type_modes(thread_httpd, http_server(:(any), :(any)), det).
type_modes(pldoc_pack, doc_pack(+any), unknown).
type_modes(pldoc_pack, list_pack(+any, +any, +any), unknown).
type_modes(pldoc_pack, pack_table(+any, ?(list), ?(list)), det).
type_modes(pldoc_pack, pldoc_pack(+any), unknown).
type_modes(udp_broadcast, udp_broadcast_initialize(+any, +any), semidet).
type_modes(udp_broadcast, udp_initialize, semidet).
type_modes(udp_broadcast, udp_host_to_address(?(any), ?(any)), nondet).
type_modes(udp_broadcast, udp_broadcast_service(?(any), ?(any)), nondet).
type_modes(chr, goal_colours(+any, -any), unknown).
type_modes(chr, term_colours(+any, -any), unknown).
type_modes(chr, chr_residuals(?(list), ?(list)), det).
type_modes(chr, debug_ask_continue(-any), semidet).
type_modes(chr, debug_interact(+any, +any, -any), unknown).
type_modes(chr, debug_event(+any, +any), unknown).
type_modes(chr, call_preprocess(+any, -any), det).
type_modes(tipc, tipc_initialize, semidet).
type_modes(tipc, tipc_service_port_monitor(+any, :(any), ?(any)), det).
type_modes(tipc, tipc_service_port_monitor(+any, :(any)), det).
type_modes(tipc, tipc_service_probe(?(any), ?(any)), nondet).
type_modes(tipc, tipc_service_probe(?(any)), nondet).
type_modes(tipc, tipc_service_exists(+any), semidet).
type_modes(tipc, tipc_service_exists(+any, +any), semidet).
type_modes(tipc, tipc_canonical_address(-any, +any), det).
type_modes(tipc, tipc_send(+any, +any, +any, +any), det).
type_modes(tipc, tipc_receive(+any, -any, -any, +any), det).
type_modes(tipc, tipc_setopt(+any, +any), det).
type_modes(tipc, tipc_get_peer_name(+any, -any), det).
type_modes(tipc, tipc_get_name(+any, -any), det).
type_modes(tipc, tipc_connect(+any, +any), det).
type_modes(tipc, tipc_accept(+any, -any, -any), det).
type_modes(tipc, tipc_listen(+any, +any), det).
type_modes(tipc, tipc_bind(+any, +any, +any), det).
type_modes(tipc, tipc_open_socket(+any, -any, -any), det).
type_modes(tipc, tipc_close_socket(+any), det).
type_modes(tipc, tipc_socket(-any, +any), det).
type_modes(uuid, uuid(-any, +any), det).
type_modes(uuid, uuid(-any), det).
type_modes(coinduction, rename_clause(+any, +any, -any), det).
type_modes(coinduction, wrap_coinductive(+any, +any, -any), det).
type_modes(coinduction, coinductive(:(any)), unknown).
type_modes(coinduction, head(+any, -any), semidet).
type_modes(sicstus_system, file_exists(+any), semidet).
type_modes(sicstus_system, tmpnam(-any), unknown).
type_modes(sicstus_system, mktemp(+any, -any), det).
type_modes(sicstus_system, host_name(-any), unknown).
type_modes(sicstus_system, popen(+any, +any, ?(any)), unknown).
type_modes(sicstus_system, system(+any, -any), unknown).
type_modes(sicstus_system, system(+any), unknown).
type_modes(sicstus_system, system, unknown).
type_modes(sicstus_system, datime(-any), det).
type_modes(sicstus_system, datime(+any, -any), det).
type_modes(sicstus_system, now(-any), det).
type_modes(sicstus_system, pid(-any), unknown).
type_modes(sicstus_system, wait(+any, -any), unknown).
type_modes(sicstus_system, exec(+any, +any, -any), unknown).
type_modes(sicstus_system, environ(?(any), ?(any)), nondet).
type_modes(http_mime_plugin, http_client:http_convert_data(+any, +any, -any, +any), semidet).
type_modes(www_browser, expand_url_path(+any, -any), unknown).
type_modes(www_browser, url_path(+any, -any), nondet).
type_modes(www_browser, has_command(+any), unknown).
type_modes(www_browser, known_browser(+any, -any), unknown).
type_modes(www_browser, run_command(+any, +any, +any), unknown).
type_modes(www_browser, run_browser(+any, +any), det).
type_modes(www_browser, www_open_url(+any), unknown).
type_modes(freeze, blocked(+any), semidet).
type_modes(sparql_client, sparql_read_json_result(+any, -any), det).
type_modes(sparql_client, sub_element(+any, +any, -any, -any), unknown).
type_modes(sparql_client, variables(+any, -any), unknown).
type_modes(sparql_client, sparql_read_xml_result(+any, -any), unknown).
type_modes(sparql_client, sparql_set_server(+any), unknown).
type_modes(sparql_client, ssl_verify(+any, +any, +any, +any, +any), unknown).
type_modes(sparql_client, sparql_extra_headers(-any), unknown).
type_modes(sparql_client, sparql_query(+any, -any, +any), nondet).
type_modes(syslog, prolog:debug_print_hook(+any, +any, +any), semidet).
type_modes(syslog, closelog, det).
type_modes(syslog, syslog(+any, +any, +any), det).
type_modes(syslog, syslog(+any, +any), det).
type_modes(syslog, openlog(+atom, +list(atom), +atom), det).
type_modes(clpfd_gcc_vs, blocked(+any), semidet).
type_modes(dicts, dicts_to_compounds(?(any), +any, :(any), ?(any)), semidet).
type_modes(dicts, dicts_slice(+any, +any, -any), det).
type_modes(dicts, dicts_join(+any, +any, +any, -any), semidet).
type_modes(dicts, dicts_join(+any, +any, -any), semidet).
type_modes(dicts, dict_no_fill, det).
type_modes(dicts, dict_fill(+any, +any, +any, -any), det).
type_modes(dicts, dicts_to_same_keys(+any, :(any), -any), unknown).
type_modes(dicts, dicts_same_keys(+any, -any), semidet).
type_modes(dicts, dict_keys(+any, -any), det).
type_modes(dicts, dicts_same_tag(+any, -any), semidet).
type_modes(http_openid, xor_codes(+list(int), +list(int), -list(int)), det).
type_modes(http_openid, btwoc(-any, +any), det).
type_modes(http_openid, btwoc(+any, -any), det).
type_modes(http_openid, base64_btwoc(-any, +atom), det).
type_modes(http_openid, base64_btwoc(-any, +list(code)), det).
type_modes(http_openid, base64_btwoc(+any, -list(code)), det).
type_modes(http_openid, utf8_string(-any, ?(list), ?(list)), nondet).
type_modes(http_openid, key_values_data(-list(_-_), +list(code)), det).
type_modes(http_openid, key_values_data(+list(_-_), -list(code)), det).
type_modes(http_openid, random_bytes(+any, -any), det).
type_modes(http_openid, associate_data(-any, -any, -any, -any, +any), det).
type_modes(http_openid, expires_at(+any, -any), det).
type_modes(http_openid, shared_secret(+any, +any, +any, -list(codes)), unknown).
type_modes(http_openid, openid_associate(?(any), +any, -any, +any), semidet).
type_modes(http_openid, openid_associate(+any, -any, -any, +any), det).
type_modes(http_openid, openid_associate(?(any), ?(any), ?(any)), det).
type_modes(http_openid, signature(+any, +any, -any), unknown).
type_modes(http_openid, signed_fields(+any, -any), det).
type_modes(http_openid, trusted(+any, +any), unknown).
type_modes(http_openid, grant_login(+any, +any), det).
type_modes(http_openid, openid_grant(+any), unknown).
type_modes(http_openid, checkid_setup_server(+any, +any, +any), unknown).
type_modes(http_openid, associate_server(+any, +any, +any), unknown).
type_modes(http_openid, openid_server(+any, +any), unknown).
type_modes(http_openid, ax_store(+any), unknown).
type_modes(http_openid, ax_options(+any, +any, +any), det).
type_modes(http_openid, check_authentication(+any, +any), semidet).
type_modes(http_openid, check_obligatory_fields(+list), det).
type_modes(http_openid, signed_pairs(+any, +list(_-_), +any, -any), det).
type_modes(http_openid, openid_authenticate(+any, -url, -url, -url), semidet).
type_modes(http_openid, openid_authenticate(+any), unknown).
type_modes(http_openid, ssl_verify(+any, +any, +any, +any, +any), unknown).
type_modes(http_openid, openid_resolve(+any, -any, -any, -any, -any), unknown).
type_modes(http_openid, redirect_browser(+any, +any), unknown).
type_modes(http_openid, openid_current_host(?(any), ?(any), ?(any)), unknown).
type_modes(http_openid, openid_current_url(+any, -any), det).
type_modes(http_openid, public_url(+any, +any, -any), det).
type_modes(http_openid, openid_server(?(any), ?(any), ?(any)), nondet).
type_modes(http_openid, assert_openid(+any, +any, +any, +any), det).
type_modes(http_openid, handle_stay_signed_in(+any), unknown).
type_modes(http_openid, stay(+any), unknown).
type_modes(http_openid, openid_verify(+any, +any), unknown).
type_modes(http_openid, prelogin_button(+any, ?(list), ?(list)), det).
type_modes(http_openid, openid_login_form(+any, +any, ?(list), ?(list)), det).
type_modes(http_openid, openid_login_page(+any), det).
type_modes(http_openid, openid_xrds(?(any)), unknown).
type_modes(http_openid, openid_user(+http_request, -url, +any), det).
type_modes(http_openid, openid_logged_in(-any), semidet).
type_modes(http_openid, openid_logout(+any), det).
type_modes(http_openid, openid_login(+any), det).
type_modes(http_openid, openid_hook(+any), unknown).
type_modes(rdf_http_plugin, rdf_content_type(?(atom), ?(between(0.0, 1.0)), ?(rdf_format)), nondet).
type_modes(rdf_http_plugin, rdf_accept_header_value(?(rdf_format), -atom), det).
type_modes(rdf_http_plugin, open_envelope(+any, +any, +any, -any, ?(any)), semidet).
type_modes(rdf_http_plugin, modified_since_header(+any, -any), det).
type_modes(rdf_http_plugin, ssl_verify(+any, +any, +any, +any, +any), unknown).
type_modes(rdf_http_plugin, rdf_db:rdf_open_hook(+any, +any, +any, -any, -any, -any, -any, +any), semidet).
type_modes(rdf_http_plugin, rdf_extra_headers(-list(compound), +list), det).
type_modes(http, blocked(+any), semidet).
type_modes(listing, blocked(+any), semidet).
type_modes(ugraphs, neighbours(+any, +any, -any), det).
type_modes(ugraphs, neighbors(+any, +any, -any), det).
type_modes(ugraphs, top_sort(+any, -any, ?(any)), semidet).
type_modes(ugraphs, top_sort(+any, -any), semidet).
type_modes(ugraphs, compose(?(any), ?(any), ?(any)), unknown).
type_modes(ugraphs, transpose_ugraph(?(any), ?(any)), det).
type_modes(ugraphs, edges(+any, -any), det).
type_modes(ugraphs, graph_subtract(+any, +any, ?(any)), unknown).
type_modes(ugraphs, ugraph_union(+any, +any, ?(any)), unknown).
type_modes(ugraphs, del_vertices(+any, +any, -any), det).
type_modes(ugraphs, vertices_edges_to_ugraph(+any, +any, -any), det).
type_modes(ugraphs, vertices(+any, -any), det).
type_modes(http_cookie, http_current_cookie(?(any), ?(any), ?(any), ?(any)), nondet).
type_modes(http_cookie, current_client(?(any)), nondet).
type_modes(http_cookie, cookie_remove_all_clients, det).
type_modes(http_cookie, cookie_remove_client(+any), det).
type_modes(http_cookie, current_cookie(+any, +any, +any, -any, -any), nondet).
type_modes(http_cookie, remove_cookies(+any, +any, +any, +any, +any), det).
type_modes(http_cookie, http:update_cookies(+any, +any, +any), semidet).
type_modes(http_cookie, cookie_value(+any, -any), det).
type_modes(http_cookie, cookie(+any, +any, -any), semidet).
type_modes(http_cookie, http:write_cookies(+any, +any, +any), det).
type_modes(cql_database, current_transaction_id(-any), unknown).
type_modes(cql_database, odbc_value_to_application_value(+any, +any, +any, +any, ?(any)), unknown).
type_modes(cql_database, application_value_to_odbc_value(+any, +any, +any, +any, +any, +any, -any), unknown).
type_modes(cql_database, register_database_connection_details(+atom, +any), det).
type_modes(cql_database, maximum_deadlock_retries(?(integer)), unknown).
type_modes(cql_database, resolve_deadlock(:(any)), unknown).
type_modes(cql_database, save_database_event(+any, +any, +any, +any, +any, +any), unknown).
type_modes(cql_database, odbc_execute_with_statement_cache(+any, +any, +any, +any, +any, +any, -any), unknown).
type_modes(cql_database, odbc_cleanup_and_disconnect(+any), det).
type_modes(fastrw, fast_write_to_string(+any, -any, ?(any)), unknown).
type_modes(fastrw, fast_read(+any, -any), unknown).
type_modes(fastrw, fast_write(+any, +any), unknown).
type_modes(fastrw, fast_write(+any), unknown).
type_modes(fastrw, fast_read(-any), unknown).
type_modes(http_ssl_plugin, http:http_connection_over_proxy(+any, +any, +any, -any, +any, -any), unknown).
type_modes(http_ssl_plugin, http:open_options(?(any), ?(any)), nondet).
type_modes(http_ssl_plugin, http:http_protocol_hook(+any, +any, +any, -any, +any), semidet).
type_modes(http_ssl_plugin, thread_httpd:accept_hook(:(any), +any), semidet).
type_modes(http_ssl_plugin, thread_httpd:make_socket_hook(?(any), :(any), -any), semidet).
type_modes(http_hook, http:http_connection_over_proxy(+any, +any, +any, -any, +any, -any), unknown).
type_modes(http_hook, http:convert_parameter(+any, +any, -any), semidet).
type_modes(pldoc_wiki, limit(+any, :(any), ?(list), ?(list)), unknown).
type_modes(pldoc_wiki, tokens_no_whitespace(-list(atom), ?(list), ?(list)), nondet).
type_modes(pldoc_wiki, tokens(+any, -list, ?(list), ?(list)), nondet).
type_modes(pldoc_wiki, tokens(-list, ?(list), ?(list)), nondet).
type_modes(pldoc_wiki, peek(?(any), ?(list), ?(list)), unknown).
type_modes(pldoc_wiki, nl(?(list), ?(list)), unknown).
type_modes(pldoc_wiki, non_ws(-any, ?(any)), det).
type_modes(pldoc_wiki, ws(?(list), ?(list)), det).
type_modes(pldoc_wiki, eos(?(list), ?(list)), det).
type_modes(pldoc_wiki, strip_leading_par(+any, -any), det).
type_modes(pldoc_wiki, normalise_indentation(+any, -any), det).
type_modes(pldoc_wiki, take_line(-codes, ?(list), ?(list)), det).
type_modes(pldoc_wiki, update_linepos(+any, +any, -any), det).
type_modes(pldoc_wiki, string_update_linepos(+any, +any, -any), det).
type_modes(pldoc_wiki, take_prefix(+list(codes), +int, -int, ?(list), ?(list)), det).
type_modes(pldoc_wiki, end_of_comment(?(list), ?(list)), unknown).
type_modes(pldoc_wiki, indented_lines(+list(codes), +list(codes), -list), det).
type_modes(pldoc_wiki, skip_empty_lines(+any, -any), det).
type_modes(pldoc_wiki, summary_from_lines(+lines, -list(codes)), det).
type_modes(pldoc_wiki, pre_indent(+any, ?(list), ?(list)), det).
type_modes(pldoc_wiki, lines_code_text(+any, +any, -any), det).
type_modes(pldoc_wiki, valid_verbatim_opening(+any), semidet).
type_modes(pldoc_wiki, indented_verbatim_body(+any, +any, -any, -any), unknown).
type_modes(pldoc_wiki, tilde_fence_ext(-any, ?(list), ?(list)), semidet).
type_modes(pldoc_wiki, verbatim(+any, +any, -any, -any), det).
type_modes(pldoc_wiki, line_tokens(-list, ?(list), ?(list)), det).
type_modes(pldoc_wiki, tokenize_lines(+lines, -any), det).
type_modes(pldoc_wiki, normalise_white_space(-any, ?(list), ?(list)), det).
type_modes(pldoc_wiki, section_comment_header(+any, -any, -any), semidet).
type_modes(pldoc_wiki, autolink_file(?(any), -any), nondet).
type_modes(pldoc_wiki, autolink_extension(?(any), ?(any)), nondet).
type_modes(pldoc_wiki, prolog_symbol_char(?(any)), unknown).
type_modes(pldoc_wiki, symbol_string(-any, ?(list), ?(list)), nondet).
type_modes(pldoc_wiki, arity(-int, ?(list), ?(list)), semidet).
type_modes(pldoc_wiki, resolve_file(+any, -any, ?(any)), det).
type_modes(pldoc_wiki, file_name(-atom, -atom, ?(list), ?(list)), semidet).
type_modes(pldoc_wiki, prolog:url_expansion_hook(+any, -any, -any), semidet).
type_modes(pldoc_wiki, wiki_link(-any, +any, ?(list), ?(list)), semidet).
type_modes(pldoc_wiki, file_options(-any), det).
type_modes(pldoc_wiki, image_label(-any, ?(list), ?(list)), unknown).
type_modes(pldoc_wiki, term_face(+any, +any, +any, -any), semidet).
type_modes(pldoc_wiki, arg_list(-any), nondet).
type_modes(pldoc_wiki, emphasis_end(+any, ?(list), ?(list)), semidet).
type_modes(pldoc_wiki, emphasis_start(-any, ?(list), ?(list)), semidet).
type_modes(pldoc_wiki, emphasis_before(-any, ?(list), ?(list)), semidet).
type_modes(pldoc_wiki, emphasis_term(+any, +any, -any), det).
type_modes(pldoc_wiki, emphasis_seq(-any, +any, +any), semidet).
type_modes(pldoc_wiki, code_face(+any, +any, +any, -any), det).
type_modes(pldoc_wiki, eq_code_words(-any, ?(list), ?(list)), unknown).
type_modes(pldoc_wiki, code_words(-any, ?(list), ?(list)), unknown).
type_modes(pldoc_wiki, wiki_face_simple(-any, +any, +any), unknown).
type_modes(pldoc_wiki, prolog:doc_wiki_face(-any, +any, +any, ?(list), ?(list)), semidet).
type_modes(pldoc_wiki, prolog:doc_wiki_face(-any, +any, ?(list), ?(list)), semidet).
type_modes(pldoc_wiki, wiki_faces(-any, +any, +any, ?(list), ?(list)), nondet).
type_modes(pldoc_wiki, wiki_faces(-any, +any, ?(list), ?(list)), nondet).
type_modes(pldoc_wiki, matches(:(any), -any, -any, ?(list), ?(list)), unknown).
type_modes(pldoc_wiki, verbatim_term(?(any)), det).
type_modes(pldoc_wiki, structure_term(-any, +any, +any), det).
type_modes(pldoc_wiki, structure_term(+any, -any, -any), semidet).
type_modes(pldoc_wiki, wiki_faces(+any, +any, -any), det).
type_modes(pldoc_wiki, combine_tags(+list(tag(_, _)), -list), det).
type_modes(pldoc_wiki, tag_order(+atom, -int), semidet).
type_modes(pldoc_wiki, renamed_tag(+atom, -atom, -any), semidet).
type_modes(pldoc_wiki, tag_name(+any, -atom, -int), semidet).
type_modes(pldoc_wiki, collect_tags(+any, -any), semidet).
type_modes(pldoc_wiki, tags(+lines, -any), semidet).
type_modes(pldoc_wiki, strip_leading_ws(+any, -any), det).
type_modes(pldoc_wiki, strip_ws_tokens(+any, -any), unknown).
type_modes(pldoc_wiki, md_section_line(+any, -any), semidet).
type_modes(pldoc_wiki, twiki_section_line(+any, -any), semidet).
type_modes(pldoc_wiki, section_header(+any, -any, -any), semidet).
type_modes(pldoc_wiki, rest_par(+any, -any, +any, +any, -any, -any), det).
type_modes(pldoc_wiki, md_table_structure_line(+any), unknown).
type_modes(pldoc_wiki, row(-any, ?(list), ?(list)), det).
type_modes(pldoc_wiki, term_item(+any, -any, ?(any)), semidet).
type_modes(pldoc_wiki, ul_to_dl(+any, -any), semidet).
type_modes(pldoc_wiki, split_dt(+any, -any, -any), unknown).
type_modes(pldoc_wiki, list_item_prefix(?(any), +any, -any), det).
type_modes(pldoc_wiki, rest_list(+any, +any, +any, -any, -any, -any), det).
type_modes(pldoc_wiki, take_blocks_at_indent(+any, +any, -any, -any), det).
type_modes(pldoc_wiki, rest_list_item(+any, +any, +any, -any, -any), det).
type_modes(pldoc_wiki, list_item(+any, ?(any), ?(any), -any, -any, -any), det).
type_modes(pldoc_wiki, ruler(+any), semidet).
type_modes(pldoc_wiki, take_block(+any, +any, ?(any), -any), semidet).
type_modes(pldoc_wiki, wiki_structure(+lines, +any, -list(block)), det).
type_modes(pldoc_wiki, wiki_codes_to_dom(+any, +any, -any), det).
type_modes(pldoc_wiki, wiki_lines_to_dom(+lines, +list(atom), -any), det).
type_modes(http_inetd, http_server(:(any), +any), unknown).
type_modes(dump, attribute_goals(@any, ?(list), ?(list)), det).
type_modes(dump, dump(+any, -any, -any), det).
type_modes(html_head, mime_include(+any, +any, ?(list), ?(list)), semidet).
type_modes(html_head, html_include(+any, ?(list), ?(list)), det).
type_modes(html_head, same_resource(+any, +any), semidet).
type_modes(html_head, res_properties(+any, -any), det).
type_modes(html_head, before(+any, -any), det).
type_modes(html_head, connect_graph(+any, -any, -any), det).
type_modes(html_head, prop_edges(+any, ?(list), ?(list)), unknown).
type_modes(html_head, requirements_graph(+any, +any, -any), det).
type_modes(html_head, order_html_resources(+any, +any, -any), det).
type_modes(html_head, requires(+any, +any, ?(list), ?(list)), det).
type_modes(html_head, requires(+any, ?(list), ?(list)), det).
type_modes(html_head, current_aggregate(-any, -any, -any), nondet).
type_modes(html_head, use_agregates(+any, -any, -any), det).
type_modes(html_head, html_insert_resource(+any, ?(list), ?(list)), det).
type_modes(html_head, html_requires(+any, ?(list), ?(list)), det).
type_modes(html_head, html_current_resource(?(any)), nondet).
type_modes(html_head, html_resource(+any, +any), det).
type_modes(prolog_install, qcompile_libraries, unknown).
type_modes(ansi_term, prolog:message_line_element(+any, +any), semidet).
type_modes(ansi_term, sgr_code(+any, -any), unknown).
type_modes(ansi_term, ansi_format(+any, +any, +any), det).
type_modes(html_quasi_quotations, html(+any, +any, +any, -any), det).
type_modes(sgml_write, dtd_character_entities(+any, -any), unknown).
type_modes(sgml_write, empty_element(+any, +any), unknown).
type_modes(sgml_write, writeq(+codes, +stream, +atom, +assoc), det).
type_modes(sgml_write, missing_namespaces(+any, +any, -any), unknown).
type_modes(sgml_write, xmlns(?(any), ?(any)), nondet).
type_modes(sgml_write, generate_ns(+any, -any), det).
type_modes(sgml_write, add_missing_namespaces(+any, +any, -any), unknown).
type_modes(sgml_write, content(+any, +any, +any, +any, +any), unknown).
type_modes(sgml_write, update_nsmap(+any, !(any)), unknown).
type_modes(sgml_write, emit(+any, +any, +any, +any), unknown).
type_modes(sgml_write, emit_doctype(+any, +any, +any), unknown).
type_modes(sgml_write, html_write(+any, +any, +any), det).
type_modes(sgml_write, sgml_write(+any, +any, +any), det).
type_modes(sgml_write, xml_write(+any, +any, +any), det).
type_modes(sgml_write, html_write(+any, +any), det).
type_modes(sgml_write, sgml_write(+any, +any), det).
type_modes(sgml_write, xml_write(+any, +any), det).
type_modes(terms, term_factorized(+any, -any, -any), unknown).
type_modes(terms, lgg_safe(+any, +any, -any, +any, -any), det).
type_modes(terms, term_subsumer(+any, +any, -any), det).
type_modes(terms, subsumes(+any, @any), unknown).
type_modes(terms, subsumes_chk(@any, @any), unknown).
type_modes(terms, variant(@any, @any), semidet).
type_modes(http_client, http:post_data_hook(+any, +any, +any), semidet).
type_modes(http_client, http_disconnect(+any), det).
type_modes(http_client, http_convert_data(+any, +any, -any, +any), semidet).
type_modes(http_client, http_read_data(+any, -any, +any), det).
type_modes(http_client, http_patch(+any, +any, -any, +any), unknown).
type_modes(http_client, http_put(+any, +any, -any, +any), unknown).
type_modes(http_client, http_post(+any, +any, -any, +any), det).
type_modes(http_client, http_delete(+any, -any, +any), det).
type_modes(http_client, http_get(+any, -any, +any), det).
type_modes(random, partial_list(@any), semidet).
type_modes(random, random_permutation(-any, +any), det).
type_modes(random, random_permutation(+any, -any), det).
type_modes(random, randseq(+int, +int, -list(int)), det).
type_modes(random, randset(+int, +int, -list(int)), det).
type_modes(random, random_select(+any, -any, +any), det).
type_modes(random, random_select(-any, +any, -any), semidet).
type_modes(random, random_member(-any, +list), semidet).
type_modes(random, random_perm2(?(any), ?(any), ?(any), ?(any)), semidet).
type_modes(random, maybe(+any, +any), semidet).
type_modes(random, maybe(+any), semidet).
type_modes(random, maybe, semidet).
type_modes(random, getrand(-any), det).
type_modes(random, setrand(+any), det).
type_modes(random, random(+float, +float, -float), det).
type_modes(random, random(+int, +int, -int), det).
type_modes(random, random_between(+int, +int, -int), semidet).
type_modes(random, random(-float), det).
type_modes(prolog_codewalk, filepos_line(+any, +any, -any, -any), det).
type_modes(prolog_codewalk, prolog_program_clause(-any, +any), nondet).
type_modes(prolog_codewalk, predicate_in_module(+any, ?(any)), nondet).
type_modes(prolog_codewalk, variants(+any, -any), det).
type_modes(prolog_codewalk, extend(+any, +any, +any, -any, +any), unknown).
type_modes(prolog_codewalk, subterm_pos(+any, +any, :(any), +any, -any), nondet).
type_modes(prolog_codewalk, walk_dcg_body(+any, +any, +any, +any), unknown).
type_modes(prolog_codewalk, walk_called_by(+list, +any, +any, +any, +any), unknown).
type_modes(prolog_codewalk, walk_meta_call(+any, +any, +any, +any, +any, +any, +any), unknown).
type_modes(prolog_codewalk, calling_metaspec(+any), semidet).
type_modes(prolog_codewalk, infer_new_meta_predicates(-any, +any), det).
type_modes(prolog_codewalk, register_possible_meta_clause(+any), det).
type_modes(prolog_codewalk, print_reference(+any, +any, +any, +any), unknown).
type_modes(prolog_codewalk, not_callable(+any, +any, +any), unknown).
type_modes(prolog_codewalk, undefined(:(any), +any, +any), unknown).
type_modes(prolog_codewalk, evaluate(?(any), ?(any), ?(any)), nondet).
type_modes(prolog_codewalk, undecided(+any, +any, +any), unknown).
type_modes(prolog_codewalk, walk_called(+any, +any, +any, +any), multi).
type_modes(prolog_codewalk, walk_called_by_body(+any, +any, +any, +any), unknown).
type_modes(prolog_codewalk, walk_called_by_body(+any, +any, +any), det).
type_modes(prolog_codewalk, clause_not_from_development(:(any), -any, ?(any), +any), nondet).
type_modes(prolog_codewalk, walk_from_multifile(+any), unknown).
type_modes(prolog_codewalk, find_walk_from_module(+any, +any), det).
type_modes(prolog_codewalk, walk_from_initialization(+any), unknown).
type_modes(prolog_codewalk, scan_module(+any, +any), semidet).
type_modes(prolog_codewalk, walk_clauses(?(any), +any), det).
type_modes(prolog_codewalk, prolog_walk_code(+any), det).
type_modes(json_convert, create_rule(+any, +any, -any, -any, -any, -any), det).
type_modes(json_convert, pairs_to_term(+any, ?(any), +any), semidet).
type_modes(json_convert, json_to_prolog(+any, -any), det).
type_modes(json_convert, prolog_to_json(:(any), -any), det).
type_modes(json_convert, types(+any, -any, -any), unknown).
type_modes(json_convert, defaults(+any, -any, -any), unknown).
type_modes(json_convert, current_clause(+any, +any, +any, +any, +any, +any), unknown).
type_modes(json_convert, clean_body(+any, -any), det).
type_modes(json_convert, type_goal(+any, +any, -any), det).
type_modes(json_convert, prolog_bool_to_json(+any, -any), semidet).
type_modes(json_convert, type_checks(+any, -any, -any, -any, +any), det).
type_modes(json_convert, record_to_json_clause(+any, +any, +any, +any), unknown).
type_modes(json_convert, compile_json_objects(+any, -any), det).
type_modes(json_convert, json_object(+any), unknown).
type_modes(json_convert, current_json_object(?(any), ?(any), ?(any)), unknown).
type_modes(rdfs, rdfs_find(+any, +any, ?(any), +any, -any), unknown).
type_modes(rdfs, rdfs_assert_list(+any, -any, +any), det).
type_modes(rdfs, rdfs_assert_list(+any, -any), det).
type_modes(rdfs, rdfs_list_to_prolog_list(+any, -any), unknown).
type_modes(rdfs, rdfs_member(?(any), +any), unknown).
type_modes(rdfs, rdfs_class_property(+any, ?(any)), unknown).
type_modes(rdfs, label_of(+any, ?(any), ?(any)), nondet).
type_modes(rdfs, take_label(+any, ?(any), -any), multi).
type_modes(rdfs, rdfs_ns_label(+any, ?(any), -any), multi).
type_modes(rdfs, rdfs_ns_label(+any, -any), multi).
type_modes(rdfs, rdfs_label(-any, ?(any), ?(any)), nondet).
type_modes(rdfs, rdfs_label(+any, ?(any), +any), semidet).
type_modes(rdfs, rdfs_label(+any, ?(any), -any), multi).
type_modes(rdfs, rdfs_label(-any, +any), unknown).
type_modes(rdfs, rdfs_label(+any, -any), unknown).
type_modes(rdfs, rdfs_individual_of_r_c(+any, ?(any)), nondet).
type_modes(rdfs, rdfs_individual_of(-any, +any), nondet).
type_modes(rdfs, rdfs_individual_of(+any, -any), nondet).
type_modes(rdfs, rdfs_individual_of(+any, +any), semidet).
type_modes(rdfs, rdfs_subclass_of(?(any), +any), nondet).
type_modes(rdfs, rdfs_subclass_of(+any, ?(any)), nondet).
type_modes(rdfs, rdfs_subproperty_of(?(any), +any), nondet).
type_modes(rdfs, rdfs_subproperty_of(+any, ?(any)), nondet).
type_modes(rdf_compare, rdf_equal_graphs(+any, +any, -any), semidet).
type_modes(prolog_cover, clause_source(-any, +any, -any), det).
type_modes(prolog_cover, clause_source(+any, -any, -any), det).
type_modes(prolog_cover, file_coverage(+any, +any), det).
type_modes(prolog_cover, covered(+any, +any, +any, -any, -any), det).
type_modes(prolog_cover, assert_exited(+any), det).
type_modes(prolog_cover, assert_entered(+any), det).
type_modes(prolog_cover, running_static_pred(+any), semidet).
type_modes(prolog_cover, assert_cover(+any, +any), det).
type_modes(prolog_cover, show_coverage(:(any)), unknown).
type_modes(iostream, open_hook(+any, +any, -any, -any, +any, -any), semidet).
type_modes(iostream, close_any(+any), unknown).
type_modes(iostream, open_any_builtin(+any, +any, -any, -any, +any, -any), semidet).
type_modes(iostream, input_options(+any, -any, +any, -any, +any), det).
type_modes(iostream, open_any(+any, +any, -any, -any, +any), unknown).
type_modes(prolog_console_input, file_prefix(-any, ?(list), ?(list)), semidet).
type_modes(prolog_console_input, atom_prefix(-any), det).
type_modes(prolog_console_input, prolog:complete_input(+any, +any, -any, -any), det).
type_modes(clpfd_gcc_occurred, blocked(+any), semidet).
type_modes(pldoc_man, prolog:doc_object_href(+any, -any), semidet).
type_modes(pldoc_man, prolog:doc_object_link(+any, +any, ?(list), ?(list)), unknown).
type_modes(pldoc_man, pldoc_refman(+any), unknown).
type_modes(pldoc_man, paperback(+any, ?(list), ?(list)), unknown).
type_modes(pldoc_man, pldoc_package_overview(+any), unknown).
type_modes(pldoc_man, pldoc_package(+any), unknown).
type_modes(pldoc_man, pldoc_jpl(+any), unknown).
type_modes(pldoc_man, man_overview(+any, ?(list), ?(list)), det).
type_modes(pldoc_man, function_link(+any, +any), det).
type_modes(pldoc_man, section_link(+any, +any, ?(list), ?(list)), det).
type_modes(pldoc_man, man_links(+any, +any, ?(list), ?(list)), det).
type_modes(pldoc_man, referenced_section(+any, +any, +any, -any), unknown).
type_modes(pldoc_man, name_to_object(+any, -any), semidet).
type_modes(pldoc_man, rewrite_ref(+any, +any, +any, -any), semidet).
type_modes(pldoc_man, documented(+any), semidet).
type_modes(pldoc_man, man_match(+any, +any, ?(list), ?(list)), unknown).
type_modes(pldoc_man, object_module(+any, -any, -any), semidet).
type_modes(pldoc_man, man_synopsis(+any, +any, ?(list), ?(list)), unknown).
type_modes(pldoc_man, man_qualified_object(+any, +any, -any, -any), semidet).
type_modes(pldoc_man, man_page(+any, +any, ?(list), ?(list)), semidet).
type_modes(pldoc_man, object_spec(+any, -any), unknown).
type_modes(pldoc_man, object_section(+any, +any, -any), semidet).
type_modes(pldoc_man, parent_section(+any, -any), det).
type_modes(pldoc_man, resolve_section(+any, -any), det).
type_modes(pldoc_man, load_man_object(+any, -any, -any, -any), nondet).
type_modes(pldoc_man, html_content_tree(+any, -any), det).
type_modes(pldoc_man, man_packages_tree(-any), det).
type_modes(pldoc_man, man_content_tree(+any, -any), det).
type_modes(pldoc_man, ensure_man_tree, unknown).
type_modes(pldoc_man, man_child_of(?(any), ?(any)), nondet).
type_modes(pldoc_man, man_nav_tree(+any, -any, +any), semidet).
type_modes(pldoc_man, man_nav_tree(+any, +any), semidet).
type_modes(pldoc_man, current_man_object(?(any)), nondet).
type_modes(pldoc_man, dom_to_text(+any, -any), unknown).
type_modes(pldoc_man, summary(+any, -string), det).
type_modes(pldoc_man, dom_section(+any, -any, -any), semidet).
type_modes(pldoc_man, index_on_begin(+any, +any, +any), semidet).
type_modes(pldoc_man, index_man_file(+any, +any), unknown).
type_modes(pldoc_man, index_man_directory(?(any), +any), det).
type_modes(pldoc_man, index_manual, det).
type_modes(pldoc_man, manual_directory(-any, -any, ?(list), ?(list)), nondet).
type_modes(pldoc_man, clean_man_index, det).
type_modes(odbc, odbc_table_foreign_key(+any, ?(any), ?(any), ?(any), ?(any)), unknown).
type_modes(odbc, odbc_table_primary_key(+any, +any, ?(any)), unknown).
type_modes(odbc, odbc_data_source(?(any), ?(any)), unknown).
type_modes(odbc, odbc_type(+any, +any, ?(any)), unknown).
type_modes(odbc, sql_type(+any, +any, -any), unknown).
type_modes(odbc, odbc_table_column(+any, +any, ?(any), -any), unknown).
type_modes(odbc, odbc_table_column(+any, +any, -any), nondet).
type_modes(odbc, odbc_table_column(+any, +any, +any), semidet).
type_modes(odbc, odbc_current_table(-any, -any), unknown).
type_modes(odbc, odbc_query(+any, +any), unknown).
type_modes(odbc, odbc_query(+any, +any, -any), unknown).
type_modes(odbc, odbc_driver_connect(+any, -any, +any), det).
type_modes(odbc, odbc_current_connection(?(any), ?(any)), nondet).
type_modes(unicode, unicode_property(?(any), ?(any)), nondet).
type_modes(unicode, unicode_nfkc(+any, -any), det).
type_modes(unicode, unicode_nfkd(+any, -any), det).
type_modes(unicode, unicode_nfc(+any, -any), det).
type_modes(unicode, unicode_nfd(+any, -any), det).
type_modes(unicode, unicode_map(+any, -any, +any), det).
type_modes(http_files, http_reply_from_files(+any, +any, +any), unknown).
type_modes(base32, base32(-any, ?(list), ?(list)), det).
type_modes(base32, base32(+any, ?(list), ?(list)), det).
type_modes(base32, base32(-any, +any), det).
type_modes(base32, base32(+any, -any), det).
type_modes(portray_text, set_portray_text(+any, +any), det).
type_modes(portray_text, portray_text(+any), det).
type_modes(quasi_quotations, stream_syntax_error_context(+any, -any), det).
type_modes(quasi_quotations, quasi_quotation_syntax_error(+any), unknown).
type_modes(quasi_quotations, quasi_quotation_syntax(:(any)), det).
type_modes(quasi_quotations, phrase_from_quasi_quotation(:(any), +any), det).
type_modes(quasi_quotations, with_quasi_quotation_input(+any, -any, :(any)), det).
type_modes(javascript, json_to_string(+any, -any), unknown).
type_modes(javascript, js_identifier(+atom), semidet).
type_modes(javascript, js_identifier(+atom, ?(list), ?(list)), det).
type_modes(javascript, js_quoted_string(+any, -any), unknown).
type_modes(javascript, js_arg(+any, ?(list), ?(list)), semidet).
type_modes(javascript, js_expression(+any, ?(list), ?(list)), det).
type_modes(javascript, js_arg_list(+list, ?(list), ?(list)), det).
type_modes(javascript, js_new(+any, +any, ?(list), ?(list)), det).
type_modes(javascript, js_call(+any, ?(list), ?(list)), det).
type_modes(javascript, javascript(+any, +any, +any, -any), det).
type_modes(javascript, js_script(+any, ?(list), ?(list)), det).
type_modes(http_dispatch, add_path_tree(+any, +any, +any, +any, -any), det).
type_modes(http_dispatch, plain_path(-any, -any, -any), nondet).
type_modes(http_dispatch, add_paths_tree(+any, -any), det).
type_modes(http_dispatch, prefix_options(+any, +any, -any), unknown).
type_modes(http_dispatch, prefix_tree(?(any), +any, -any), unknown).
type_modes(http_dispatch, path_tree(-any), det).
type_modes(http_dispatch, http_switch_protocol(:(any), +any), unknown).
type_modes(http_dispatch, http_404(+any, +any), det).
type_modes(http_dispatch, http_redirect(+any, +any, +any), det).
type_modes(http_dispatch, http_safe_file(+any, +any), det).
type_modes(http_dispatch, http_reply_file(+any, +any, +any), det).
type_modes(http_dispatch, call_action(+any, +any, +any), unknown).
type_modes(http_dispatch, action(+any, +any, +any), det).
type_modes(http_dispatch, supports_method(+any, +any), det).
type_modes(http_dispatch, find_handler(+any, -any, -any), det).
type_modes(http_dispatch, authentication(+any, +any, -any), det).
type_modes(http_dispatch, http_reload_with_parameters(+any, +any, -any), det).
type_modes(http_dispatch, http_link_to_id(+any, +any, -any), unknown).
type_modes(http_dispatch, http_location_by_id(+any, -any), det).
type_modes(http_dispatch, http_current_handler(?(any), :(any), ?(any)), nondet).
type_modes(http_dispatch, http_current_handler(+any, :(any), -any), semidet).
type_modes(http_dispatch, http_current_handler(-any, :(any)), nondet).
type_modes(http_dispatch, http_current_handler(+any, :(any)), semidet).
type_modes(http_dispatch, http_dispatch(?(any)), det).
type_modes(http_dispatch, check_path(+any, -any), det).
type_modes(http_dispatch, combine_methods(+any, -any), det).
type_modes(http_dispatch, compile_handler(+any, :(any), +any), det).
type_modes(http_dispatch, current_generation(-any), det).
type_modes(http_dispatch, next_generation, det).
type_modes(http_dispatch, http_delete_handler(+any), det).
type_modes(http_dispatch, http_handler(+any, :(any), +any), det).
type_modes(persistency, db_sync_all(+any), unknown).
type_modes(persistency, db_sync(:(any)), unknown).
type_modes(persistency, db_retract(:(any)), nondet).
type_modes(persistency, db_retractall(:(any)), det).
type_modes(persistency, sync(+any, +any), det).
type_modes(persistency, db_detach, det).
type_modes(persistency, db_assert(:(any)), det).
type_modes(persistency, db_size(+any, -any), det).
type_modes(persistency, db_attach(:(any), +any), unknown).
type_modes(persistency, current_persistent_predicate(:(any)), nondet).
type_modes(persistency, persistent(+any), unknown).
type_modes(rewrite, simplify(+any, -any), unknown).
type_modes(rewrite, rewrite(:(any), +any), unknown).
type_modes(prolog_edit, select_location(+any, +any, -any), unknown).
type_modes(prolog_edit, edit_command(+any, -any), unknown).
type_modes(prolog_edit, editor(-any), unknown).
type_modes(prolog_edit, do_edit_source(+any), unknown).
type_modes(prolog_edit, locate(+any, -any), unknown).
type_modes(prolog_edit, locate(+any, -any, -any), unknown).
type_modes(prolog_edit, edit, unknown).
type_modes(prolog_edit, edit(+any), unknown).
type_modes(rdf_write, rdf_value(+any, -any, +any), unknown).
type_modes(rdf_write, rdf_att_id(+any, +any, -any), unknown).
type_modes(rdf_write, rdf_write_id(+any, +any), det).
type_modes(rdf_write, rdf_id(+any, +any, -any), unknown).
type_modes(rdf_write, is_bag_li_predicate(+any), semidet).
type_modes(rdf_write, rdf_p_id(+any, +any, -any), unknown).
type_modes(rdf_write, save_attribute(+any, +any, +any, +any, +any, +any, +any), unknown).
type_modes(rdf_write, save_attributes(+any, +any, +any, +any, ?(any), +any, +any), unknown).
type_modes(rdf_write, node_id_map(+any, -any), det).
type_modes(rdf_write, decl_used_predicate_ns(+list(rdf(_, _, _))), unknown).
type_modes(rdf_write, resources(+list(rdf(_, _, _)), -list(atom)), det).
type_modes(rdf_write, used_namespaces(+list(rdf(_, _, _)), -atom), det).
type_modes(rdf_write, xml_escape_parameter_entity(+any, -any), det).
type_modes(rdf_write, rdf_write_header(+any, +any), unknown).
type_modes(rdf_write, rdf_write_xml(+stream, +list(rdf(_, _, _))), det).
type_modes(broadcast, canonical_listener(+any, -any), unknown).
type_modes(broadcast, broadcast_request(+any), nondet).
type_modes(broadcast, broadcast(+any), det).
type_modes(broadcast, listening(?(any), ?(any), ?(any)), nondet).
type_modes(broadcast, unlisten(+any, +any, :(any)), det).
type_modes(broadcast, unlisten(+any, +any), det).
type_modes(broadcast, unlisten(+any), det).
type_modes(broadcast, listen(+any, :(any)), det).
type_modes(broadcast, listen(+any, +any, :(any)), det).
type_modes(unix, sysconf(+any), semidet).
type_modes(unix, prctl(+any), det).
type_modes(unix, detach_IO, det).
type_modes(unix, detach_IO(+any), det).
type_modes(unix, dup(+any, +any), det).
type_modes(unix, pipe(-any, -any), det).
type_modes(unix, kill(+any, +any), det).
type_modes(unix, wait(?(any), -any), det).
type_modes(unix, exec(+any), unknown).
type_modes(unix, fork_exec(+any), det).
type_modes(unix, fork_warn_threads, unknown).
type_modes(unix, fork(-any), det).
type_modes(udp, blocked(+any), semidet).
type_modes(hub, hub_thread(:(any), +any, +any), det).
type_modes(hub, broadcast_from_queue(+any, +any), det).
type_modes(hub, broadcast_from_queues(+any, +any), det).
type_modes(hub, hub_broadcast(+any, +any), det).
type_modes(hub, hub_send(+any, +any), det).
type_modes(hub, io_error(+any, +any, +any), unknown).
type_modes(hub, get_messages(+any, +any, -any), det).
type_modes(hub, wait_timeout(+any, +any, -any), det).
type_modes(hub, hub_add(+any, +any, ?(any)), det).
type_modes(hub, current_hub(?(any), ?(any)), nondet).
type_modes(hub, hub_create(+any, -any, +any), det).
type_modes(socket, negotiate_socks_connection(+any, +any), det).
type_modes(socket, gethostname(-any), det).
type_modes(socket, tcp_host_to_address(?(any), ?(any)), det).
type_modes(socket, tcp_fcntl(+any, +any, ?(any)), det).
type_modes(socket, tcp_setopt(+any, +any), det).
type_modes(socket, proxy_for_url(+any, +any, -any), nondet).
type_modes(socket, try_proxy(+any, +any, -any, -any), semidet).
type_modes(socket, tcp_connect(+any, +any, -any), det).
type_modes(socket, tcp_connect(+any, -any, +any), det).
type_modes(socket, tcp_connect(+any, +any, -any, -any), det).
type_modes(socket, tcp_connect(+any, +any), det).
type_modes(socket, tcp_accept(+any, -any, -any), det).
type_modes(socket, tcp_listen(+any, +any), det).
type_modes(socket, tcp_bind(?(any), ?(any)), det).
type_modes(socket, tcp_open_socket(+any, -any, -any), det).
type_modes(socket, tcp_open_socket(+any, -any), det).
type_modes(socket, tcp_close_socket(+any), det).
type_modes(socket, tcp_socket(-any), det).
type_modes(xsdp_type, xsdp_convert(+any, +any, -any), unknown).
type_modes(xsdp_type, xsdp_numeric_uri(?(any), -any), nondet).
type_modes(xsdp_type, xsdp_subtype_of(?(any), ?(any)), unknown).
type_modes(xsdp_type, xsdp_uri_type(?(any), ?(any)), unknown).
type_modes(xsdp_type, xsdp_type(?(any)), unknown).
type_modes(rdf_edit, make_path(+any, +any, -any), unknown).
type_modes(rdf_edit, replay_action(+any), unknown).
type_modes(rdf_edit, rdfe_replay_journal(+any), unknown).
type_modes(rdf_edit, rdfe_current_journal(-any), unknown).
type_modes(rdf_edit, rdfe_close_journal, unknown).
type_modes(rdf_edit, rdfe_open_journal(+any, +any), det).
type_modes(rdf_edit, rdfe_reset_journal, unknown).
type_modes(rdf_edit, rdfe_reset, unknown).
type_modes(rdf_edit, rdfe_set_watermark(?(any)), unknown).
type_modes(rdf_edit, rdfe_clear_modified(+any), det).
type_modes(rdf_edit, rdfe_is_modified(?(any)), unknown).
type_modes(rdf_edit, to_uri(+any, -any), det).
type_modes(rdf_edit, check_file_protection(-any), unknown).
type_modes(rdf_edit, rdfe_get_file_property(-any, ?(any)), unknown).
type_modes(rdf_edit, rdfe_get_file_property(+any, ?(any)), unknown).
type_modes(rdf_edit, rdfe_set_file_property(+any, +any), unknown).
type_modes(rdf_edit, rdfe_transaction_member(+any, -any), unknown).
type_modes(rdf_edit, rdfe_set_transaction_name(+any), unknown).
type_modes(rdf_edit, rdfe_transaction_name(+any, -any), unknown).
type_modes(rdf_edit, rdfe_can_undo(-any), semidet).
type_modes(rdf_edit, rdfe_can_redo(-any), semidet).
type_modes(rdf_edit, rdfe_redo, unknown).
type_modes(rdf_edit, rdfe_undo, unknown).
type_modes(rdf_edit, undo(+any), unknown).
type_modes(rdf_edit, rollback(+any), unknown).
type_modes(rdf_edit, rdfe_begin_transaction, unknown).
type_modes(rdf_edit, rdfe_transaction(:(any)), unknown).
type_modes(rdf_edit, rdfe_register_ns(?(any), ?(any)), unknown).
type_modes(rdf_edit, rdfe_snapshot_file(-any), unknown).
type_modes(rdf_edit, snapshot_file(+any, +any, +any, -any), unknown).
type_modes(rdf_edit, load_snapshot(+any, +any), unknown).
type_modes(rdf_edit, ensure_snapshot(+any), unknown).
type_modes(rdf_edit, rdfe_load(+any, +any), det).
type_modes(rdf_edit, rdfe_load(+any), det).
type_modes(rdf_edit, rdfe_delete(+any), unknown).
type_modes(rdf_edit, rdfe_update(+any, +any, +any, +any), unknown).
type_modes(prolog_modewalk, filepos_line(+any, +any, -any, -any), det).
type_modes(prolog_modewalk, prolog_program_mode_clause(-any, +any), nondet).
type_modes(prolog_modewalk, predicate_in_module(+any, ?(any)), nondet).
type_modes(prolog_modewalk, variants(+any, -any), det).
type_modes(prolog_modewalk, extend(+any, +any, +any, -any, +any), unknown).
type_modes(prolog_modewalk, subterm_pos(+any, +any, :(any), +any, -any), nondet).
type_modes(prolog_modewalk, walk_dcg_body(+any, +any, +any, +any), unknown).
type_modes(prolog_modewalk, walk_called_by(+list, +any, +any, +any, +any), unknown).
type_modes(prolog_modewalk, walk_meta_call(+any, +any, +any, +any, +any, +any, +any), unknown).
type_modes(prolog_modewalk, calling_metaspec(+any), semidet).
type_modes(prolog_modewalk, infer_new_modes(-any, +any), det).
type_modes(prolog_modewalk, register_possible_meta_clause(+any), det).
type_modes(prolog_modewalk, print_reference(+any, +any, +any, +any), unknown).
type_modes(prolog_modewalk, not_knowable(+any, +any, +any), unknown).
type_modes(prolog_modewalk, undefined(:(any), +any, +any), unknown).
type_modes(prolog_modewalk, evaluate(?(any), ?(any), ?(any)), nondet).
type_modes(prolog_modewalk, undecided(+any, +any, +any), unknown).
type_modes(prolog_modewalk, walk_called(+any, +any, +any, +any), multi).
type_modes(prolog_modewalk, walk_called_by_body(+any, +any, +any, +any), unknown).
type_modes(prolog_modewalk, walk_called_by_body(+any, +any, +any), det).
type_modes(prolog_modewalk, clause_not_from_development(:(any), -any, ?(any), +any), nondet).
type_modes(prolog_modewalk, walk_from_multifile(+any), unknown).
type_modes(prolog_modewalk, find_walk_from_module(+any, +any), det).
type_modes(prolog_modewalk, walk_from_initialization(+any), unknown).
type_modes(prolog_modewalk, scan_module(+any, +any), semidet).
type_modes(prolog_modewalk, walk_clauses(?(any), +any), det).
type_modes(prolog_modewalk, prolog_walk_mode(+any), det).
type_modes(oset, oset_delel(+any, +any, -any), unknown).
type_modes(oset, oset_addel(+any, +any, -any), unknown).
type_modes(oset, oset_power(+any, -any), unknown).
type_modes(oset, oset_dint(+any, -any), unknown).
type_modes(oset, oset_dunion(+any, -any), unknown).
type_modes(oset, oset_diff(+any, +any, -any), unknown).
type_modes(oset, oset_int(+any, +any, -any), unknown).
type_modes(oset, oset_union(+any, +any, -any), unknown).
type_modes(oset, oset_is(+any), unknown).
type_modes(predopts_analysis, blocked(+any), semidet).
type_modes(clpb_bdd, blocked(+any), semidet).
type_modes(html_write, html_meta(+any), det).
type_modes(html_write, reply_html_page(+any, :(any), :(any)), det).
type_modes(html_write, reply_html_page(:(any), :(any)), det).
type_modes(html_write, html_print_length(+any, -any), det).
type_modes(html_write, valid_cdata(+any, +any), det).
type_modes(html_write, print_html(+stream, +any), det).
type_modes(html_write, print_html(+any), det).
type_modes(html_write, layout(+any, -any, -any), det).
type_modes(html_write, head_html(+list, ?(list), ?(list)), det).
type_modes(html_write, sorted_html(+list, ?(list), ?(list)), det).
type_modes(html_write, mail_handlers(+any, -any, -any), det).
type_modes(html_write, mailboxes(+any, -any), det).
type_modes(html_write, html_token(?(any), +any), nondet).
type_modes(html_write, mailman(+any), det).
type_modes(html_write, html_noreceive(+any, ?(list), ?(list)), det).
type_modes(html_write, html_receive(+any, :(any), ?(list), ?(list)), det).
type_modes(html_write, html_receive(+any, ?(list), ?(list)), det).
type_modes(html_write, html_post(+any, :(any), ?(list), ?(list)), det).
type_modes(html_write, cdata_element(?(any)), unknown).
type_modes(html_write, html_quoted_attribute(+any, ?(list), ?(list)), det).
type_modes(html_write, html_quoted(?(any), ?(list), ?(list)), det).
type_modes(html_write, attribute_value_m(+any, ?(list), ?(list)), unknown).
type_modes(html_write, attribute_value(+any), det).
type_modes(html_write, attributes(+any, +any, ?(list), ?(list)), det).
type_modes(html_write, xhtml_ns(?(any), ?(any), ?(list), ?(list)), unknown).
type_modes(html_write, xhtml_empty(+any, +any, ?(list), ?(list)), det).
type_modes(html_write, html_end(+any, ?(list), ?(list)), det).
type_modes(html_write, html_begin(+any, ?(list), ?(list)), det).
type_modes(html_write, raw(+any, +any, ?(list), ?(list)), det).
type_modes(html_write, html(+dom, ?(list), ?(list)), det).
type_modes(html_write, doctype(?(list), ?(list)), unknown).
type_modes(html_write, page(+dom, +dom, ?(list), ?(list)), det).
type_modes(html_write, page(+dom, ?(list), ?(list)), det).
type_modes(html_write, ns(?(any), ?(any)), unknown).
type_modes(html_write, xml_header(-any), unknown).
type_modes(html_write, init_options, det).
type_modes(html_write, html_current_option(?(any)), nondet).
type_modes(html_write, html_set_options(+any), det).
type_modes(http_session, random_4(-any, -any, -any, -any), det).
type_modes(http_session, urandom(-any), semidet).
type_modes(http_session, route(-any), semidet).
type_modes(http_session, http_session_cookie(-any), det).
type_modes(http_session, http_gc_sessions, det).
type_modes(http_session, expire_session_cookie(+any), det).
type_modes(http_session, http_close_session(+any), det).
type_modes(http_session, http_current_session(?(any), ?(any)), nondet).
type_modes(http_session, http_session_data(?(any)), nondet).
type_modes(http_session, http_session_retractall(?(any)), det).
type_modes(http_session, http_session_retract(?(any)), nondet).
type_modes(http_session, http_session_assert(+any), det).
type_modes(http_session, http_session_asserta(+any), det).
type_modes(http_session, valid_session_id(+any, +any), semidet).
type_modes(http_session, open_session(+any, +any), unknown).
type_modes(http_session, peer(+any, -any), det).
type_modes(http_session, http_open_session(-any, +any), det).
type_modes(http_session, http_session(+any, -any, -any), semidet).
type_modes(http_session, http_in_session(-any), semidet).
type_modes(http_session, http_session_id(-any), det).
type_modes(http_session, http_set_session(?(any)), det).
type_modes(http_session, session_setting(+any, ?(any)), semidet).
type_modes(http_session, http_session_option(?(any)), nondet).
type_modes(http_session, http_set_session_options(+any), det).
type_modes(sicstus_sockets, hostname_address(+atom, -atom), det).
type_modes(sicstus_sockets, current_host(-any), det).
type_modes(sicstus_sockets, socket_select(+any, -any, +any, +any, -any), det).
type_modes(turtle, rdf_db:rdf_load_stream(+any, +any, :(any)), unknown).
type_modes(turtle, turtle_write_uri(+any, +any), det).
type_modes(turtle, turtle_write_quoted_string(+any, +any), det).
type_modes(turtle, turtle_write_quoted_string(+any, +any, ?(any)), det).
type_modes(turtle, turtle_pn_local(+atom), semidet).
type_modes(turtle, base_uri(+any, -any, +any), unknown).
type_modes(turtle, open_input(+any, -any, -any), det).
type_modes(turtle, rdf_process_turtle(+any, :(any), +any), det).
type_modes(turtle, rdf_load_turtle(+any, -any, +any), unknown).
type_modes(turtle, rdf_read_turtle(+any, -any, +any), unknown).
type_modes(pldoc_htmlsrc, element(+any, -any, -any), nondet).
type_modes(pldoc_htmlsrc, read_n_codes(+any, +any, -any), unknown).
type_modes(pldoc_htmlsrc, copy_rest(+any, +any, +any, -any), det).
type_modes(pldoc_htmlsrc, content_escape(+any, +any, +any, -any), det).
type_modes(pldoc_htmlsrc, write_codes(+any, +any, +any), det).
type_modes(pldoc_htmlsrc, append(-any, -any, ?(any), +any), nondet).
type_modes(pldoc_htmlsrc, copy_without_trailing_white_lines(+any, +any, +any, -any), det).
type_modes(pldoc_htmlsrc, delete_leading_white_lines(+any, -any, +any, -any), det).
type_modes(pldoc_htmlsrc, copy_full_stop(+any, +any), det).
type_modes(pldoc_htmlsrc, copy_to(+stream, +int, +stream, +any), det).
type_modes(pldoc_htmlsrc, anchor(+any, -any), semidet).
type_modes(pldoc_htmlsrc, html_fragment(+any, +any, +any, +any, -any, +any), det).
type_modes(pldoc_htmlsrc, html_fragments(+any, +any, +any, +any, +any), det).
type_modes(pldoc_htmlsrc, print_html_head(+stream, +any), det).
type_modes(pldoc_htmlsrc, source_to_html(+filename, +any, :(any)), det).
type_modes(prolog_vm, vm_list(:(any)), det).
type_modes(backward_compatibility, set_base_module(:(any)), det).
type_modes(backward_compatibility, hash(:(any)), det).
type_modes(backward_compatibility, index(:(any)), det).
type_modes(backward_compatibility, merge(+any, +any, -any), unknown).
type_modes(backward_compatibility, merge_set(+any, +any, -any), unknown).
type_modes(backward_compatibility, setup_and_call_cleanup(:(any), :(any), ?(any), :(any)), unknown).
type_modes(backward_compatibility, setup_and_call_cleanup(:(any), :(any), :(any)), unknown).
type_modes(backward_compatibility, export_list(+any, -any), det).
type_modes(backward_compatibility, current_module(?(any), ?(any)), nondet).
type_modes(backward_compatibility, unlock_predicate(+any, +any), det).
type_modes(backward_compatibility, lock_predicate(+any, +any), det).
type_modes(backward_compatibility, message_queue_size(+any, -any), det).
type_modes(backward_compatibility, current_mutex(?(any), ?(any), ?(any)), nondet).
type_modes(backward_compatibility, current_thread(?(any), ?(any)), nondet).
type_modes(backward_compatibility, 'C'(?(any), ?(any), ?(any)), det).
type_modes(backward_compatibility, convert_time(+any, -any, -any, -any, -any, -any, -any, -any), unknown).
type_modes(backward_compatibility, convert_time(+any, -any), unknown).
type_modes(backward_compatibility, at_initialization(:(any)), det).
type_modes(backward_compatibility, '$declare_module'(?(any), ?(any), ?(any)), unknown).
type_modes(backward_compatibility, '$module'(-any, +any), unknown).
type_modes(backward_compatibility, '$strip_module'(+any, -any, -any), unknown).
type_modes(backward_compatibility, sumlist(+any, -any), det).
type_modes(backward_compatibility, sublist(:(any), +any, ?(any)), unknown).
type_modes(backward_compatibility, checklist(:(any), +any), unknown).
type_modes(backward_compatibility, hash_term(+any, -any), det).
type_modes(backward_compatibility, subsumes(+any, @any), unknown).
type_modes(backward_compatibility, subsumes_chk(@any, @any), unknown).
type_modes(backward_compatibility, free_variables(+any, -any), unknown).
type_modes(backward_compatibility, proper_list(+any), unknown).
type_modes(backward_compatibility, write_ln(?(any)), det).
type_modes(backward_compatibility, flush, det).
type_modes(backward_compatibility, string_to_atom(?(any), ?(any)), det).
type_modes(backward_compatibility, string_to_list(?(any), ?(any)), det).
type_modes(backward_compatibility, substring(+any, +any, +any, -any), unknown).
type_modes(backward_compatibility, set_feature(+any, @any), det).
type_modes(backward_compatibility, feature(?(any), ?(any)), nondet).
type_modes(backward_compatibility, read_pending_input(+any, -any, ?(any)), det).
type_modes(backward_compatibility, read_variables(+stream, -any, -any), det).
type_modes(backward_compatibility, read_variables(-any, -any), det).
type_modes(backward_compatibility, read_clause(+any, -any), det).
type_modes(backward_compatibility, read_clause(-any), det).
type_modes(backward_compatibility, '$apropos_match'(+any, +any), semidet).
type_modes(backward_compatibility, concat_atom(+any, +any, -any), det).
type_modes(backward_compatibility, concat_atom(+any, -any), det).
type_modes(backward_compatibility, concat(+any, +any, -any), det).
type_modes(backward_compatibility, sformat(-any, +any), det).
type_modes(backward_compatibility, sformat(-any, +any, +any), det).
type_modes(backward_compatibility, displayq(+any, @any), det).
type_modes(backward_compatibility, displayq(@any), det).
type_modes(backward_compatibility, '$set_prompt'(+any), det).
type_modes(backward_compatibility, '$argv'(-list), det).
type_modes(backward_compatibility, '$home'(-any), det).
type_modes(backward_compatibility, '$version'(?(integer)), det).
type_modes(backward_compatibility, '$arch'(-any, -any), det).
type_modes(clpb_atom, blocked(+any), semidet).
type_modes(rdf11, rdf_retract_list(+any), det).
type_modes(rdf11, rdf_assert_list(+any, ?(any), +any), det).
type_modes(rdf11, rdf_assert_list(+any, ?(any)), det).
type_modes(rdf11, rdf_last(+any, -any), det).
type_modes(rdf11, rdf_nth0(?(any), +any, ?(any)), nondet).
type_modes(rdf11, rdf_member(?(any), +any), nondet).
type_modes(rdf11, rdf_length(+any, -nonneg), nondet).
type_modes(rdf11, rdf_list(+any, -any), det).
type_modes(rdf11, rdf_list(?(any)), semidet).
type_modes(rdf11, rdf_is_term(@any), semidet).
type_modes(rdf11, rdf_is_subject(@any), semidet).
type_modes(rdf11, rdf_is_predicate(@any), semidet).
type_modes(rdf11, rdf_is_object(@any), semidet).
type_modes(rdf11, rdf_is_name(@any), semidet).
type_modes(rdf11, rdf_is_literal(@any), semidet).
type_modes(rdf11, rdf_is_bnode(@any), semidet).
type_modes(rdf11, rdf_is_iri(@any), semidet).
type_modes(rdf11, rdf_create_bnode(--(any)), unknown).
type_modes(rdf11, resource(?(any)), unknown).
type_modes(rdf11, rdf_node(?(any)), nondet).
type_modes(rdf11, rdf_object(?(any)), nondet).
type_modes(rdf11, rdf_predicate(?(any)), nondet).
type_modes(rdf11, rdf_subject(?(any)), nondet).
type_modes(rdf11, rdf_name(?(any)), nondet).
type_modes(rdf11, rdf_iri(?(any)), nondet).
type_modes(rdf11, rdf_bnode(?(any)), nondet).
type_modes(rdf11, rdf_literal(?(any)), nondet).
type_modes(rdf11, rdf_term(?(any)), nondet).
type_modes(rdf11, out_date_time(+any, -any, +any), det).
type_modes(rdf11, rdf_lexical_form(++(any), -compound), det).
type_modes(rdf11, rdf_canonical_literal(++(any), -any), det).
type_modes(rdf11, in_xml_literal(+any, +any, -any), det).
type_modes(rdf11, xsd_date_time_type(?(any)), unknown).
type_modes(rdf11, xsd_numerical(?(any), ?(any), ?(any)), unknown).
type_modes(rdf11, in_number(+any, +any, +any, +any, -any), unknown).
type_modes(rdf11, in_boolean(?(any), ?(any)), unknown).
type_modes(rdf11, in_date_time(+any, +any, -any), det).
type_modes(rdf11, in_ground_type(+any, +any, -atom), det).
type_modes(rdf11, pre_ground_object(+any, -any), det).
type_modes(rdf11, rdf_default_graph(-any, +any), det).
type_modes(rdf11, rdf_default_graph(-any), det).
type_modes(rdf11, attr_unify_hook(+any, +any), unknown).
type_modes(rdf11, literal_class(+any, -any), unknown).
type_modes(rdf11, in_constaint_type(?(any), -any, ++(any), -any), unknown).
type_modes(rdf11, best_literal_cond(+any, -any, -any), semidet).
type_modes(rdf11, literal_condition(+any, -any), semidet).
type_modes(rdf11, add_value_constraint(?(any), +any, +any), unknown).
type_modes(rdf11, add_lang_constraint(?(any), +any), unknown).
type_modes(rdf11, rdf_where(+any), semidet).
type_modes(rdf11, {+any}, semidet).
type_modes(rdf11, rdf_compare(-any, +any, +any), det).
type_modes(rdf11, rdf_retractall(?(any), ?(any), ?(any), ?(any)), nondet).
type_modes(rdf11, rdf_retractall(?(any), ?(any), ?(any)), nondet).
type_modes(rdf11, rdf_assert(+any, +any, +any, +any), det).
type_modes(rdf11, rdf_assert(+any, +any, +any), det).
type_modes(rdf11, rdf_reachable(?(any), +any, ?(any), +any, -any), nondet).
type_modes(rdf11, rdf_reachable(?(any), +any, ?(any)), nondet).
type_modes(rdf11, rdf_has(?(any), ?(any), ?(any), -any), nondet).
type_modes(rdf11, rdf_has(?(any), ?(any), ?(any)), nondet).
type_modes(rdf11, rdf(?(any), ?(any), ?(any), ?(any)), nondet).
type_modes(rdf11, rdf(?(any), ?(any), ?(any)), nondet).
type_modes(sgml, xml_is_dom(@any), semidet).
type_modes(sgml, xml_extender(+any), semidet).
type_modes(sgml, xml_digit(+any), semidet).
type_modes(sgml, xml_combining_char(+any), semidet).
type_modes(sgml, xml_ideographic(+any), semidet).
type_modes(sgml, xml_basechar(+any), semidet).
type_modes(sgml, xml_name(+any), semidet).
type_modes(sgml, xml_quote_cdata(+any, -any), det).
type_modes(sgml, xml_quote_attribute(+any, -any), det).
type_modes(sgml, load_sgml(+any, -any, +any), det).
type_modes(sgml, load_xml(+any, -any, +any), det).
type_modes(sgml, load_html(+any, -any, +any), det).
type_modes(sgml, load_html_file(+any, -any), det).
type_modes(sgml, load_xml_file(+any, -any), det).
type_modes(sgml, load_sgml_file(+any, -any), det).
type_modes(sgml, set_input_location(+any, +stream), det).
type_modes(sgml, parser_meta_options(+any, +any, -any), unknown).
type_modes(sgml, load_structure(+any, -any, :(any)), det).
type_modes(sgml, register_cleanup, unknown).
type_modes(sgml, destroy_dtds, unknown).
type_modes(sgml, load_dtd(+any, +any, +any), unknown).
type_modes(sgml, dtd(+any, -any), det).
type_modes(isub, isub(+atomic, +atomic, +bool, -float), det).
type_modes(qsave, find_foreign_library(+any, -any), det).
type_modes(qsave, save_foreign_libraries(+any, +any), det).
type_modes(qsave, save_operators(+any), det).
type_modes(qsave, restore_prolog_flag(+any, +any, +any), unknown).
type_modes(qsave, restore_import(+any, +any, +any), det).
type_modes(qsave, save_imports, unknown).
type_modes(qsave, save_module(+any, +any), unknown).
type_modes(qsave, save_option_value(+any, +any, +any, -any), unknown).
type_modes(qsave, save_options(+any, +any, +any), unknown).
type_modes(qsave, qsave_program(+any, :(any)), det).
type_modes(qsave, qsave_program(+any), det).
type_modes(http_stream, cgi_statistics(?(any)), unknown).
type_modes(http_stream, http:current_transfer_encoding(?(any)), semidet).
type_modes(http_stream, http:encoding_filter(+any, +any, -any), semidet).
type_modes(http_stream, is_cgi_stream(+any), semidet).
type_modes(http_stream, cgi_discard(+any), det).
type_modes(http_stream, cgi_set(+any, ?(any)), det).
type_modes(http_stream, cgi_property(+any, ?(any)), det).
type_modes(http_stream, cgi_open(+any, -any, :(any), +any), det).
type_modes(http_stream, multipart_open_next(+any), semidet).
type_modes(http_stream, multipart_open(+any, -any, +any), det).
type_modes(http_stream, stream_range_open(+any, -any, +any), det).
type_modes(http_stream, http_chunked_open(+any, -any, +any), det).
type_modes(rdf_library, rdf_current_manifest(-any), nondet).
type_modes(rdf_library, define_namespace(?(ns(_, _))), det).
type_modes(rdf_library, normalize_url(+any, -any), unknown).
type_modes(rdf_library, library(?(any), ?(any), ?(any)), unknown).
type_modes(rdf_library, assert_ontology(+any, +library(_, _, _)), det).
type_modes(rdf_library, read_triples(+any, -any), det).
type_modes(rdf_library, source_time(+any, -any), semidet).
type_modes(rdf_library, edge(+any, ?(any), ?(any), ?(any)), nondet).
type_modes(rdf_library, facet(+any, +any, -any), nondet).
type_modes(rdf_library, extract_ontology(+any, -any), nondet).
type_modes(rdf_library, extract_namespace(+any, -any, -any), unknown).
type_modes(rdf_library, process_manifest(+any), det).
type_modes(rdf_library, rdf_update_library_index, unknown).
type_modes(rdf_library, rdf_attach_library(+any), unknown).
type_modes(rdf_library, rdf_library_index(?(any), ?(any)), nondet).
type_modes(rdf_library, rdf_list_library, unknown).
type_modes(rdf_library, rdf_list_library(+any, +any), det).
type_modes(rdf_library, rdf_list_library(+any), det).
type_modes(rdf_library, import(+any, +any, +any), det).
type_modes(rdf_library, load_commands(+any, +any, -list(_-_)), det).
type_modes(rdf_library, guess_concurrency(+any, -any), det).
type_modes(rdf_library, check_existence(+any, -any, +any), det).
type_modes(rdf_library, no_sources_in_same_graph(+any), semidet).
type_modes(rdf_library, no_source_with_different_options(+any), semidet).
type_modes(rdf_library, find_conflicts(+any), semidet).
type_modes(rdf_library, rdf_library_source(+any, -any), nondet).
type_modes(rdf_library, rdf_load_library(+any, +any), det).
type_modes(rdf_library, rdf_load_library(+any), det).
type_modes(nb_set, size_nb_set(+any, -any), unknown).
type_modes(nb_set, gen_nb_set(+any, -any), unknown).
type_modes(nb_set, nb_set_to_list(+any, -any), unknown).
type_modes(nb_set, hash_key(+any, +any, -any), det).
type_modes(nb_set, add_nb_set(+any, !(any), ?(any)), semidet).
type_modes(nb_set, add_nb_set(+any, !(any)), det).
type_modes(nb_set, empty_nb_set(-any), unknown).
type_modes(uri, delete_leading_slash(+any, -any), unknown).
type_modes(uri, ensure_leading_slash(+any, -any), unknown).
type_modes(uri, uri_file_name(-any, +any), det).
type_modes(uri, uri_file_name(+any, -any), semidet).
type_modes(uri, uri_iri(-any, +any), det).
type_modes(uri, uri_iri(+any, -any), det).
type_modes(uri, uri_encoded(+any, -any, +any), det).
type_modes(uri, uri_encoded(+any, +any, -any), det).
type_modes(uri, uri_authority_data(+any, ?(any), ?(any)), semidet).
type_modes(uri, uri_authority_components(-any, +any), det).
type_modes(uri, uri_authority_components(+any, -any), det).
type_modes(uri, uri_query_components(-any, +any), det).
type_modes(uri, uri_query_components(+any, -any), det).
type_modes(uri, uri_normalized_iri(+any, +any, -any), det).
type_modes(uri, iri_normalized(+any, +any, -any), det).
type_modes(uri, uri_normalized(+any, +any, -any), det).
type_modes(uri, uri_resolve(+any, +any, -any), det).
type_modes(uri, uri_is_global(+any), semidet).
type_modes(uri, uri_normalized_iri(+any, -any), det).
type_modes(uri, iri_normalized(+any, -any), det).
type_modes(uri, uri_normalized(+any, -any), det).
type_modes(uri, uri_data(+any, +any, +any, -any), semidet).
type_modes(uri, uri_data(?(any), +any, ?(any)), semidet).
type_modes(uri, uri_components(-any, +any), det).
type_modes(uri, uri_components(+any, -any), det).
type_modes(arithmetic, tidy(+any, -any), unknown).
type_modes(arithmetic, evaluable(?(any)), semidet).
type_modes(arithmetic, arithmetic_expression_value(:(any), -any), det).
type_modes(arithmetic, eval_clause(+any, -any), det).
type_modes(arithmetic, arithmetic_function(:(any)), det).

